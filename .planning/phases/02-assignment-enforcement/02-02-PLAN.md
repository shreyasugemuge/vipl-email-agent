---
phase: 02-assignment-enforcement
plan: 02
type: execute
wave: 2
depends_on: [02-01]
files_modified:
  - templates/emails/_thread_detail.html
  - templates/emails/_context_menu.html
  - templates/emails/_editable_priority.html
  - templates/emails/_editable_status.html
  - templates/emails/_editable_category.html
  - apps/emails/views.py
autonomous: false
requirements: [ROLE-03, ROLE-04, ROLE-05]

must_haves:
  truths:
    - "Admin/gatekeeper sees full Assign to dropdown in detail panel"
    - "Member sees Claim Thread button on unassigned threads in their category"
    - "Member sees disabled Claim Thread button with tooltip on threads outside their category"
    - "Member sees Reassign button on threads assigned to them"
    - "Member on others thread sees read-only assignee display with no action buttons"
    - "Inline reassign form has select (category-filtered users), reason textarea, Reassign Thread + Keep Thread buttons"
    - "Context menu shows Assign to for admin/gatekeeper, Claim for member unassigned, Reassign for member own thread, nothing for member others thread"
    - "Editable priority/status/category dropdowns hidden for member on others thread"
  artifacts:
    - path: "templates/emails/_thread_detail.html"
      provides: "Four-branch assignment UI based on role and thread ownership"
      contains: "reassign-form"
    - path: "templates/emails/_context_menu.html"
      provides: "Role-conditional assignment menu items"
      contains: "Reassign..."
    - path: "apps/emails/views.py"
      provides: "reassign_candidates and claim_disabled context variables in _build_thread_detail_context"
      contains: "reassign_candidates"
  key_links:
    - from: "templates/emails/_thread_detail.html reassign form"
      to: "apps/emails/views.py reassign_thread_view"
      via: "hx-post to reassign URL"
      pattern: "emails:reassign_thread"
    - from: "apps/emails/views.py _build_thread_detail_context"
      to: "apps/emails/models.py CategoryVisibility"
      via: "reassign_candidates query filtered by category"
      pattern: "CategoryVisibility.objects.filter"
---

<objective>
Update detail panel template with four-branch role-conditional assignment UI (assign/claim/reassign/read-only), add inline reassign form with exact UI-SPEC styling, update context menu with role-conditional items, enforce read-only view for members on others' threads by hiding editable dropdowns, and visually verify.

Purpose: Completes the user-facing assignment enforcement. Members see only the actions they're permitted to take. The UI matches the approved UI-SPEC design contract.
Output: Updated templates with role-conditional rendering. Visual verification by user.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-assignment-enforcement/02-CONTEXT.md
@.planning/phases/02-assignment-enforcement/02-RESEARCH.md
@.planning/phases/02-assignment-enforcement/02-UI-SPEC.md
@.planning/phases/02-assignment-enforcement/02-01-SUMMARY.md

<interfaces>
<!-- From Plan 01: new URL and service -->
```python
# apps/emails/urls.py
path("threads/<int:pk>/reassign/", views.reassign_thread_view, name="reassign_thread")

# apps/emails/services/assignment.py
def reassign_thread(thread, new_assignee, reassigned_by, reason): ...
```

<!-- _build_thread_detail_context signature (apps/emails/views.py ~line 869) -->
<!-- Note: is_admin parameter may have been renamed by Phase 1 Plan 02 -->
```python
def _build_thread_detail_context(thread, request, is_admin, team_members):
    # Returns context dict with keys: thread, emails, notes, activity_logs,
    # is_admin, can_claim, team_members, etc.
```

<!-- CategoryVisibility model -->
```python
class CategoryVisibility(TimestampedModel):
    user = models.ForeignKey(User, on_delete=models.CASCADE)
    category = ...
```

<!-- UI-SPEC exact values -->
# Button labels: "Claim Thread", "Reassign", "Reassign Thread", "Keep Thread", "Assign to..."
# Colors: emerald-500 for Claim, amber-500 for Reassign buttons, white+slate-200 border for Keep Thread
# Typography: text-[11px] font-medium for labels, text-[10px] font-bold for buttons
# Disabled claim: opacity-50 cursor-not-allowed, title="This thread is outside your assigned categories"
# Reassign form container: bg-slate-50 border border-slate-200 rounded-lg, p-3 mt-2
# Context menu reassign: "Reassign..." with keyboard hint R
# 403 messages: "Only gatekeepers and admins can assign threads to other users."
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add reassign_candidates + claim_disabled to view context and update detail panel + context menu + editable templates</name>
  <files>apps/emails/views.py, templates/emails/_thread_detail.html, templates/emails/_context_menu.html, templates/emails/_editable_priority.html, templates/emails/_editable_status.html, templates/emails/_editable_category.html</files>
  <read_first>
    - apps/emails/views.py
    - templates/emails/_thread_detail.html
    - templates/emails/_context_menu.html
    - templates/emails/_editable_priority.html
    - templates/emails/_editable_status.html
    - templates/emails/_editable_category.html
    - .planning/phases/02-assignment-enforcement/02-UI-SPEC.md
    - .planning/phases/02-assignment-enforcement/02-CONTEXT.md
    - apps/emails/models.py
  </read_first>
  <action>
**Part A: Update _build_thread_detail_context in apps/emails/views.py**

Find `_build_thread_detail_context()` and add two new context variables: `reassign_candidates` and `claim_disabled`.

After the existing `can_claim` computation, add:

```python
# Claim disabled state (member sees disabled button with tooltip for out-of-category threads)
claim_disabled = False
if thread.assigned_to is None and not is_admin:  # Note: adapt to can_assign if Phase 1 Plan 02 has renamed
    if not can_claim:
        claim_disabled = True

# Reassign candidates (category-filtered active users for member reassign form)
reassign_candidates = []
if not is_admin and thread.assigned_to == request.user:  # adapt to can_assign check
    reassign_candidates = User.objects.filter(
        is_active=True,
        pk__in=CategoryVisibility.objects.filter(
            category=thread.category
        ).values_list("user_id", flat=True)
    ).exclude(pk=request.user.pk).order_by("first_name", "username")
```

Add to the returned context dict:
```python
"claim_disabled": claim_disabled,
"reassign_candidates": reassign_candidates,
```

Ensure `CategoryVisibility` is imported. Check existing imports at top of views.py.

Also ensure `User` is imported (likely already is from `apps.accounts.models`).

**Part B: Update templates/emails/_thread_detail.html -- Four-branch assignment UI**

Find the current assignment section (the area with `{% if is_admin %}` or `{% if can_assign %}` for the assign dropdown). Replace it with the four-branch structure. Use exact UI-SPEC values.

```html
{# -- Assignment Action Bar -- #}

{# Branch 1: Admin/Gatekeeper -- full assign dropdown (existing, keep as-is) #}
{% if can_assign %}
    {# Existing assign form -- keep unchanged. It uses the full team_members dropdown #}
    {# ... existing assign form code ... #}

{# Branch 2: Member on their own assigned thread -- Reassign button + inline form #}
{% elif thread.assigned_to == request.user %}
    <div class="flex items-center gap-2">
        <div class="flex items-center gap-2">
            {% if thread.assigned_to.avatar_url %}
            <img src="{{ thread.assigned_to.avatar_url }}" alt="" class="w-5 h-5 rounded-full">
            {% endif %}
            <span class="text-[11px] font-medium text-slate-700">{{ thread.assigned_to.get_full_name|default:thread.assigned_to.username }}</span>
        </div>
        <button type="button"
                onclick="document.getElementById('reassign-form-{{ thread.pk }}').style.display = document.getElementById('reassign-form-{{ thread.pk }}').style.display === 'none' ? 'block' : 'none'"
                class="px-3 py-2 text-[10px] font-bold text-white bg-amber-500 rounded-md hover:bg-amber-600 transition-colors">
            Reassign
        </button>
    </div>

    {# Inline reassign form -- hidden by default #}
    <div id="reassign-form-{{ thread.pk }}" style="display: none" class="mt-2 p-3 bg-slate-50 rounded-lg border border-slate-200">
        <form hx-post="{% url 'emails:reassign_thread' thread.pk %}"
              hx-target="#thread-detail-panel" hx-swap="innerHTML">
            {% csrf_token %}
            <label class="block text-[11px] font-medium text-slate-600 mb-1">Reassign to</label>
            <select name="assignee_id" required
                    class="w-full text-[11px] font-medium border border-slate-200 rounded-md px-2 py-2 mb-2 bg-white">
                <option value="">Select team member...</option>
                {% for candidate in reassign_candidates %}
                <option value="{{ candidate.pk }}">{{ candidate.get_full_name|default:candidate.username }}</option>
                {% endfor %}
            </select>
            {% if not reassign_candidates %}
            <p class="text-[11px] font-medium text-slate-400 mb-2">No eligible team members for this category</p>
            {% endif %}
            <label class="block text-[11px] font-medium text-slate-600 mb-1">Reason (required)</label>
            <textarea name="reason" required rows="2"
                      placeholder="Why are you reassigning this thread?"
                      class="w-full text-[11px] font-medium border border-slate-200 rounded-md px-2 py-2 mb-2 resize-none"></textarea>
            <div class="flex gap-2">
                <button type="submit" hx-disabled-elt="this"
                        class="px-3 py-2 text-[10px] font-bold text-white bg-amber-500 rounded-md hover:bg-amber-600 transition-colors">
                    Reassign Thread
                </button>
                <button type="button"
                        onclick="this.closest('[id^=reassign-form]').style.display='none'"
                        class="px-3 py-2 text-[10px] font-bold text-slate-500 bg-white border border-slate-200 rounded-md hover:bg-slate-50 transition-colors">
                    Keep Thread
                </button>
            </div>
        </form>
    </div>

{# Branch 3: Member on unassigned thread in their category -- Claim button #}
{% elif can_claim %}
    {# Existing claim form -- keep as-is #}
    {# ... existing claim button code ... #}

{# Branch 3b: Member on unassigned thread outside their category -- disabled Claim #}
{% elif claim_disabled %}
    <button disabled
            title="This thread is outside your assigned categories"
            class="px-3 py-2 text-[10px] font-bold text-white bg-emerald-500 rounded-md opacity-50 cursor-not-allowed">
        Claim Thread
    </button>

{# Branch 4: Member on someone else's thread -- read-only #}
{% elif thread.assigned_to %}
    <div class="flex items-center gap-2">
        {% if thread.assigned_to.avatar_url %}
        <img src="{{ thread.assigned_to.avatar_url }}" alt="" class="w-5 h-5 rounded-full">
        {% endif %}
        <span class="text-[11px] font-medium text-slate-700">Assigned to {{ thread.assigned_to.get_full_name|default:thread.assigned_to.username }}</span>
    </div>
{% endif %}
```

**Read-only enforcement on status/priority actions:** Find the existing condition that gates the Acknowledge/Close/status action buttons. It is currently something like `{% if thread.assigned_to == request.user or is_admin %}`. Ensure it uses `{% if thread.assigned_to == request.user or can_assign %}` (or `is_admin` if Phase 1 Plan 02 hasn't run -- adapt to whichever variable name exists in the template). The key: members on others' threads must NOT see these buttons.

**Part C: Update templates/emails/_context_menu.html**

Find the assignment section (currently `{% if is_admin %}` or `{% if can_assign %}` for "Assign to..."). Replace with four conditions per UI-SPEC:

```html
{# Assignment group #}

{# Admin/Gatekeeper: full assign submenu #}
{% if can_assign %}
    {# Existing "Assign to..." menu item -- keep as-is #}
{% endif %}

{# Member on unassigned thread in their category: Claim #}
{% if can_claim and not can_assign %}
    {# Existing "Claim" menu item -- keep as-is #}
{% endif %}

{# Member on own thread: Reassign option #}
{% if not can_assign and thread.assigned_to == request.user %}
<button class="ctx-item flex items-center justify-between w-full px-3 py-2 text-[12px] font-medium hover:bg-slate-700/60 transition-colors"
        data-action="reassign"
        hx-get="{% url 'emails:thread_detail' thread.pk %}"
        hx-target="#thread-detail-panel" hx-swap="innerHTML"
        @click="contextMenu.open = false">
    <span class="flex items-center gap-2">
        <svg class="w-3.5 h-3.5 opacity-60" fill="none" stroke="currentColor" stroke-width="2" viewBox="0 0 24 24">
            <path stroke-linecap="round" stroke-linejoin="round" d="M7.5 21L3 16.5m0 0L7.5 12M3 16.5h13.5m0-13.5L21 7.5m0 0L16.5 12M21 7.5H7.5"/>
        </svg>
        Reassign...
    </span>
    <span class="text-slate-500 text-[10px] font-bold ml-4">R</span>
</button>
{% endif %}

{# Member on others' threads: no assignment items shown #}
```

Add keyboard shortcut handler for R key in the context menu JavaScript section (find where existing keyboard shortcuts A, C are handled):
```javascript
case 'r':
    // Reassign (member on own thread)
    const reassignBtn = menu.querySelector('[data-action="reassign"]');
    if (reassignBtn) reassignBtn.click();
    break;
```

**Part D: Update editable templates for read-only enforcement**

For each of these three templates, find the editable dropdown/form and wrap it in a permission check. Members on others' threads should see the value as plain text, not an editable dropdown.

**templates/emails/_editable_priority.html:**
Find the existing content. Wrap the editable `<select>` or form in:
```html
{% if thread.assigned_to == request.user or can_assign %}
    {# Existing editable dropdown #}
{% else %}
    {# Read-only display #}
    <span class="...">{{ thread.get_priority_display }}</span>
{% endif %}
```

**templates/emails/_editable_status.html:**
Same pattern -- editable for owner/admin/gatekeeper, read-only text for others.

**templates/emails/_editable_category.html:**
Same pattern.

Use the existing badge/chip styling from the template for the read-only display. Read each template first to understand its current structure and adapt the read-only display to match the existing visual style (colors, size classes).

**Part E: Update _build_thread_detail_context to pass can_assign to template**

Ensure the context dict includes `"can_assign": is_admin` (or `"can_assign": can_assign` if Phase 1 Plan 02 has already renamed it). The templates need `can_assign` as a boolean to gate the four branches.

If Phase 1 Plan 02 has already replaced `is_admin` with `can_assign` in the context, no change needed. If not, add:
```python
"can_assign": is_admin,  # Will be properly replaced by Phase 1 Plan 02
```

Also ensure the context menu view passes the same variables. Check `context_menu_view` in views.py -- it likely builds its own context dict. Ensure it includes `can_assign`, `can_claim`, and passes `thread.assigned_to` so the template conditions work.

**Part F: Activity log display for REASSIGNED_BY_MEMBER**

Find where ActivityLog entries are rendered in the detail panel (likely `_thread_detail.html` or a partial). Ensure `REASSIGNED_BY_MEMBER` displays correctly. The existing ActivityLog rendering likely uses a generic pattern like:
```html
{{ log.get_action_display }}: {{ log.detail }}
```

Verify this works for the new action type. The `get_action_display` will return "Reassigned by Member" (from the TextChoices label). The `detail` field contains the reason. If the template has special formatting per action type, add a case for `reassigned_by_member`:
```html
{% if log.action == "reassigned_by_member" %}
<span>Reassigned by {{ log.user.get_full_name }} to {{ log.new_value }}: {{ log.detail }}</span>
{% endif %}
```

Match the UI-SPEC copy: "Reassigned by {user} to {new_assignee}: {reason}"
  </action>
  <verify>
    <automated>cd /Users/uge/code/vipl-email-agent-gatekeeper && grep -c "reassign-form" templates/emails/_thread_detail.html && grep -c "Reassign\.\.\." templates/emails/_context_menu.html && grep -c "reassign_candidates" apps/emails/views.py && grep -c "claim_disabled" apps/emails/views.py && pytest -x -q</automated>
  </verify>
  <acceptance_criteria>
    - templates/emails/_thread_detail.html contains `id="reassign-form-{{ thread.pk }}"` (inline reassign form)
    - templates/emails/_thread_detail.html contains `bg-amber-500` on Reassign button
    - templates/emails/_thread_detail.html contains `bg-emerald-500` on Claim Thread button (or existing claim button preserved)
    - templates/emails/_thread_detail.html contains `"Reassign Thread"` as submit button text (exact copy from UI-SPEC)
    - templates/emails/_thread_detail.html contains `"Keep Thread"` as cancel button text (exact copy from UI-SPEC)
    - templates/emails/_thread_detail.html contains `"Select team member..."` as placeholder option (exact copy from UI-SPEC)
    - templates/emails/_thread_detail.html contains `"Why are you reassigning this thread?"` as textarea placeholder
    - templates/emails/_thread_detail.html contains `"Reason (required)"` as textarea label
    - templates/emails/_thread_detail.html contains disabled claim button with `title="This thread is outside your assigned categories"` (exact tooltip from UI-SPEC)
    - templates/emails/_thread_detail.html contains `opacity-50 cursor-not-allowed` on disabled claim button
    - templates/emails/_thread_detail.html form posts to `{% url 'emails:reassign_thread' thread.pk %}`
    - templates/emails/_thread_detail.html contains `hx-disabled-elt="this"` on Reassign Thread submit button
    - templates/emails/_thread_detail.html contains `"No eligible team members for this category"` empty state text
    - templates/emails/_context_menu.html contains `Reassign...` menu item with keyboard hint `R`
    - templates/emails/_context_menu.html contains condition `not can_assign and thread.assigned_to == request.user` for Reassign item
    - templates/emails/_editable_priority.html contains `can_assign` or `is_admin` gate with read-only fallback
    - templates/emails/_editable_status.html contains `can_assign` or `is_admin` gate with read-only fallback
    - templates/emails/_editable_category.html contains `can_assign` or `is_admin` gate with read-only fallback
    - apps/emails/views.py `_build_thread_detail_context` returns `reassign_candidates` in context
    - apps/emails/views.py `_build_thread_detail_context` returns `claim_disabled` in context
    - `text-[11px] font-medium` used for labels and body text in reassign form
    - `text-[10px] font-bold` used for button text in reassign form
    - `p-3` padding on reassign form container
    - `mt-2` margin top on reassign form container
    - `flex gap-2` on button row
    - `px-3 py-2` on all buttons
    - `pytest -x -q` exits 0 (full suite still green)
  </acceptance_criteria>
  <done>Detail panel shows four-branch role-conditional UI. Inline reassign form matches UI-SPEC exactly (button labels, colors, spacing, typography). Context menu shows role-appropriate items. Editable dropdowns hidden for members on others' threads. All tests pass.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Visual verification of assignment enforcement</name>
  <files>N/A</files>
  <action>
Human verifies the complete assignment enforcement UI across all three roles.

What was built: Complete assignment enforcement -- server-side guards, role-conditional detail panel (4 branches), inline reassign form, context menu, read-only view for members on others' threads.

Steps to verify:
1. Start dev server: `python manage.py runserver 8000`
2. Visit http://triage.local/dev-login/

Test as Admin:
3. Log in as Admin
4. Open any thread detail -- verify full "Assign to" dropdown with all active users visible
5. Right-click a thread card -- verify "Assign to..." in context menu with "A" keyboard hint

Test as Triage Lead (gatekeeper):
6. Log in as Triage Lead
7. Open any thread detail -- verify same full "Assign to" dropdown (same as admin)
8. Right-click thread card -- verify "Assign to..." in context menu

Test as Member on own thread:
9. Log in as Member
10. Open a thread assigned to you -- verify:
    - Assignee display (avatar + name) visible
    - Amber "Reassign" button visible
    - Click "Reassign" -- inline form appears with:
      - "Reassign to" label + select dropdown (only category-eligible users)
      - "Reason (required)" label + textarea with placeholder "Why are you reassigning this thread?"
      - Amber "Reassign Thread" submit button
      - White "Keep Thread" cancel button
    - Click "Keep Thread" -- form hides
    - Fill form and submit -- thread reassigns, detail panel updates, card updates
    - Check activity log shows "Reassigned by Member" entry with reason
11. Right-click own thread card -- verify "Reassign..." with "R" keyboard hint

Test as Member on unassigned thread (in category):
12. Open an unassigned thread in your category -- verify emerald "Claim Thread" button visible
13. Claim it -- verify success

Test as Member on unassigned thread (outside category):
14. Open an unassigned thread outside your category -- verify:
    - "Claim Thread" button is grayed out (opacity-50)
    - Hover shows tooltip "This thread is outside your assigned categories"
    - Button is not clickable

Test as Member on someone else's thread:
15. Open a thread assigned to another user -- verify:
    - Read-only "Assigned to [Name]" display
    - NO Reassign, Claim, Acknowledge, Close buttons
    - Priority/status/category shown as plain text (not editable dropdowns)
    - Can still read messages, view activity log, add internal notes
16. Right-click that thread card -- verify NO assignment options in context menu
  </action>
  <verify>User confirms all visual and functional checks pass</verify>
  <done>All 3 roles verified: admin/gatekeeper sees full assign, member sees claim/reassign/read-only as appropriate. Type "approved" or describe issues.</done>
</task>

</tasks>

<verification>
```bash
# Reassign form exists in detail template
grep "reassign-form" templates/emails/_thread_detail.html

# Context menu has Reassign option
grep "Reassign\.\.\." templates/emails/_context_menu.html

# View context passes reassign_candidates
grep "reassign_candidates" apps/emails/views.py

# UI-SPEC button labels used verbatim
grep "Reassign Thread" templates/emails/_thread_detail.html
grep "Keep Thread" templates/emails/_thread_detail.html
grep "Select team member\.\.\." templates/emails/_thread_detail.html

# Disabled claim tooltip exact text
grep "This thread is outside your assigned categories" templates/emails/_thread_detail.html

# Full test suite
pytest -x -q
```
</verification>

<success_criteria>
- Detail panel shows 4 role-conditional branches: assign dropdown / claim / reassign / read-only
- Inline reassign form uses exact UI-SPEC values: amber-500 buttons, "Reassign Thread" / "Keep Thread" labels, text-[10px] font-bold buttons, text-[11px] font-medium labels
- Disabled claim button has opacity-50, cursor-not-allowed, and exact tooltip text
- Context menu shows "Reassign..." with R keyboard hint for member on own thread
- Editable dropdowns (priority/status/category) hidden for members on others' threads
- Activity log displays "Reassigned by Member" with reason
- All tests pass
- User approves visual verification
</success_criteria>

<output>
After completion, create `.planning/phases/02-assignment-enforcement/02-02-SUMMARY.md`
</output>
