---
phase: 01-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - manage.py
  - config/__init__.py
  - config/settings/__init__.py
  - config/settings/base.py
  - config/settings/dev.py
  - config/settings/prod.py
  - config/urls.py
  - config/wsgi.py
  - config/asgi.py
  - apps/__init__.py
  - apps/accounts/__init__.py
  - apps/accounts/models.py
  - apps/accounts/admin.py
  - apps/accounts/views.py
  - apps/accounts/urls.py
  - apps/accounts/apps.py
  - apps/accounts/tests/__init__.py
  - apps/accounts/tests/test_models.py
  - apps/accounts/tests/test_auth.py
  - apps/accounts/tests/test_admin.py
  - apps/emails/__init__.py
  - apps/emails/models.py
  - apps/emails/admin.py
  - apps/emails/apps.py
  - apps/emails/tests/__init__.py
  - apps/core/__init__.py
  - apps/core/models.py
  - apps/core/views.py
  - apps/core/urls.py
  - apps/core/apps.py
  - apps/core/tests/__init__.py
  - apps/core/tests/test_health.py
  - templates/base.html
  - templates/registration/login.html
  - static/.gitkeep
  - requirements.txt
  - requirements-dev.txt
  - gunicorn.conf.py
  - .env.example
  - pytest.ini
  - conftest.py
autonomous: true
requirements:
  - AUTH-01
  - AUTH-02
  - AUTH-03
  - INFR-01
  - INFR-06
  - INFR-12

must_haves:
  truths:
    - "User can log in with username and password and is redirected to a logged-in page"
    - "User without credentials is redirected to the login page"
    - "Admin user can access Django admin and manage team members (add, remove, set role)"
    - "Team member user has role=member and can_see_all_emails flag"
    - "Health endpoint returns JSON with status, version, uptime, database connectivity"
    - "All models use soft delete (deleted_at field, default manager excludes deleted)"
  artifacts:
    - path: "apps/accounts/models.py"
      provides: "Custom User model with role and can_see_all_emails fields"
      contains: "class User(AbstractUser)"
    - path: "apps/accounts/admin.py"
      provides: "Custom UserAdmin with role and visibility fields"
      contains: "class UserAdmin"
    - path: "apps/core/models.py"
      provides: "SoftDeleteModel and TimestampedModel base classes"
      contains: "class SoftDeleteModel"
    - path: "apps/core/views.py"
      provides: "Health check endpoint returning JSON"
      contains: "def health_check"
    - path: "apps/emails/models.py"
      provides: "Email and AttachmentMetadata models for Phase 2"
      contains: "class Email"
    - path: "config/settings/base.py"
      provides: "Django settings with AUTH_USER_MODEL set before first migration"
      contains: "AUTH_USER_MODEL"
  key_links:
    - from: "config/settings/base.py"
      to: "apps/accounts/models.py"
      via: "AUTH_USER_MODEL = 'accounts.User'"
      pattern: "AUTH_USER_MODEL.*accounts\\.User"
    - from: "config/urls.py"
      to: "apps/core/urls.py"
      via: "include('apps.core.urls')"
      pattern: "include.*apps\\.core"
    - from: "config/urls.py"
      to: "apps/accounts/urls.py"
      via: "include('apps.accounts.urls')"
      pattern: "include.*apps\\.accounts"
    - from: "apps/emails/models.py"
      to: "apps/core/models.py"
      via: "Email extends SoftDeleteModel + TimestampedModel"
      pattern: "SoftDeleteModel.*TimestampedModel|TimestampedModel.*SoftDeleteModel"
---

<objective>
Create the Django 5.2 project skeleton with PostgreSQL models, custom user authentication, team management via Django admin, health endpoint, and test infrastructure. This is the application code that Plan 02 will containerize and deploy.

Purpose: Establish the foundation codebase -- custom user model (MUST be done before first migration), base model patterns (soft delete), email schema stubs, auth views, and health endpoint. Everything else in the project builds on these models and patterns.

Output: A working Django project that passes all unit tests locally, with login/logout, admin panel for user management, health endpoint, and email models ready for Phase 2.
</objective>

<execution_context>
@/Users/uge/.claude/get-shit-done/workflows/execute-plan.md
@/Users/uge/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation/01-CONTEXT.md
@.planning/phases/01-foundation/01-RESEARCH.md
@.planning/phases/01-foundation/01-VALIDATION.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Django project structure, settings, base models, and test infrastructure</name>
  <files>
    manage.py, config/__init__.py, config/settings/__init__.py, config/settings/base.py,
    config/settings/dev.py, config/settings/prod.py, config/urls.py, config/wsgi.py, config/asgi.py,
    apps/__init__.py, apps/core/__init__.py, apps/core/models.py, apps/core/views.py,
    apps/core/urls.py, apps/core/apps.py, apps/core/tests/__init__.py, apps/core/tests/test_health.py,
    apps/accounts/__init__.py, apps/accounts/apps.py, apps/accounts/tests/__init__.py,
    apps/emails/__init__.py, apps/emails/apps.py, apps/emails/tests/__init__.py,
    templates/base.html, static/.gitkeep,
    requirements.txt, requirements-dev.txt, gunicorn.conf.py, .env.example, pytest.ini, conftest.py
  </files>
  <behavior>
    - Test: SoftDeleteModel.delete() sets deleted_at instead of removing row
    - Test: SoftDeleteModel.objects excludes soft-deleted records
    - Test: SoftDeleteModel.all_objects includes soft-deleted records
    - Test: SoftDeleteModel.hard_delete() actually removes the row
    - Test: TimestampedModel auto-sets created_at and updated_at
    - Test: Health endpoint returns 200 with JSON containing status, version, uptime_seconds, database
    - Test: Health endpoint returns "healthy" when DB is connected
  </behavior>
  <action>
    1. Create Django project structure following the multi-app layout from RESEARCH.md:
       - `config/` with split settings (base.py, dev.py, prod.py)
       - `apps/` directory with `core`, `accounts`, `emails` apps
       - Each app configured in INSTALLED_APPS as `apps.core`, `apps.accounts`, `apps.emails`

    2. `config/settings/base.py`: Set AUTH_USER_MODEL = "accounts.User" BEFORE any migrations.
       Include django.contrib.auth, django.contrib.admin, django.contrib.sessions, whitenoise middleware.
       Set LOGIN_URL, LOGIN_REDIRECT_URL, LOGOUT_REDIRECT_URL.

    3. `config/settings/dev.py`: Load .env via python-dotenv, DEBUG=True, DATABASES using SQLite for dev default (or DATABASE_URL if set via dj-database-url).

    4. `config/settings/prod.py`: DEBUG=False, ALLOWED_HOSTS from env, DATABASES from DATABASE_URL, SECURE_* settings, whitenoise staticfiles storage.

    5. `apps/core/models.py`: Create SoftDeleteManager, SoftDeleteModel (abstract), TimestampedModel (abstract) exactly as specified in RESEARCH.md. SoftDeleteModel overrides delete() to set deleted_at, provides hard_delete() for actual removal. Two managers: objects (filtered) and all_objects (unfiltered).

    6. `apps/core/views.py`: Health check endpoint returning JSON with status (healthy/degraded), version (from APP_VERSION env var), uptime_seconds, database connectivity. Returns 503 if DB unreachable.

    7. `apps/core/urls.py`: Wire health endpoint to `/health/`.

    8. `config/urls.py`: Include admin, accounts URLs, core URLs. Wire `/health/` at root level via core.urls.

    9. `requirements.txt`: django==5.2.*, psycopg[binary], gunicorn, whitenoise, dj-database-url, python-dotenv

    10. `requirements-dev.txt`: -r requirements.txt, pytest, pytest-django, factory-boy

    11. `gunicorn.conf.py`: bind 0.0.0.0:8000, 2 workers, 2 threads, timeout 120, access/error log to stdout.

    12. `pytest.ini`: Set DJANGO_SETTINGS_MODULE=config.settings.dev, testpaths=apps, python_files=test_*.py.

    13. `conftest.py`: Shared fixtures -- admin_user (role=admin), member_user (role=member), api_client (Django test client).

    14. `.env.example`: Template with DATABASE_URL, SECRET_KEY, DEBUG, ALLOWED_HOSTS, APP_VERSION.

    15. `templates/base.html`: Minimal HTML5 base template with block content.

    16. Write tests FIRST (RED), then implement (GREEN):
        - `apps/core/tests/test_health.py`: Test health endpoint returns 200 + correct JSON structure
        - Test SoftDeleteModel behavior (create a concrete test model in tests)
  </action>
  <verify>
    <automated>cd /Users/uge/code/vipl-email-agent && pip install -r requirements-dev.txt -q && pytest apps/core/tests/ --tb=short -q</automated>
  </verify>
  <done>
    Django project runs `python manage.py check` without errors. Health endpoint test passes. SoftDeleteModel tests pass.
    AUTH_USER_MODEL is set to accounts.User in base settings. All test infrastructure (pytest.ini, conftest.py, requirements-dev.txt) is in place.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Custom User model, auth views, email models, Django admin, and full test suite</name>
  <files>
    apps/accounts/models.py, apps/accounts/admin.py, apps/accounts/views.py, apps/accounts/urls.py,
    apps/accounts/tests/test_models.py, apps/accounts/tests/test_auth.py, apps/accounts/tests/test_admin.py,
    apps/emails/models.py, apps/emails/admin.py,
    templates/registration/login.html
  </files>
  <behavior>
    - Test: User.role defaults to "member"
    - Test: User.can_see_all_emails defaults to False
    - Test: User.is_admin_role returns True for admin, False for member
    - Test: Admin user (role=admin, is_staff=True) can access Django admin
    - Test: Admin can create a new user via Django admin with role field
    - Test: Unauthenticated request to protected page redirects to login
    - Test: Authenticated user can access protected page
    - Test: Login with valid credentials succeeds
    - Test: Login with invalid credentials fails
    - Test: Logout redirects to login page
    - Test: Email model has all required fields (message_id, from_address, to_inbox, subject, body, headers, received_at, etc.)
    - Test: Email model uses soft delete
    - Test: AttachmentMetadata model stores filename, size, mime_type, linked to Email
  </behavior>
  <action>
    1. `apps/accounts/models.py`: Custom User extending AbstractUser with:
       - role: CharField with choices (ADMIN="admin", MEMBER="member"), default=MEMBER
       - can_see_all_emails: BooleanField, default=False, help_text about visibility
       - is_admin_role property
       Per user decision: "Two roles: Admin and Team Member" and "Visibility is configurable per-user"

    2. `apps/accounts/admin.py`: Custom UserAdmin extending Django's UserAdmin:
       - Add role and can_see_all_emails to fieldsets and list_display
       - Add role to list_filter
       Per user decision: "Team member creation starts via Django admin panel"

    3. `apps/accounts/views.py`: Login view using Django's built-in LoginView, logout using LogoutView.
       A simple dashboard placeholder view (protected with LoginRequiredMixin) that shows "Welcome, {user}" -- this is the LOGIN_REDIRECT_URL target.

    4. `apps/accounts/urls.py`: Wire login, logout, and dashboard placeholder.

    5. `templates/registration/login.html`: Minimal login form extending base.html. Username + password fields, submit button, error display. No styling needed yet (Tailwind comes later).

    6. `apps/emails/models.py`: Define models for Phase 2 consumption (models only, no views yet):
       - Email(SoftDeleteModel, TimestampedModel): message_id (unique), from_address, from_name, to_inbox, subject, body (TextField), headers (JSONField), received_at, gmail_id, gmail_thread_id, gmail_labels (JSONField), category, priority, ai_summary, ai_draft_reply, status (New/Acknowledged/Replied/Closed default New), assigned_to (FK to User, nullable), assigned_by (FK to User, nullable), assigned_at
       - AttachmentMetadata(TimestampedModel): email (FK to Email), filename, size_bytes, mime_type, gmail_attachment_id
       Per user decision: "Store full email body + headers + metadata" and "Attachment metadata only"

    7. `apps/emails/admin.py`: Register Email and AttachmentMetadata with basic admin config.

    8. Run `python manage.py makemigrations` and `python manage.py migrate` to create initial migrations.

    9. Write tests FIRST, then implement:
       - `apps/accounts/tests/test_models.py`: User role defaults, is_admin_role property, can_see_all_emails default
       - `apps/accounts/tests/test_auth.py`: Login success/failure, logout, protected view redirect
       - `apps/accounts/tests/test_admin.py`: Admin user can access admin panel, create user with role field
  </action>
  <verify>
    <automated>cd /Users/uge/code/vipl-email-agent && pytest apps/ --tb=short -q</automated>
  </verify>
  <done>
    All tests pass. Custom User model with role and can_see_all_emails works. Login/logout functional.
    Django admin shows users with role field. Email and AttachmentMetadata models created with soft delete.
    Migrations generated and applied successfully. `python manage.py check` passes.
  </done>
</task>

</tasks>

<verification>
1. `python manage.py check` passes with no errors
2. `python manage.py migrate` runs without errors (all migrations applied)
3. `pytest apps/ --tb=short -q` -- all tests green
4. `python manage.py createsuperuser` creates an admin user successfully
5. Health endpoint at /health/ returns JSON with status, version, uptime, database fields
6. Login page renders at /accounts/login/
7. Unauthenticated access to dashboard redirects to login
</verification>

<success_criteria>
- Django project structure matches RESEARCH.md multi-app layout
- AUTH_USER_MODEL = "accounts.User" set before first migration
- Custom User model has role (admin/member) and can_see_all_emails fields
- SoftDeleteModel base class works (delete sets deleted_at, objects filters, all_objects doesn't)
- Email model has all fields for Phase 2 (body, headers, category, priority, status, assignment)
- Health endpoint returns JSON with system status
- Django admin shows User with role and can_see_all_emails fields
- Login/logout works with Django's built-in auth
- All unit tests pass
- Test infrastructure (pytest, fixtures, conftest) fully operational
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-01-SUMMARY.md`
</output>
