---
phase: 04-page-polish
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - templates/emails/settings.html
  - templates/emails/activity_log.html
  - templates/emails/_activity_feed.html
  - apps/emails/views.py
autonomous: true
requirements: [PAGE-02, PAGE-03]

must_haves:
  truths:
    - "Settings tabs are visually grouped into 3 sections with labels"
    - "Settings tab names are descriptive (Assignment Rules, Team Visibility, SLA Targets, Chat Webhooks, Email Inboxes, System Config, Spam Whitelist)"
    - "Each settings tab content panel has a bold title + description header"
    - "Activity page shows events grouped by thread (not flat date list)"
    - "Clicking a thread group header navigates to thread detail"
    - "Activity stat cards and filter chips still work"
  artifacts:
    - path: "templates/emails/settings.html"
      provides: "Grouped tab bar with section headers and dividers"
      contains: "Assignment"
    - path: "templates/emails/activity_log.html"
      provides: "Activity page with stat cards and filter chips"
    - path: "templates/emails/_activity_feed.html"
      provides: "Thread-grouped activity entries"
      contains: "thread"
    - path: "apps/emails/views.py"
      provides: "Thread-grouped queryset for activity log"
  key_links:
    - from: "apps/emails/views.py"
      to: "templates/emails/_activity_feed.html"
      via: "thread_groups context variable"
      pattern: "thread_groups|grouped_by_thread"
    - from: "templates/emails/_activity_feed.html"
      to: "/emails/threads/<pk>/detail/"
      via: "thread header link"
      pattern: "threads.*detail"
    - from: "templates/emails/settings.html"
      to: "switchTab JS function"
      via: "tab click handlers"
      pattern: "switchTab"
---

<objective>
Reorganize settings tabs into 3 labeled groups with dividers, and redesign the activity page to group events by thread instead of flat date list.

Purpose: PAGE-02 makes settings easier to navigate by grouping related tabs. PAGE-03 gives activity entries context by showing them within their thread.
Output: Grouped settings tab bar, thread-grouped activity feed.
</objective>

<execution_context>
@/Users/uge/.claude/get-shit-done/workflows/execute-plan.md
@/Users/uge/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-page-polish/04-CONTEXT.md

<interfaces>
<!-- Key types and contracts the executor needs -->

From apps/emails/models.py (ActivityLog):
```python
class ActivityLog(TimestampedModel):
    class Action(models.TextChoices):
        ASSIGNED = "assigned", "Assigned"
        REASSIGNED = "reassigned", "Reassigned"
        STATUS_CHANGED = "status_changed", "Status Changed"
        ACKNOWLEDGED = "acknowledged", "Acknowledged"
        CLOSED = "closed", "Closed"
        AUTO_ASSIGNED = "auto_assigned", "Auto-Assigned"
        CLAIMED = "claimed", "Claimed"
        # ... plus SLA_BREACHED, PRIORITY_BUMPED, NEW_EMAIL_RECEIVED, REOPENED,
        # THREAD_CREATED, NOTE_ADDED, MENTIONED, AI_SUMMARY_EDITED,
        # SPAM_MARKED, SPAM_UNMARKED, PRIORITY_CHANGED, CATEGORY_CHANGED

    thread = models.ForeignKey(Thread, on_delete=models.CASCADE, null=True, blank=True, related_name="activity_logs")
    email = models.ForeignKey(Email, on_delete=models.CASCADE, null=True, blank=True)
    user = models.ForeignKey(User, on_delete=models.SET_NULL, null=True, blank=True)
    action = models.CharField(max_length=30, choices=Action.choices)
    old_value = models.CharField(max_length=200, blank=True, default="")
    new_value = models.CharField(max_length=200, blank=True, default="")
    detail = models.TextField(blank=True, default="")
```

From apps/emails/views.py (current activity_log view, line 2279):
- Groups by date using `itertools.groupby` on `timezone.localdate(e.created_at)`
- Context: `grouped_entries`, `page_obj`, `mis_stats`, `action_filter`, `date_filter`, `action_choices`, `today`, `yesterday`
- HTMX partial: returns `_activity_feed.html` when `request.htmx`

From templates/emails/settings.html:
- `switchTab(tab)` JS function toggles `tab-panel` visibility
- `tabDescriptions` JS object maps tab IDs to descriptions
- Tab IDs: rules, visibility, sla, webhooks, inboxes, config, whitelist
- Panel IDs: panel-rules, panel-visibility, panel-sla, panel-webhooks, panel-inboxes, panel-config, panel-whitelist
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Settings page grouped tab bar with section headers</name>
  <files>templates/emails/settings.html</files>
  <action>
Redesign the settings tab bar in `templates/emails/settings.html` to group 7 tabs into 3 sections with visible group headers and dividers.

**Tab grouping (per user decision):**
- **Assignment** group: Assignment Rules (rules), Team Visibility (visibility), SLA Targets (sla)
- **Integrations** group: Chat Webhooks (webhooks), Email Inboxes (inboxes)
- **System** group: System Config (config), Spam Whitelist (whitelist)

**Tab bar redesign:**
1. Replace the flat `flex items-center gap-0.5` tab bar with a grouped layout:
   - Each group gets a small label above its tabs: `text-[9px] font-bold text-slate-400 uppercase tracking-wider`
   - Tabs within each group sit in a flex row
   - Between groups, add a vertical divider: `w-px h-6 bg-slate-200 mx-2` (or similar thin line)
   - The whole bar should still scroll horizontally on mobile

2. Rename tab button labels to descriptive names:
   - "Assignment Rules" (was already correct)
   - "Team Visibility" (was "Category Visibility")
   - "SLA Targets" (was "SLA Configuration")
   - "Chat Webhooks" (was "Webhooks")
   - "Email Inboxes" (was "Inboxes")
   - "System Config" (was "System")
   - "Spam Whitelist" (was "Whitelist")

3. Update `tabDescriptions` JS object with new descriptive texts for renamed tabs.

4. Each tab content panel should get a bold title + 1-line description at the top. For panels that already have titles (webhooks, inboxes, whitelist), keep them. For those that don't (rules, visibility, sla, config), add:
   - Rules: "Assignment Rules" / "Configure automatic email assignment rules by category."
   - Visibility: "Team Visibility" / "Control which email categories each team member can see."
   - SLA: "SLA Targets" / "Set acknowledgment and response time targets by priority and category."
   - Config: "System Configuration" / "System-level settings, feature toggles, and operational parameters."

**Structure for the tab bar HTML:**
```html
<div class="flex items-center gap-0.5 overflow-x-auto scrollbar-hide">
  <!-- Group label: Assignment -->
  <span class="text-[9px] font-bold text-slate-400 uppercase tracking-wider shrink-0 mr-1">Assignment</span>
  [Assignment Rules btn] [Team Visibility btn] [SLA Targets btn]
  <!-- Divider -->
  <div class="w-px h-5 bg-slate-200/60 mx-2 shrink-0"></div>
  <!-- Group label: Integrations -->
  <span class="text-[9px] font-bold text-slate-400 uppercase tracking-wider shrink-0 mr-1">Integrations</span>
  [Chat Webhooks btn] [Email Inboxes btn]
  <!-- Divider -->
  <div class="w-px h-5 bg-slate-200/60 mx-2 shrink-0"></div>
  <!-- Group label: System -->
  <span class="text-[9px] font-bold text-slate-400 uppercase tracking-wider shrink-0 mr-1">System</span>
  [System Config btn] [Spam Whitelist btn]
</div>
```

**Do NOT change:** Tab panel IDs, switchTab() function logic (just rename labels/descriptions), panel content includes, SortableJS integration, active_tab switching at bottom.
  </action>
  <verify>
    <automated>cd /Users/uge/code/vipl-email-agent-fixes && python -c "
content = open('templates/emails/settings.html').read()
# Verify all 7 tab panels still present
for panel in ['panel-rules', 'panel-visibility', 'panel-sla', 'panel-webhooks', 'panel-inboxes', 'panel-config', 'panel-whitelist']:
    assert panel in content, f'{panel} missing'
# Verify group labels
assert 'Assignment' in content, 'Assignment group label missing'
assert 'Integrations' in content, 'Integrations group label missing'
assert 'System' in content, 'System group label missing'
# Verify renamed tabs
assert 'Team Visibility' in content, 'Team Visibility tab missing'
assert 'SLA Targets' in content, 'SLA Targets tab missing'
assert 'Chat Webhooks' in content, 'Chat Webhooks tab missing'
assert 'switchTab' in content, 'switchTab function missing'
print('Settings page structure verified')
"</automated>
  </verify>
  <done>Settings tabs grouped into 3 labeled sections (Assignment, Integrations, System) with dividers. All 7 tabs renamed to descriptive names. Each content panel has a title + description header. Tab switching still works.</done>
</task>

<task type="auto">
  <name>Task 2: Activity page thread-grouped redesign</name>
  <files>apps/emails/views.py, templates/emails/_activity_feed.html, templates/emails/activity_log.html</files>
  <action>
Redesign the activity page to group events by thread instead of by date.

**View changes (`apps/emails/views.py`, `activity_log` function starting line 2279):**

1. Change the groupby logic (currently groups by date) to group by thread:
   - After pagination, group `page_obj` entries by `log.thread` (or `log.thread_id`)
   - For entries without a thread (thread=None), group them under a "System / No Thread" bucket
   - Each group: `(thread_obj_or_None, entries_list)`
   - Sort groups by most recent entry first (the thread with the newest activity on top)
   - Keep `select_related("email", "user", "thread")` — add "thread" to the select_related call

2. Rename context variable from `grouped_entries` to `thread_groups` to make the semantic change clear.

3. Keep all existing filter logic (action_filter, date_filter), stat cards (mis_stats), pagination, and HTMX partial rendering intact.

4. Still pass `today` and `yesterday` for potential date formatting within thread groups.

**Template changes (`templates/emails/_activity_feed.html`):**

1. Replace the date-grouped structure with thread-grouped structure:
   - Each thread group is a card/section with a clickable header
   - Header shows: thread subject (truncated ~60 chars), thread sender, event count badge, most recent event time
   - Header links to `/emails/threads/<pk>/detail/` via HTMX or regular link
   - For "No Thread" group, show "System Events" as header (not clickable)

2. Inside each thread group, list events chronologically (oldest first within the group, so the story reads naturally):
   - Keep the existing icon + content + time layout for each event row
   - Add relative date/time formatting: "Today 14:30" or "Mar 14 09:15"
   - Remove the old date section headers (they're replaced by thread group headers)

3. Keep "Load more" pagination at the bottom.

4. Keep the empty state message.

**Template changes (`templates/emails/activity_log.html`):**

1. Keep stat cards and filter chips exactly as they are.

2. The `#activity-feed` div still wraps `{% include "_activity_feed.html" %}` — no change needed there since the partial handles the new layout.

3. Optionally improve filter chips: group action types by category. Use Claude's discretion on whether to add small dividers between assignment-related, status-related, and other action types in the chip row.

**Thread group header design:**
```html
<div class="border-b border-slate-100">
  <a href="/emails/threads/{{ thread.pk }}/detail/"
     class="px-5 py-3 bg-slate-50/80 flex items-center justify-between hover:bg-slate-100 transition-colors group">
    <div class="flex items-center gap-3 min-w-0">
      <!-- Thread icon -->
      <div class="w-8 h-8 rounded-lg bg-primary-50 flex items-center justify-center border border-primary-200/40 shrink-0">
        <svg class="w-3.5 h-3.5 text-primary-500">...</svg>
      </div>
      <div class="min-w-0">
        <div class="text-[13px] font-bold text-slate-700 truncate group-hover:text-primary-600">{{ thread.subject|truncatechars:60 }}</div>
        <div class="text-[10px] text-slate-400 font-medium">{{ thread.sender_name }}</div>
      </div>
    </div>
    <div class="flex items-center gap-2 shrink-0">
      <span class="text-[10px] font-bold text-slate-400 bg-slate-200/60 px-2 py-0.5 rounded-full">{{ entries|length }}</span>
    </div>
  </a>
  <!-- Events within thread -->
  <div class="relative">
    {% for log in entries %}
    ... (existing event row layout, slightly indented)
    {% endfor %}
  </div>
</div>
```

**Important:** Update the context variable name in both the view and templates consistently. If the view passes `thread_groups`, the template must iterate over `thread_groups`.
  </action>
  <verify>
    <automated>cd /Users/uge/code/vipl-email-agent-fixes && python -c "
# Check view has thread grouping
view_content = open('apps/emails/views.py').read()
assert 'thread_groups' in view_content or 'grouped_by_thread' in view_content, 'Thread grouping not in view'
assert 'select_related' in view_content, 'select_related missing'

# Check template has thread-based structure
feed = open('templates/emails/_activity_feed.html').read()
assert 'thread' in feed.lower(), 'Thread reference missing from feed template'
assert '/detail/' in feed, 'Thread detail link missing'

# Check activity page still has stat cards
page = open('templates/emails/activity_log.html').read()
assert 'stat-card' in page, 'Stat cards missing'
assert 'activity-feed' in page, 'Activity feed container missing'
print('Activity redesign structure verified')
" && cd /Users/uge/code/vipl-email-agent-fixes && python -m pytest apps/emails/tests/ -x -q --tb=short 2>&1 | tail -5</automated>
  </verify>
  <done>Activity page groups events by thread. Each thread shown as a card with clickable header linking to thread detail. Events listed chronologically within each thread group. Stat cards and filter chips preserved. No Thread events grouped under "System Events" header.</done>
</task>

</tasks>

<verification>
- Visit `/emails/settings/` -- tabs grouped into 3 sections with labels and dividers
- Tab names show descriptive labels (Team Visibility, SLA Targets, Chat Webhooks, etc.)
- Each tab panel has a title + description header
- Visit `/emails/activity/` -- events grouped by thread, not flat date list
- Thread headers are clickable and link to thread detail
- Filter chips and stat cards still work on activity page
- pytest passes without regressions
</verification>

<success_criteria>
- Settings page has 3 grouped tab sections with visible labels and dividers (PAGE-02)
- All 7 settings tabs renamed to descriptive names (PAGE-02)
- Each settings tab panel has a bold title and description (PAGE-02)
- Activity page displays thread-grouped events (PAGE-03)
- Thread group headers are clickable, linking to thread detail (PAGE-03)
- Stat cards and filter chips still functional (PAGE-03)
</success_criteria>

<output>
After completion, create `.planning/phases/04-page-polish/04-02-SUMMARY.md`
</output>
