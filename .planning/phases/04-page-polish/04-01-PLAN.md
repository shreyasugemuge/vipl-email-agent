---
phase: 04-page-polish
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - config/settings/base.py
  - apps/core/context_processors.py
  - templates/base.html
  - Dockerfile
  - templates/registration/login.html
autonomous: true
requirements: [PAGE-01, PAGE-04]

must_haves:
  truths:
    - "Sidebar footer shows version number (e.g. v2.5.4) instead of Online"
    - "Sidebar footer shows environment badge [PROD]/[DEV]/[OFF] with color coding"
    - "Green pulsing health dot is still present"
    - "Login page logo displays without a colored background rectangle"
    - "Login page has retro-modern aesthetic while remaining VIPL-branded"
    - "Login glass card and Google sign-in button still function"
  artifacts:
    - path: "apps/core/context_processors.py"
      provides: "Version + operating mode context for all templates"
      exports: ["vipl_context"]
    - path: "config/settings/base.py"
      provides: "APP_VERSION constant"
      contains: "APP_VERSION"
    - path: "templates/base.html"
      provides: "Version + env badge in sidebar footer"
      contains: "app_version"
    - path: "templates/registration/login.html"
      provides: "Retro-modern login page without logo background rectangle"
    - path: "Dockerfile"
      provides: "Git tag injection via build arg"
      contains: "APP_VERSION"
  key_links:
    - from: "apps/core/context_processors.py"
      to: "config/settings/base.py"
      via: "reads APP_VERSION from settings"
      pattern: "settings\\.APP_VERSION"
    - from: "apps/core/context_processors.py"
      to: "apps/core/models.py"
      via: "reads operating_mode from SystemConfig"
      pattern: "SystemConfig.*operating_mode"
    - from: "templates/base.html"
      to: "apps/core/context_processors.py"
      via: "template context variable"
      pattern: "app_version|operating_mode"
---

<objective>
Add version + environment badge to sidebar footer, and retheme the login page as retro-modern without logo background rectangle.

Purpose: PAGE-04 gives users instant visibility into which version and environment they're on. PAGE-01 makes the login page visually distinctive and fixes the logo background issue.
Output: Updated sidebar footer, new context processor, retro-modern login page.
</objective>

<execution_context>
@/Users/uge/.claude/get-shit-done/workflows/execute-plan.md
@/Users/uge/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-page-polish/04-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Version + environment badge infrastructure and sidebar</name>
  <files>config/settings/base.py, apps/core/context_processors.py, templates/base.html, Dockerfile</files>
  <action>
1. In `config/settings/base.py`, add `APP_VERSION` constant near the top (after SECRET_KEY):
   - `APP_VERSION = os.environ.get("APP_VERSION", "dev")`
   - This allows Docker to inject the git tag, while local dev shows "dev"

2. Create `apps/core/context_processors.py`:
   - Define `vipl_context(request)` that returns a dict with:
     - `app_version`: from `settings.APP_VERSION`
     - `operating_mode`: from `SystemConfig.get("operating_mode", "off")` (use the cached get)
   - Import SystemConfig from apps.core.models

3. Register the context processor in `config/settings/base.py` TEMPLATES OPTIONS context_processors list:
   - Add `"apps.core.context_processors.vipl_context"` after the existing processors

4. Update `Dockerfile` to accept and set APP_VERSION build arg:
   - Add `ARG APP_VERSION=dev` before the COPY step
   - Add `ENV APP_VERSION=$APP_VERSION` so it's available at runtime
   - Update `docker-compose.yml` build section if needed to pass build arg (check if build args section exists)

5. Update `templates/base.html` lines ~197-200 (the "Online" status area):
   - Replace "Online" text with `{{ app_version }}` (display as "v2.5.4" or "dev")
   - Add environment badge next to version: colored pill showing operating_mode
   - Badge colors: green bg for "production", amber bg for "dev", red bg for "off"
   - Keep the green pulsing dot as health indicator
   - Example layout: `[pulse dot] v2.5.4 [PROD]` or `[pulse dot] dev [OFF]`
   - Badge should be a small inline pill: `text-[8px] font-bold uppercase px-1.5 py-0.5 rounded`
  </action>
  <verify>
    <automated>cd /Users/uge/code/vipl-email-agent-fixes && python -c "from apps.core.context_processors import vipl_context; print('context processor OK')" && python -c "from config.settings.base import APP_VERSION; print(f'version: {APP_VERSION}')" && grep -q 'app_version' templates/base.html && grep -q 'APP_VERSION' Dockerfile && echo 'All checks pass'</automated>
  </verify>
  <done>Sidebar shows version string and colored environment badge instead of "Online". Context processor registered and functional. Dockerfile accepts APP_VERSION build arg.</done>
</task>

<task type="auto">
  <name>Task 2: Login page retro-modern retheme with logo fix</name>
  <files>templates/registration/login.html</files>
  <action>
Retheme the login page (`templates/registration/login.html`) as retro-modern while keeping it VIPL-branded. This is a full CSS/HTML restyle of the existing page.

**Logo fix (PAGE-01 core requirement):**
- The `vipl-logo-full.jpg` has a colored background rectangle baked into the image
- Use CSS to hide/blend the background: apply `mix-blend-mode: multiply` on the img element against the dark gradient background, OR use `border-radius` to soften edges, OR wrap in a container with matching background
- The simplest approach: since the card is white (`glass-card` with near-white bg), use `mix-blend-mode: multiply` which will make white backgrounds transparent
- Test: the logo letters should be visible but the rectangular background should blend away

**Retro-modern retheme:**
- Update the gradient background: shift to a warmer, more dramatic palette. Consider deep navy + burgundy/magenta accents (staying within VIPL's primary `rgb(168,51,98)` brand)
- Typography: keep Plus Jakarta Sans but use heavier weights (700-800) for headings, add letter-spacing
- Glass card: keep the backdrop-blur glass effect but consider adding a subtle border glow or gradient border
- Decorative elements: update the blur circles to be more geometric or grid-patterned (retro feel). Consider adding subtle grid lines, dot patterns, or scanline effects via CSS
- Add a retro-modern decorative touch: subtle horizontal line accents, geometric shapes, or a monospace "system status" style element
- Color palette: dark background (#0f1117 base), accent with primary-500 `rgb(168,51,98)`, warm amber/gold highlights
- The "Welcome back" heading and "Sign in to VIPL Triage" subtitle should feel premium
- Keep the "Vidarbha Infotech Private Limited" footer text
- Error messages, pending approval, password form, Google button -- all must remain functional. Only restyle, don't change structure/logic
- Keep dev login link at bottom for debug mode

**Important:** Do NOT change any URL references, form actions, CSRF tokens, or conditional logic. Only change CSS classes, styles, and decorative HTML elements.
  </action>
  <verify>
    <automated>cd /Users/uge/code/vipl-email-agent-fixes && python -c "
import re
content = open('templates/registration/login.html').read()
# Verify essential elements still present
assert 'csrf_token' in content, 'CSRF token missing'
assert 'provider_login_url' in content, 'Google login URL missing'
assert 'vipl-logo-full.jpg' in content, 'Logo reference missing'
assert 'dev_login' in content, 'Dev login link missing'
assert 'mix-blend-mode' in content or 'border-radius' in content or 'clip-path' in content, 'No logo background fix found'
print('Login page structure verified')
"</automated>
  </verify>
  <done>Login page displays with retro-modern aesthetic. Logo shows without visible background rectangle (blended or clipped). All auth functionality preserved. Glass card, Google sign-in, error messages, dev login all working.</done>
</task>

</tasks>

<verification>
- Visit `/accounts/login/` -- login page shows retro-modern design with logo that has no visible background rectangle
- Visit `/emails/` (logged in) -- sidebar footer shows version and environment badge instead of "Online"
- Green pulsing dot still visible in sidebar
- Environment badge shows correct mode (OFF/DEV/PROD based on SystemConfig)
</verification>

<success_criteria>
- Login page logo displays without colored background rectangle (PAGE-01)
- Sidebar footer shows version number instead of "Online" (PAGE-04)
- Sidebar footer shows environment badge with correct color coding (PAGE-04)
- Context processor provides version + mode to all templates
- Dockerfile accepts APP_VERSION build arg for production version injection
</success_criteria>

<output>
After completion, create `.planning/phases/04-page-polish/04-01-SUMMARY.md`
</output>
