---
phase: 04-read-unread-tracking
plan: 02
type: execute
wave: 2
depends_on:
  - 04-01
files_modified:
  - templates/emails/_thread_card.html
  - templates/emails/thread_list.html
  - templates/emails/_thread_detail.html
  - templates/base.html
autonomous: false
requirements:
  - READ-03
  - READ-05
must_haves:
  truths:
    - "Unread threads show bold text and blue indicator dot; read threads show normal weight, no dot"
    - "Sidebar badges show unread count (blue pill) when unreads exist, muted total when all read"
    - "Browser tab title shows unread count prefix like (3) VIPL Triage"
    - "Mark as unread button appears in thread detail action bar"
    - "Marking unread closes detail panel and card shows unread styling"
    - "Opening a thread updates card to read styling via OOB swap"
  artifacts:
    - path: "templates/emails/_thread_card.html"
      provides: "Per-user unread visual indicators (bold + blue dot)"
      contains: "is_unread"
    - path: "templates/emails/thread_list.html"
      provides: "Sidebar unread count badges with conditional styling"
      contains: "unread_mine"
    - path: "templates/emails/_thread_detail.html"
      provides: "Mark as unread button in action bar"
      contains: "mark_thread_unread"
    - path: "templates/base.html"
      provides: "Tab title with unread count prefix"
      contains: "unread_total"
  key_links:
    - from: "templates/emails/_thread_card.html"
      to: "apps/emails/views.py:annotate_unread"
      via: "is_unread annotation from queryset"
      pattern: "thread\\.is_unread"
    - from: "templates/emails/thread_list.html"
      to: "apps/emails/views.py:sidebar_counts"
      via: "unread_mine, unread_open context variables"
      pattern: "sidebar_counts\\.unread"
    - from: "templates/emails/_thread_detail.html"
      to: "apps/emails/urls.py:mark_thread_unread"
      via: "hx-post URL for mark unread button"
      pattern: "emails:mark_thread_unread"
---

<objective>
Update all templates to display per-user read/unread state: thread card visual indicators, sidebar unread badges, mark-as-unread button, and browser tab title count.

Purpose: Makes the backend read state from Plan 01 visible to users with Gmail-like visual cues.
Output: Complete read/unread UI that users can see and interact with.
</objective>

<execution_context>
@/Users/uge/.claude/get-shit-done/workflows/execute-plan.md
@/Users/uge/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-read-unread-tracking/04-CONTEXT.md
@.planning/phases/04-read-unread-tracking/04-RESEARCH.md
@.planning/phases/04-read-unread-tracking/04-01-SUMMARY.md

<interfaces>
<!-- From Plan 01: annotate_unread adds is_unread to queryset -->
<!-- thread.is_unread is a boolean annotation: True = unread, False = read -->
<!-- No ThreadReadState row = treated as read (is_unread=False) -->

<!-- From Plan 01: sidebar_counts now includes these keys -->
<!-- sidebar_counts.unread_mine, sidebar_counts.unread_unassigned -->
<!-- sidebar_counts.unread_open, sidebar_counts.unread_closed -->
<!-- context also has unread_total (sum for current view) -->

<!-- From Plan 01: mark_thread_unread endpoint -->
<!-- URL: emails:mark_thread_unread (POST, takes thread pk) -->
<!-- Returns: empty detail panel + OOB card swap with thread.is_unread=True set on object -->

<!-- From Plan 01: OOB card swap approach -->
<!-- OOB renders set thread.is_unread attribute directly on the thread object -->
<!-- List renders get thread.is_unread from queryset annotation -->
<!-- Template always uses thread.is_unread (single source) -->

<!-- Current _thread_card.html: uses thread.status == 'new' for bold + blue dot -->
<!-- Lines 6, 17-19, 20, 44: Replace status=='new' with thread.is_unread -->

<!-- Current thread_list.html sidebar badges: -->
<!-- Lines 60, 81, 105, 119: .view-count spans with sidebar_counts.X -->

<!-- Current _thread_detail.html action bar: lines 86-180 -->
<!-- Add mark-unread button alongside existing status action buttons -->

<!-- Current base.html title: line 7 -->
<!-- <title>{% block title %}VIPL Triage{% endblock %}</title> -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update thread card template for per-user unread styling</name>
  <files>templates/emails/_thread_card.html</files>
  <action>
    Replace all `thread.status == 'new'` conditions that control bold text and blue dot with `thread.is_unread`. The `is_unread` attribute comes from queryset annotation (list renders) or is set directly on the thread object (OOB swap renders in Plan 01).

    Replace these 4 occurrences:

    1. **Line 6** (card div class): `{% if thread.status == 'new' %}font-semibold{% endif %}` becomes `{% if thread.is_unread %}font-semibold{% endif %}`

    2. **Lines 17-19** (blue dot): `{% if thread.status == 'new' %}` becomes `{% if thread.is_unread %}`

    3. **Line 20** (sender name weight): `{% if thread.status == 'new' %}font-bold text-slate-900{% else %}font-medium text-slate-700{% endif %}` becomes `{% if thread.is_unread %}font-bold text-slate-900{% else %}font-medium text-slate-700{% endif %}`

    4. **Line 44** (subject weight): `{% if thread.status == 'new' %}font-bold text-slate-900{% else %}font-medium text-slate-600{% endif %}` becomes `{% if thread.is_unread %}font-bold text-slate-900{% else %}font-medium text-slate-600{% endif %}`

    Do NOT change the status badge chip on line 49 (`get_status_display`) -- that shows thread status (New/Acknowledged/Closed), not read state.

    Also update Plan 01's OOB swap renders in `views.py` to set `thread.is_unread` attribute on the thread object before passing to `render_to_string`:
    - In `mark_thread_unread`: add `thread.is_unread = True` before `render_to_string`
    - In `thread_detail` OOB card: add `thread.is_unread = False` before `render_to_string`
    - In `assign_thread_view` and `change_thread_status_view` OOB cards: leave as-is (they don't have annotation, so `thread.is_unread` will be undefined/falsy = read, which is fine for the acting user)
  </action>
  <verify>
    <automated>cd /Users/uge/code/vipl-email-agent && python -c "
import re
content = open('templates/emails/_thread_card.html').read()
status_new = len(re.findall(r\"status.*==.*'new'\", content))
is_unread = content.count('is_unread')
# status badge chip is allowed to reference status
print(f'status==new occurrences: {status_new} (expect 0 for bold/dot)')
print(f'is_unread occurrences: {is_unread} (expect 4+)')
assert is_unread >= 4, f'Expected 4+ is_unread, got {is_unread}'
print('PASS')
"</automated>
  </verify>
  <done>Zero occurrences of `status == 'new'` for bold/dot logic. 4+ occurrences of `thread.is_unread` controlling bold text, blue dot, and font weight. Status badge chip unchanged.</done>
</task>

<task type="auto">
  <name>Task 2: Sidebar unread badges, mark-unread button, and browser tab title</name>
  <files>templates/emails/thread_list.html, templates/emails/_thread_detail.html, templates/base.html</files>
  <action>
    **1. Sidebar unread badges** (`thread_list.html`):

    For each sidebar view link (Unassigned, Mine, All Open, Closed), replace the static count badge with conditional unread/total display. Per user decision: "show unread count (blue pill) when unreads exist; show muted total when all read."

    Replace each badge span. Example for "Mine" (line 81):
    ```html
    {% if sidebar_counts.unread_mine > 0 %}
    <span class="view-count text-[10px] font-bold text-white bg-blue-500 px-1.5 py-0.5 rounded-full min-w-[20px] text-center">{{ sidebar_counts.unread_mine }}</span>
    {% else %}
    <span class="view-count text-[10px] font-bold text-slate-500 bg-white/[0.06] px-1.5 py-0.5 rounded-full min-w-[20px] text-center">{{ sidebar_counts.mine }}</span>
    {% endif %}
    ```

    Apply same pattern to:
    - **Unassigned** (line 60): `unread_unassigned` / `unassigned`
    - **Mine** (line 81): `unread_mine` / `mine`
    - **All Open** (line 105): `unread_open` / `all_open`
    - **Closed** (line 119): `unread_closed` / `closed`

    Per discretion recommendation from research: Yes, add to Triage Queue (Unassigned) too.

    **2. Mark as Unread button** (`_thread_detail.html`):

    Add a "Mark as Unread" icon button in the action bar. Place it OUTSIDE the `{% if thread.assigned_to == request.user or is_admin %}` guard (all logged-in users can mark their own read state). Add after the closing `{% endif %}` of the status actions conditional, still inside the outer flex container:

    ```html
    <!-- Mark as Unread -->
    <form hx-post="{% url 'emails:mark_thread_unread' thread.pk %}"
          hx-target="#thread-detail-panel" hx-swap="innerHTML" class="inline">
        {% csrf_token %}
        <button type="submit" hx-disabled-elt="this"
                class="inline-flex items-center gap-1 px-2.5 py-1.5 text-[10px] font-bold text-slate-500 bg-slate-100 rounded-md hover:bg-slate-200 transition-colors cursor-pointer"
                title="Mark as unread">
            <svg class="w-3.5 h-3.5" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2"
                      d="M3 8l7.89 5.26a2 2 0 002.22 0L21 8M5 19h14a2 2 0 002-2V7a2 2 0 00-2-2H5a2 2 0 00-2 2v10a2 2 0 002 2z"/>
            </svg>
        </button>
    </form>
    ```

    **3. Browser tab title** (`base.html`):

    Update line 7 from:
    ```html
    <title>{% block title %}VIPL Triage{% endblock %}</title>
    ```
    to:
    ```html
    <title>{% if unread_total %}({{ unread_total }}) {% endif %}{% block title %}VIPL Triage{% endblock %}</title>
    ```

    For HTMX partial swaps, add an OOB title updater script in `_thread_list_body.html` (the partial returned for HTMX requests):
    ```html
    <script id="title-updater" hx-swap-oob="true">
        document.title = '{% if unread_total %}({{ unread_total }}) {% endif %}VIPL Triage | Inbox';
    </script>
    ```

    **4. Keyboard shortcut** (discretion: add 'U' key for mark unread):

    In `_thread_detail.html`, add a keydown listener:
    ```html
    <script>
    document.addEventListener('keydown', function(e) {
        if (e.key === 'u' || e.key === 'U') {
            if (e.target.tagName === 'INPUT' || e.target.tagName === 'TEXTAREA' || e.target.tagName === 'SELECT') return;
            var form = document.querySelector('[hx-post*="mark-unread"]');
            if (form) { htmx.trigger(form, 'submit'); }
        }
    });
    </script>
    ```
  </action>
  <verify>
    <automated>cd /Users/uge/code/vipl-email-agent && python -m pytest -x --tb=short -q 2>&1 | tail -5</automated>
  </verify>
  <done>Sidebar badges show blue pill with unread count when unreads > 0, muted total otherwise. Mark-unread envelope button visible in detail panel action bar. Browser tab title shows "(N) VIPL Triage" when unreads exist. 'U' key triggers mark-unread from detail panel.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Visual verification of read/unread tracking</name>
  <files></files>
  <action>Human verifies the complete read/unread tracking feature visually and functionally.</action>
  <verify>User confirms all behaviors work as expected.</verify>
  <done>User types "approved".</done>
  <what-built>Complete per-user read/unread tracking: bold+dot on unread threads, auto-mark-read on detail open, mark-as-unread button, sidebar badges, browser tab title, keyboard shortcut</what-built>
  <how-to-verify>
    1. Start dev server: `python manage.py runserver 8000`
    2. Open http://triage.local/emails/ -- all threads should appear as READ (normal weight, no blue dot) since no ThreadReadState rows exist yet
    3. Run `python manage.py test_pipeline --count 3` to create new threads
    4. Refresh page -- new threads should still appear read (no row = read convention)
    5. Open any thread detail -- verify card immediately loses bold (if it was bold) via OOB swap
    6. In Django shell, manually create an unread state:
       ```python
       from apps.emails.models import ThreadReadState, Thread
       from django.contrib.auth import get_user_model
       User = get_user_model()
       u = User.objects.first()
       t = Thread.objects.first()
       ThreadReadState.objects.update_or_create(thread=t, user=u, defaults={"is_read": False, "read_at": None})
       ```
    7. Refresh -- that thread should show bold text + blue dot
    8. Click the thread -- should open detail and card should update to read styling
    9. In detail panel, click the envelope (Mark as Unread) button -- detail panel should close, card should show bold+dot again
    10. Check sidebar badges -- "My Inbox" should show blue pill if you have unread assigned threads
    11. Check browser tab title -- should show "(1) VIPL Triage" if 1 unread
    12. Press 'U' key while detail panel is open -- should mark as unread
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

</tasks>

<verification>
1. `pytest -v` -- full suite green
2. Visual: unread threads show bold + blue dot, read threads show normal
3. Visual: sidebar badges show blue unread counts
4. Visual: browser tab title includes unread count
5. Functional: mark-as-unread button closes panel and reverts card to unread styling
6. Functional: opening thread detail marks it as read (card updates via OOB)
</verification>

<success_criteria>
- Thread cards use per-user is_unread (not status=='new') for bold and blue dot
- Sidebar shows blue pill with unread count when unreads exist, muted total when all read
- Mark-as-unread envelope button in detail action bar works and closes panel
- Browser tab title shows (N) prefix when unreads exist
- 'U' keyboard shortcut triggers mark-as-unread
- All existing tests still pass
</success_criteria>

<output>
After completion, create `.planning/phases/04-read-unread-tracking/04-02-SUMMARY.md`
</output>
