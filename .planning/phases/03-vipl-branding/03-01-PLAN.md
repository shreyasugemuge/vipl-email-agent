---
phase: 03-vipl-branding
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - static/img/vipl-icon.jpg
  - static/img/vipl-logo-full.jpg
  - static/img/vipl-logo-white.jpg
  - static/img/favicon.ico
  - templates/base.html
  - templates/registration/login.html
  - templates/registration/dev_login.html
  - templates/emails/email_list.html
  - templates/emails/activity_log.html
  - templates/emails/settings.html
  - templates/emails/_email_detail.html
  - templates/emails/_config_editor.html
  - templates/emails/_whitelist_tab.html
  - templates/emails/_inboxes_tab.html
  - templates/emails/_sla_config.html
  - templates/emails/_category_visibility.html
  - templates/emails/_webhooks_tab.html
  - templates/emails/_email_card.html
  - templates/emails/_assign_dropdown.html
  - templates/emails/_assignment_rules.html
  - templates/emails/_activity_feed.html
  - apps/emails/tests/test_branding.py
autonomous: true
requirements: [R3.1, R3.2, R3.3, R3.4]

must_haves:
  truths:
    - "VIPL Vi mark icon visible in sidebar header"
    - "VIPL full logo with company name visible on login page"
    - "Favicon shows Vi mark in browser tab"
    - "All buttons, links, and accents use plum/purple brand colors instead of indigo"
    - "Functional status colors (red, amber, green, blue) unchanged"
    - "Page titles show 'VIPL Triage | Page' format"
    - "Copyright footer visible at bottom of dashboard"
  artifacts:
    - path: "static/img/vipl-icon.jpg"
      provides: "Vi mark logo for sidebar and favicon source"
    - path: "static/img/vipl-logo-full.jpg"
      provides: "Full logo with company name for login page"
    - path: "static/img/favicon.ico"
      provides: "32x32 browser tab icon"
    - path: "templates/base.html"
      provides: "Brand @theme palette, sidebar logo, favicon link, footer"
      contains: "--color-primary-600"
    - path: "apps/emails/tests/test_branding.py"
      provides: "Automated verification of assets and color replacement"
  key_links:
    - from: "templates/base.html"
      to: "static/img/vipl-icon.jpg"
      via: "{% static 'img/vipl-icon.jpg' %}"
      pattern: "static.*vipl-icon"
    - from: "templates/registration/login.html"
      to: "static/img/vipl-logo-full.jpg"
      via: "{% static 'img/vipl-logo-full.jpg' %}"
      pattern: "static.*vipl-logo-full"
    - from: "templates/emails/_email_detail.html"
      to: "templates/base.html @theme"
      via: "primary-600 classes resolved by @theme tokens"
      pattern: "primary-600"
---

<objective>
Copy VIPL logo assets, generate favicon, replace the indigo @theme palette with brand plum/purple, and update all 17 template files to use the new brand colors consistently.

Purpose: Transform the application from generic indigo placeholder to VIPL brand identity.
Output: All static assets committed, all templates branded, automated branding tests passing.
</objective>

<execution_context>
@/Users/uge/.claude/get-shit-done/workflows/execute-plan.md
@/Users/uge/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-vipl-branding/03-CONTEXT.md
@.planning/phases/03-vipl-branding/03-RESEARCH.md

<interfaces>
<!-- Key patterns the executor needs -->

Logo source files (confirmed accessible):
  /Users/uge/Library/CloudStorage/GoogleDrive-shreyas@vidarbhainfotech.com/Other computers/Laptop/Downloads/vipl logo/1.jpg  (Vi mark icon, 1920x1080)
  /Users/uge/Library/CloudStorage/GoogleDrive-shreyas@vidarbhainfotech.com/Other computers/Laptop/Downloads/vipl logo/2.jpg  (Full logo with text, 1394x619)
  /Users/uge/Library/CloudStorage/GoogleDrive-shreyas@vidarbhainfotech.com/Other computers/Laptop/Downloads/vipl logo/3.jpg  (White variant for dark backgrounds, 1920x1080)

Django static config (config/settings/base.py):
  STATIC_URL = "static/"
  STATICFILES_DIRS = [BASE_DIR / "static"]
  STATIC_ROOT = BASE_DIR / "staticfiles"

Current @theme block (base.html lines 13-25) -- REPLACE entirely:
  --color-primary-50 through --color-primary-900 (currently indigo values)

Hardcoded hex values in base.html CSS (lines 39, 42-43) -- UPDATE:
  .card-selected: #eef2ff, #f0f9ff, #6366f1
  .nav-active: rgba(99,102,241,0.12)
  .nav-active::before: #818cf8

Login page CSS (login.html) -- UPDATE:
  .gradient-bg: #1a1635, #251d4d (indigo-tinted)
  .input-focus: rgba(99,102,241,0.12)

Login and dev_login are STANDALONE templates (not {% extends "base.html" %}).
Their @theme blocks and CSS must be updated independently.

Template tag for static: {% load static %} + {% static 'img/filename' %}
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Copy logo assets, generate favicon, create branding test scaffold</name>
  <files>
    static/img/vipl-icon.jpg
    static/img/vipl-logo-full.jpg
    static/img/vipl-logo-white.jpg
    static/img/favicon.ico
    apps/emails/tests/test_branding.py
  </files>
  <action>
1. Create `static/img/` directory if it does not exist.

2. Copy logo files from Google Drive sync path to static/img/:
   - `/Users/uge/Library/CloudStorage/GoogleDrive-shreyas@vidarbhainfotech.com/Other computers/Laptop/Downloads/vipl logo/1.jpg` -> `static/img/vipl-icon.jpg`
   - `/Users/uge/Library/CloudStorage/GoogleDrive-shreyas@vidarbhainfotech.com/Other computers/Laptop/Downloads/vipl logo/2.jpg` -> `static/img/vipl-logo-full.jpg`
   - `/Users/uge/Library/CloudStorage/GoogleDrive-shreyas@vidarbhainfotech.com/Other computers/Laptop/Downloads/vipl logo/3.jpg` -> `static/img/vipl-logo-white.jpg`

3. Generate favicon from the Vi mark icon using macOS sips:
   `sips -s format ico -z 32 32 static/img/vipl-icon.jpg --out static/img/favicon.ico`
   If sips does not support ico output, convert to png first then use it as favicon:
   `sips -s format png -z 32 32 static/img/vipl-icon.jpg --out static/img/favicon.png`
   Then use the png as favicon (update link tag to type="image/png" and href to favicon.png).

4. Create `apps/emails/tests/test_branding.py` with these test functions:
   - `test_static_assets_exist`: Assert that `static/img/vipl-icon.jpg`, `static/img/vipl-logo-full.jpg`, and `static/img/favicon.ico` (or .png) all exist on disk using `Path.exists()`.
   - `test_sidebar_contains_logo`: Use Django test client to GET `/emails/` (login required -- create a test user, login), check that response HTML contains `vipl-icon` (the static tag reference to the sidebar logo).
   - `test_login_contains_logo`: GET the login page URL, check response HTML contains `vipl-logo-full`.
   - `test_no_indigo_in_templates`: Glob all `templates/**/*.html` files, read each, assert zero occurrences of the regex pattern `\bindigo\b` (word-boundary match). This catches any missed replacements. Exclude `inspect.html` (dev inspector, left as-is per Claude's discretion).
   - `test_no_violet_in_brand_templates`: Same glob, assert zero occurrences of `\bviolet\b` in template files except `inspect.html`.
   - `test_page_titles_contain_vipl`: GET `/emails/`, check `<title>` contains "VIPL Triage".
   - `test_favicon_link_in_base`: GET `/emails/`, check response HTML contains `favicon`.

   Use `@pytest.mark.django_db` where needed. Import `from pathlib import Path` and `from django.conf import settings`.
  </action>
  <verify>
    <automated>pytest apps/emails/tests/test_branding.py::test_static_assets_exist -x -v</automated>
  </verify>
  <done>All 3 logo files copied to static/img/, favicon generated, test_branding.py created with 7 test functions. test_static_assets_exist passes green. Other tests expected to fail (RED) until Task 2 completes the template changes.</done>
</task>

<task type="auto">
  <name>Task 2: Apply brand palette and update all templates</name>
  <files>
    templates/base.html
    templates/registration/login.html
    templates/registration/dev_login.html
    templates/emails/email_list.html
    templates/emails/activity_log.html
    templates/emails/settings.html
    templates/emails/_email_detail.html
    templates/emails/_config_editor.html
    templates/emails/_whitelist_tab.html
    templates/emails/_inboxes_tab.html
    templates/emails/_sla_config.html
    templates/emails/_category_visibility.html
    templates/emails/_webhooks_tab.html
    templates/emails/_email_card.html
    templates/emails/_assign_dropdown.html
    templates/emails/_assignment_rules.html
    templates/emails/_activity_feed.html
  </files>
  <action>
**base.html changes:**

1. Add `{% load static %}` at top of file (line 1, before DOCTYPE).

2. Replace the @theme block (lines 13-25) with brand plum palette. Eye-drop the exact plum color from `static/img/vipl-icon.jpg` (approximately #7B2D5F) and build a perceptually uniform 50-900 scale around it. The 600 value MUST be the logo's plum color (or nearest accessible variant -- WCAG AA 4.5:1 contrast on white for normal text). Draft palette from research:
   ```
   --color-primary-50: #fdf2f8;
   --color-primary-100: #fce7f1;
   --color-primary-200: #f9c4db;
   --color-primary-300: #f49bbe;
   --color-primary-400: #e06a97;
   --color-primary-500: #c94476;
   --color-primary-600: #a83362;
   --color-primary-700: #8b2852;
   --color-primary-800: #742345;
   --color-primary-900: #5f1d3a;
   ```
   Adjust if needed after viewing the logo -- the exact plum hex from the "i" stroke drives the palette.

3. Update hardcoded hex values in `<style>` block:
   - `.card-selected`: Replace `#eef2ff` with primary-50 value, `#f0f9ff` with a slightly lighter tint, `#6366f1` with `var(--color-primary-600)` or the primary-600 hex.
   - `.nav-active`: Replace `rgba(99,102,241,0.12)` with brand plum at 12% opacity (e.g., `rgba(168,51,98,0.12)` using primary-600 RGB).
   - `.nav-active::before`: Replace `#818cf8` with `var(--color-primary-400)` or the primary-400 hex.

4. Replace sidebar logo area (lines 76-83, the gradient div with SVG envelope) with:
   ```html
   <img src="{% static 'img/vipl-icon.jpg' %}" alt="VIPL" class="w-8 h-8 rounded-lg shadow-lg object-cover">
   ```
   Remove the old SVG envelope icon entirely.

5. Replace the `from-indigo-500 to-violet-600` and `shadow-indigo-500/25` classes on the old logo div. Since the div is being replaced by an img, these go away.

6. Replace the user avatar fallback gradient (line ~153): change `from-indigo-400 to-violet-500` to `from-primary-400 to-primary-500`.

7. Add favicon link in `<head>`:
   ```html
   <link rel="icon" type="image/x-icon" href="{% static 'img/favicon.ico' %}">
   ```
   (Or type="image/png" with favicon.png if ico generation failed.)

8. The `<title>` tag already says "VIPL Triage" -- confirm and leave as-is.

9. Add copyright footer before closing `</div>` of the main area (after `</main>`, before closing flex div):
   ```html
   <footer class="h-8 bg-white border-t border-slate-200/60 flex items-center justify-center px-4 shrink-0">
     <p class="text-[10px] text-slate-400 font-medium">&copy; 2026 Vidarbha Infotech Pvt. Ltd.</p>
   </footer>
   ```

**login.html changes:**

1. Add `{% load static %}` (it already has `{% load socialaccount %}` -- add static on a new line or combine).

2. Add favicon link in `<head>`.

3. Update `.gradient-bg` hex values from indigo-tinted (`#1a1635`, `#251d4d`) to plum-tinted equivalents (e.g., `#1a0f1a`, `#2d1525`, `#3d1a30` -- dark plum tones that complement the brand).

4. Update `.input-focus` from `rgba(99,102,241,0.12)` to brand plum rgba.

5. Replace decorative blur elements: `bg-indigo-500/8` -> `bg-primary-500/8`, `bg-violet-500/8` -> `bg-primary-500/8`.

6. Replace the login icon area (lines 46-51, the gradient div with SVG envelope) with:
   ```html
   <img src="{% static 'img/vipl-logo-full.jpg' %}" alt="Vidarbha Infotech Pvt. Ltd." class="max-w-[220px] mx-auto mb-4">
   ```
   Remove the SVG and the enclosing gradient div.

7. Update heading from "Welcome back" to something brand-appropriate (or keep, Claude's discretion). Update subtitle to reference VIPL Triage.

8. Replace all `indigo-*` classes: `focus:border-indigo-400` -> `focus:border-primary-400`, `from-indigo-600 to-violet-600` -> `from-primary-600 to-primary-700`, `hover:from-indigo-700 hover:to-violet-700` -> `hover:from-primary-700 hover:to-primary-800`, `focus:ring-indigo-500/30` -> `focus:ring-primary-500/30`, `shadow-indigo-500/20` -> `shadow-primary-500/20`, `focus:ring-indigo-500/20` -> `focus:ring-primary-500/20`.

9. Update `<title>` to "VIPL Triage | Login".

10. Update footer text class: `text-indigo-200/30` -> `text-primary-200/30`.

**dev_login.html changes:**

1. Add `{% load static %}` and favicon link in `<head>`.

2. Update `.gradient-bg` hex values same as login.html.

3. Replace all indigo/violet classes:
   - Admin button hover: `hover:bg-indigo-50 hover:border-indigo-200` -> `hover:bg-primary-50 hover:border-primary-200`
   - Admin icon bg: `bg-indigo-100` -> `bg-primary-100`, `text-indigo-600` -> `text-primary-600`
   - Member button hover: `hover:bg-violet-50 hover:border-violet-200` -> `hover:bg-primary-50 hover:border-primary-200`
   - Member icon bg: `bg-violet-100` -> `bg-primary-100`, `text-violet-600` -> `text-primary-600`
   - Back link: `hover:text-indigo-500` -> `hover:text-primary-500`
   - Footer: `text-indigo-200/30` -> `text-primary-200/30`

4. Update `<title>` to "VIPL Triage | Dev Login".

**All HTMX partial templates (_*.html) -- search and replace:**

For each of these 12 partial files, replace ALL occurrences of `indigo-*` and `violet-*` Tailwind classes with `primary-*` equivalents:
- `_email_detail.html` (10 occurrences): indigo classes on AI suggestion bar, form controls, assign button, avatar gradient, acknowledge button, prose links
- `_config_editor.html` (6): form control focus rings/borders, save button gradient
- `_whitelist_tab.html` (3): form control, add button, badge colors
- `_inboxes_tab.html` (3): form control, add button, icon color
- `_sla_config.html` (3): form controls, save button
- `_category_visibility.html` (3): checkbox, count badge, save button
- `_webhooks_tab.html` (2): form control, test button
- `_email_card.html` (1): assignee avatar gradient
- `_assign_dropdown.html` (1): select focus ring
- `_assignment_rules.html` (1): save button
- `_activity_feed.html` (1): hover link color

Also update the full page templates:
- `email_list.html` (5): focus:ring and focus:border indigo classes, add title block `{% block title %}VIPL Triage | Inbox{% endblock %}`
- `activity_log.html` (5): indigo stat card colors, add title block `{% block title %}VIPL Triage | Activity{% endblock %}`
- `settings.html` (1): ghostClass indigo, add title block `{% block title %}VIPL Triage | Settings{% endblock %}`

**CRITICAL: Do NOT change functional status colors.** These use red, amber, green, blue, emerald -- NOT indigo or violet. The template tags `priority_base`, `status_base`, `sla_color` resolve to semantic colors and must remain unchanged.

**Verification after all changes:**
Run `grep -rn "indigo\|violet" templates/` and confirm ZERO results (excluding inspect.html if it exists). Every indigo/violet occurrence in templates is a brand color and must be replaced with `primary-*`.
  </action>
  <verify>
    <automated>pytest apps/emails/tests/test_branding.py -x -v && grep -rn "indigo\|violet" templates/ --include="*.html" | grep -v inspect.html | wc -l | xargs test 0 -eq</automated>
  </verify>
  <done>All 17 template files updated. @theme palette uses brand plum values. Sidebar shows Vi mark logo. Login shows full logo. Favicon in all page heads. Page titles use "VIPL Triage | Page" format. Copyright footer on dashboard. Zero indigo/violet occurrences remain in templates (except inspect.html). All test_branding.py tests pass green. Full test suite (pytest -v) passes with no regressions.</done>
</task>

</tasks>

<verification>
1. `pytest apps/emails/tests/test_branding.py -x -v` -- all branding tests green
2. `pytest -v` -- full suite passes (no regressions from template changes)
3. `grep -rn "indigo\|violet" templates/ --include="*.html" | grep -v inspect.html` -- zero results
4. Visual: `python manage.py runserver 8000` and inspect triage.local -- plum/purple brand across all pages
</verification>

<success_criteria>
- 3 logo files + favicon committed to static/img/
- @theme palette uses plum/purple derived from logo (~#7B2D5F)
- All 17 template files use `primary-*` classes instead of `indigo-*`/`violet-*`
- Sidebar shows Vi mark icon, login shows full logo with company name
- Favicon appears in browser tab
- Page titles: "VIPL Triage | Inbox", "VIPL Triage | Activity", "VIPL Triage | Settings", etc.
- Copyright footer at bottom of dashboard
- Functional status colors (red, amber, green, blue) unchanged
- All automated tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/03-vipl-branding/03-01-SUMMARY.md`
</output>
