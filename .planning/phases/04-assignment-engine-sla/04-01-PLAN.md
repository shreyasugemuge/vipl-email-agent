---
phase: 04-assignment-engine-sla
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/emails/models.py
  - apps/emails/services/sla.py
  - apps/emails/services/assignment.py
  - apps/emails/services/pipeline.py
  - apps/emails/services/ai_processor.py
  - apps/emails/services/dtos.py
  - apps/emails/templatetags/email_tags.py
  - apps/emails/tests/test_sla.py
  - apps/emails/tests/test_auto_assignment.py
  - apps/emails/tests/test_ai_suggestion.py
autonomous: true
requirements: [ASGN-03, ASGN-04, SLA-02, INFR-09, INFR-10]

must_haves:
  truths:
    - "AssignmentRule, SLAConfig, CategoryVisibility models exist with proper fields and constraints"
    - "SLA deadlines are calculated using business hours only (8 AM-8 PM IST, Mon-Sat)"
    - "Auto-assign batch job correctly matches unassigned emails to rules and assigns the first-priority person"
    - "Claiming validates category visibility before allowing self-assignment"
    - "Email model has sla_ack_deadline and sla_respond_deadline DateTimeFields"
    - "AI triage prompt includes team workload context and returns structured assignee suggestion with reasoning"
  artifacts:
    - path: "apps/emails/models.py"
      provides: "AssignmentRule, SLAConfig, CategoryVisibility models, new SLA fields on Email, new ActivityLog actions"
      contains: "class AssignmentRule"
    - path: "apps/emails/services/sla.py"
      provides: "Business-hours SLA calculator, breach detection queries"
      exports: ["calculate_sla_deadline", "set_sla_deadlines", "get_breached_emails"]
    - path: "apps/emails/services/assignment.py"
      provides: "auto_assign_batch(), claim_email() functions"
      exports: ["auto_assign_batch", "claim_email"]
    - path: "apps/emails/services/ai_processor.py"
      provides: "Enhanced triage prompt with team workload context, structured assignee suggestion parsing"
      contains: "workload"
    - path: "apps/emails/tests/test_sla.py"
      provides: "Tests for SLA calculation, business hours, breach detection"
    - path: "apps/emails/tests/test_auto_assignment.py"
      provides: "Tests for auto-assign batch, claiming, rule matching"
    - path: "apps/emails/tests/test_ai_suggestion.py"
      provides: "Tests for AI workload context injection and structured suggestion parsing"
  key_links:
    - from: "apps/emails/services/pipeline.py"
      to: "apps/emails/services/sla.py"
      via: "set_sla_deadlines called after save_email_to_db"
      pattern: "set_sla_deadlines"
    - from: "apps/emails/services/assignment.py"
      to: "apps/emails/models.py"
      via: "auto_assign_batch queries AssignmentRule"
      pattern: "AssignmentRule.objects.filter"
    - from: "apps/emails/services/ai_processor.py"
      to: "apps/emails/models.py"
      via: "AI prompt includes team workload from Email.objects queries"
      pattern: "open_email_count|workload"
---

<objective>
Models, SLA calculator, auto-assignment engine, claiming service, and AI suggestion enhancement for Phase 4.

Purpose: Build the data layer (3 new models, SLA fields on Email, new ActivityLog actions), core service logic (business-hours SLA calculation, auto-assign batch job, claim flow), and AI prompt enhancement (workload-aware assignee suggestions). This is the foundation that the UI (Plan 02) and breach alerting (Plan 03) build on.

Output: Migration for new models/fields, SLA calculator utility, auto-assign batch function, claim function, pipeline integration for SLA deadline setting, AI processor with workload context, comprehensive tests.
</objective>

<execution_context>
@/Users/uge/.claude/get-shit-done/workflows/execute-plan.md
@/Users/uge/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-assignment-engine-sla/04-CONTEXT.md
@.planning/phases/04-assignment-engine-sla/04-RESEARCH.md

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase. -->

From apps/emails/models.py:
```python
class Email(SoftDeleteModel, TimestampedModel):
    class Status(models.TextChoices):
        NEW = "new", "New"
        ACKNOWLEDGED = "acknowledged", "Acknowledged"
        REPLIED = "replied", "Replied"
        CLOSED = "closed", "Closed"

    class ProcessingStatus(models.TextChoices):
        PENDING = "pending", "Pending"
        PROCESSING = "processing", "Processing"
        COMPLETED = "completed", "Completed"
        FAILED = "failed", "Failed"
        EXHAUSTED = "exhausted", "Exhausted"

    # Assignment fields already exist:
    ai_suggested_assignee = models.CharField(max_length=100, blank=True, default="")
    assigned_to = models.ForeignKey(settings.AUTH_USER_MODEL, ...)
    assigned_by = models.ForeignKey(settings.AUTH_USER_MODEL, ...)
    assigned_at = models.DateTimeField(null=True, blank=True)

class ActivityLog(TimestampedModel):
    class Action(models.TextChoices):
        ASSIGNED = "assigned", "Assigned"
        REASSIGNED = "reassigned", "Reassigned"
        STATUS_CHANGED = "status_changed", "Status Changed"
        ACKNOWLEDGED = "acknowledged", "Acknowledged"
        CLOSED = "closed", "Closed"
```

From apps/core/models.py:
```python
class TimestampedModel(models.Model):
    created_at = models.DateTimeField(auto_now_add=True)
    updated_at = models.DateTimeField(auto_now=True)

class SoftDeleteModel(models.Model):
    deleted_at = models.DateTimeField(null=True, blank=True, db_index=True)
```

From apps/emails/services/assignment.py:
```python
def assign_email(email, assignee, assigned_by, note="") -> Email
def change_status(email, new_status, changed_by) -> Email
def _user_display(user) -> str
```

From apps/emails/services/dtos.py:
```python
VALID_CATEGORIES = [
    "Government/Tender", "Sales Lead", "Support Request", "Complaint",
    "Partnership", "Vendor", "Internal", "General Inquiry",
]
VALID_PRIORITIES = ["CRITICAL", "HIGH", "MEDIUM", "LOW"]

@dataclass
class TriageResult:
    ...
    suggested_assignee: str = ""
```

From apps/emails/services/ai_processor.py:
```python
class AIProcessor:
    def process_email(self, email_msg: EmailMessage) -> Optional[TriageResult]: ...
    # TRIAGE_TOOL_SCHEMA includes "suggested_assignee" as string type
    # _build_user_message(email_msg) builds the user message for Claude
```

From apps/emails/services/pipeline.py:
```python
def save_email_to_db(email_msg: EmailMessage, triage: TriageResult) -> Email
def process_single_email(...) -> Optional[Email]
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: New models, Email SLA fields, ActivityLog actions, migration</name>
  <files>apps/emails/models.py, apps/emails/services/sla.py, apps/emails/tests/test_sla.py, apps/emails/tests/test_auto_assignment.py</files>
  <behavior>
    - AssignmentRule: category + assignee FK + priority_order + is_active; unique_together (category, assignee); ordering by category, priority_order
    - SLAConfig: priority + category + ack_hours + respond_hours; unique_together (priority, category); default ack_hours=1.0, respond_hours=24.0
    - CategoryVisibility: user FK + category; unique_together (user, category)
    - Email gains: sla_ack_deadline (DateTimeField, null), sla_respond_deadline (DateTimeField, null)
    - ai_suggested_assignee changed from CharField to JSONField (default=dict) to store structured suggestion with reasoning
    - ActivityLog gains new actions: AUTO_ASSIGNED, CLAIMED, SLA_BREACHED, PRIORITY_BUMPED
    - calculate_sla_deadline(start_time, hours): adds business hours (8-20 IST, Mon-Sat), returns IST datetime
    - calculate_sla_deadline at 3 PM Wed with 2 hours -> 5 PM Wed
    - calculate_sla_deadline at 7 PM Wed with 2 hours -> 10 AM Thu (crosses overnight)
    - calculate_sla_deadline at 3 PM Sat with 14 hours -> 5 PM Mon (skips Sunday)
    - calculate_sla_deadline at 11 PM Sat with 1 hour -> 9 AM Mon (starts at next biz open)
    - set_sla_deadlines(email) reads SLAConfig for email's priority+category, sets both deadline fields
    - get_breached_emails() returns emails where now > deadline and status not closed
  </behavior>
  <action>
1. Add new models to apps/emails/models.py:
   - AssignmentRule(TimestampedModel): category CharField(max_length=100, db_index=True), assignee FK to AUTH_USER_MODEL (CASCADE, related_name="assignment_rules"), priority_order PositiveSmallIntegerField(default=0), is_active BooleanField(default=True). Meta: ordering=["category","priority_order"], unique_together=[("category","assignee")].
   - SLAConfig(TimestampedModel): priority CharField(max_length=20), category CharField(max_length=100), ack_hours FloatField(default=1.0), respond_hours FloatField(default=24.0). Meta: unique_together=[("priority","category")].
   - CategoryVisibility(TimestampedModel): user FK to AUTH_USER_MODEL (CASCADE, related_name="visible_categories"), category CharField(max_length=100). Meta: unique_together=[("user","category")].

2. Modify Email model:
   - Add sla_ack_deadline = DateTimeField(null=True, blank=True)
   - Add sla_respond_deadline = DateTimeField(null=True, blank=True)
   - Change ai_suggested_assignee from CharField(max_length=100) to JSONField(default=dict, blank=True). This stores {"name": "Rahul", "user_id": 5, "reason": "..."}.

3. Extend ActivityLog.Action choices:
   - AUTO_ASSIGNED = "auto_assigned", "Auto-Assigned"
   - CLAIMED = "claimed", "Claimed"
   - SLA_BREACHED = "sla_breached", "SLA Breached"
   - PRIORITY_BUMPED = "priority_bumped", "Priority Bumped"

4. Run makemigrations and migrate.

5. Create apps/emails/services/sla.py with:
   - IST, BIZ_START=8, BIZ_END=20, BIZ_DAYS=(0,1,2,3,4,5) constants
   - calculate_sla_deadline(start_time, hours) -- business hours calculator per RESEARCH.md Pattern 4 (the optimized version that jumps by day blocks, not minute-by-minute)
   - _snap_to_business_hours(dt) -- snap to next biz open if outside hours
   - _next_business_open(dt) -- return next business day at BIZ_START
   - set_sla_deadlines(email) -- looks up SLAConfig for email.priority + email.category, calculates both deadlines from email.received_at, saves to email. If no SLAConfig found, uses defaults: ack based on priority (CRITICAL=0.25, HIGH=0.5, MEDIUM=1, LOW=2), respond based on priority (CRITICAL=2, HIGH=4, MEDIUM=8, LOW=24).
   - get_breached_emails(breach_type="respond") -- returns Email queryset where now > sla_respond_deadline (or sla_ack_deadline), status not "closed", processing_status="completed", is_spam=False. Used by breach alerting in Plan 03.

6. Write tests in test_sla.py:
   - TestSLACalculator: test business hours calculation for same-day, overnight crossing, weekend skip, outside-hours start snap, fractional hours
   - TestBusinessHours: test _snap_to_business_hours, _next_business_open
   - TestSetSLADeadlines: test with SLAConfig present, test fallback defaults, test no-op for spam emails
   - TestBreachDetection: test get_breached_emails returns only breached non-closed emails

7. Write tests in test_auto_assignment.py:
   - TestAssignmentRuleModel: test creation, unique_together constraint, ordering
   - TestSLAConfigModel: test creation, unique_together constraint
   - TestCategoryVisibilityModel: test creation, unique_together constraint
  </action>
  <verify>
    <automated>cd /Users/uge/code/vipl-email-agent && source .venv/bin/activate && python manage.py makemigrations --check --dry-run && pytest apps/emails/tests/test_sla.py apps/emails/tests/test_auto_assignment.py -x -v</automated>
  </verify>
  <done>Three new models created with migrations applied. SLA calculator passes all business-hours edge cases. Breach detection query works. ai_suggested_assignee is JSONField. ActivityLog has 4 new action types.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Auto-assign batch job, claim service, pipeline SLA integration</name>
  <files>apps/emails/services/assignment.py, apps/emails/services/pipeline.py, apps/emails/tests/test_auto_assignment.py, apps/emails/tests/test_claiming.py</files>
  <behavior>
    - auto_assign_batch(): finds unassigned NEW COMPLETED non-spam emails, matches AssignmentRule by category, assigns to first-priority active person, uses optimistic locking (re-check unassigned before update), logs AUTO_ASSIGNED action, fires notifications
    - auto_assign_batch(): skips emails with no matching rule
    - auto_assign_batch(): skips emails where all rule assignees are inactive
    - auto_assign_batch(): does not overwrite manually assigned emails (race condition guard)
    - claim_email(email, claimed_by): validates email is unassigned, validates user has CategoryVisibility for that category (admin bypasses), calls assign_email() internally with note="Self-claimed", logs CLAIMED action
    - claim_email(): raises ValueError if email already assigned
    - claim_email(): raises PermissionError if user lacks category visibility (non-admin)
    - Pipeline: save_email_to_db calls set_sla_deadlines after creating/updating email
    - TriageResult.suggested_assignee is still a string; pipeline maps it to JSON when saving to Email.ai_suggested_assignee
  </behavior>
  <action>
1. Extend apps/emails/services/assignment.py:
   - Import AssignmentRule, CategoryVisibility from models
   - Add auto_assign_batch(): query Email.objects.filter(assigned_to__isnull=True, status=NEW, processing_status=COMPLETED, is_spam=False). For each, find AssignmentRule.objects.filter(category=email.category, is_active=True, assignee__is_active=True).order_by("priority_order"). If rules exist, take first(). Use optimistic locking: Email.objects.filter(pk=email.pk, assigned_to__isnull=True).update(...). If updated, create ActivityLog(action=AUTO_ASSIGNED, user=None, detail=f"Auto-assigned by category rule: {email.category}"). Fire-and-forget notifications via existing pattern.
   - Add claim_email(email, claimed_by): if email.assigned_to is not None, raise ValueError. If claimed_by is not staff/admin, check CategoryVisibility.objects.filter(user=claimed_by, category=email.category).exists() -- raise PermissionError if not found. Call assign_email(email, claimed_by, assigned_by=claimed_by, note="Self-claimed"). Override the action log to CLAIMED (update the last ActivityLog entry action from ASSIGNED to CLAIMED).
   - close_old_connections() at start of auto_assign_batch (runs from scheduler).

2. Modify apps/emails/services/pipeline.py:
   - Import set_sla_deadlines from apps.emails.services.sla
   - In save_email_to_db, after the update_or_create call and attachment save, call set_sla_deadlines(email_obj). Wrap in try/except (SLA failure should not crash pipeline).
   - When mapping triage.suggested_assignee (string) to email's ai_suggested_assignee (now JSONField), convert: if triage.suggested_assignee is a non-empty string, save {"name": triage.suggested_assignee} as the JSON value. Otherwise save empty dict.

3. Write tests in test_auto_assignment.py (extending from Task 1):
   - TestAutoAssignBatch: test matches rule and assigns, test skips when no rule, test skips already-assigned (race guard), test respects priority_order, test skips inactive assignees, test skips inactive rules

4. Write tests in test_claiming.py:
   - TestClaimEmail: test successful claim, test raises ValueError when already assigned, test raises PermissionError without category visibility, test admin bypasses visibility check, test ActivityLog shows CLAIMED action
  </action>
  <verify>
    <automated>cd /Users/uge/code/vipl-email-agent && source .venv/bin/activate && pytest apps/emails/tests/test_auto_assignment.py apps/emails/tests/test_claiming.py apps/emails/tests/test_sla.py -x -v</automated>
  </verify>
  <done>Auto-assign batch correctly matches rules and assigns with optimistic locking. Claiming validates visibility and records proper action. Pipeline sets SLA deadlines on every saved email. ai_suggested_assignee stored as structured JSON. All tests pass.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: AI processor workload-aware assignee suggestion (ASGN-04)</name>
  <files>apps/emails/services/ai_processor.py, apps/emails/services/dtos.py, apps/emails/tests/test_ai_suggestion.py</files>
  <behavior>
    - AI triage prompt includes workload context: each team member's name + their open (non-closed) email count (~50 tokens)
    - TRIAGE_TOOL_SCHEMA.suggested_assignee changes from string to object with properties: name (string), reason (string explaining why this person)
    - AIProcessor._build_user_message includes a "Team workload" section appended to the user message
    - _get_team_workload() queries User + Email counts, returns list of {name, open_count}
    - TriageResult.suggested_assignee remains a string for backward compat; new field suggested_assignee_detail (dict) holds the structured response
    - Pipeline maps suggested_assignee_detail to Email.ai_suggested_assignee JSONField (with name, reason, user_id if matched)
    - When no team members exist in DB, AI still works -- workload section says "No team data available"
  </behavior>
  <action>
1. Modify apps/emails/services/ai_processor.py:
   - Add _get_team_workload() class method or module function: queries User.objects.filter(is_active=True) and for each user, counts Email.objects.filter(assigned_to=user, status__in=["new","acknowledged"]).count(). Returns list of dicts: [{"name": user.get_full_name() or user.email, "open_count": N}, ...]. Import User and Email models. Wrap in try/except -- if DB not available (e.g., during tests without DB), return empty list.
   - Modify _build_user_message(): after the email body section, append a "Team workload" section with one line per team member: "- {name}: {open_count} open emails". If no team data, append "Team workload: No team data available". This adds ~50 tokens per request (negligible cost impact).
   - Modify TRIAGE_TOOL_SCHEMA: change "suggested_assignee" from type "string" to type "object" with properties: {"name": {"type": "string", "description": "Name of the suggested team member, or empty string if unclear"}, "reason": {"type": "string", "description": "Brief reason for this suggestion based on email content, category, and team workload"}}. Keep it in "required" list.
   - Modify _parse_triage_response(): when extracting suggested_assignee from the tool response, handle both formats for backward compatibility: if it's a string (old format), convert to {"name": value, "reason": ""}. If it's a dict (new format), use as-is. Store the dict in a new TriageResult field.

2. Modify apps/emails/services/dtos.py:
   - Add suggested_assignee_detail: dict field to TriageResult dataclass, default=empty dict. This holds {"name": "Rahul", "reason": "Sales lead expert with lowest workload"}.
   - Keep existing suggested_assignee: str field for backward compatibility (populated from detail["name"]).

3. Modify apps/emails/services/pipeline.py (small addition to Task 2's changes):
   - In save_email_to_db, when mapping to ai_suggested_assignee JSONField: prefer triage.suggested_assignee_detail if non-empty (it has name + reason). Try to match name to a User object to add user_id. Fallback to {"name": triage.suggested_assignee} if detail is empty.

4. Write tests in test_ai_suggestion.py:
   - TestGetTeamWorkload: test returns correct counts, test handles no users, test excludes inactive users
   - TestBuildUserMessage: test workload section appended, test empty workload handled
   - TestParseStructuredSuggestion: test dict format parsed correctly, test string format backward compat, test empty suggestion handled
   - TestTriageResultDetail: test suggested_assignee_detail populated alongside suggested_assignee string
  </action>
  <verify>
    <automated>cd /Users/uge/code/vipl-email-agent && source .venv/bin/activate && pytest apps/emails/tests/test_ai_suggestion.py -x -v && pytest apps/emails/tests/ -x -v</automated>
  </verify>
  <done>AI triage prompt includes team workload context. Suggested assignee returned as structured object with name + reason. Pipeline maps structured suggestion to Email.ai_suggested_assignee JSONField with user_id lookup. Backward compatibility maintained for string format. All tests pass.</done>
</task>

</tasks>

<verification>
- `pytest apps/emails/tests/test_sla.py apps/emails/tests/test_auto_assignment.py apps/emails/tests/test_claiming.py apps/emails/tests/test_ai_suggestion.py -x -v` -- all pass
- `python manage.py migrate --check` -- no unapplied migrations
- `python manage.py check --deploy` -- no critical issues
- Full suite: `pytest -v` -- no regressions
</verification>

<success_criteria>
- Three new models (AssignmentRule, SLAConfig, CategoryVisibility) with migrations applied
- Email has sla_ack_deadline and sla_respond_deadline fields
- ai_suggested_assignee is JSONField storing structured data
- ActivityLog has AUTO_ASSIGNED, CLAIMED, SLA_BREACHED, PRIORITY_BUMPED actions
- SLA calculator correctly handles business hours, weekends, overnight crossing
- Auto-assign batch job matches rules, respects priority order, handles race conditions
- Claim service validates category visibility, records CLAIMED action
- Pipeline sets SLA deadlines on every processed email
- AI processor includes team workload in triage prompt and returns structured assignee suggestion
- All tests pass with zero regressions
</success_criteria>

<output>
After completion, create `.planning/phases/04-assignment-engine-sla/04-01-SUMMARY.md`
</output>
