---
phase: 06-migration-cutover
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .github/workflows/deploy.yml
  - .planning/REQUIREMENTS.md
  - .planning/ROADMAP.md
autonomous: true
requirements:
  - CUTV-04
  - CUTV-01

must_haves:
  truths:
    - "deploy.yml uses sudo on all docker commands"
    - "deploy.yml has sleep 5 between up -d and migrate"
    - "deploy.yml has no Cloud Run references (including comments)"
    - "CUTV-01 requirement reflects fresh start, not data migration"
  artifacts:
    - path: ".github/workflows/deploy.yml"
      provides: "Production-ready CI/CD pipeline for VM deploy"
      contains: "sudo docker compose"
    - path: ".planning/REQUIREMENTS.md"
      provides: "Updated CUTV-01 wording"
      contains: "fresh"
  key_links:
    - from: ".github/workflows/deploy.yml"
      to: "VM deploy"
      via: "appleboy/ssh-action with sudo docker commands"
      pattern: "sudo docker compose"
---

<objective>
Fix deploy.yml for production readiness and update requirement wording.

Purpose: The deploy.yml has three issues that will cause the first deploy to fail: (1) docker commands lack `sudo` prefix (SSH user not in docker group), (2) no wait between `up -d` and `migrate` (container not ready), (3) Cloud Run comment that CUTV-04 requires removed. Also update CUTV-01 requirement to reflect the "fresh start" decision (no data migration).
Output: Production-ready deploy.yml, updated requirements.
</objective>

<execution_context>
@/Users/uge/.claude/get-shit-done/workflows/execute-plan.md
@/Users/uge/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-migration-cutover/06-CONTEXT.md
@.planning/phases/06-migration-cutover/06-RESEARCH.md
@.github/workflows/deploy.yml
@.planning/REQUIREMENTS.md

<interfaces>
From .github/workflows/deploy.yml (current, needs fixing):
```yaml
script: |
  cd /opt/vipl-email-agent
  git fetch --tags
  git checkout ${{ github.ref_name }}
  docker compose build --no-cache       # MISSING sudo
  docker compose up -d                  # MISSING sudo
  docker compose exec -T web python manage.py migrate --noinput  # MISSING sudo, MISSING sleep before
  echo "Deployed ${{ github.ref_name }}"
```
Line 8 comment: `# Replaces v1 Cloud Run deployment with VM SSH deploy.` -- must be removed (CUTV-04).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix deploy.yml for production VM deploy</name>
  <files>.github/workflows/deploy.yml</files>
  <action>
  Three changes to the deploy.yml script block:

  1. **Remove Cloud Run comment** (line 8): Replace `# Replaces v1 Cloud Run deployment with VM SSH deploy.` with `# VM deployment via SSH to taiga VM.` -- no Cloud Run references per CUTV-04.

  2. **Add sudo prefix** to all docker commands in the SSH script block:
     - `docker compose build --no-cache` -> `sudo docker compose build --no-cache`
     - `docker compose up -d` -> `sudo docker compose up -d`
     - `docker compose exec -T web python manage.py migrate --noinput` -> `sudo docker compose exec -T web python manage.py migrate --noinput`

  3. **Add sleep 5** between `sudo docker compose up -d` and the migrate command. This gives the web container time to boot before exec works.

  Do NOT change anything else in the file (job names, trigger config, test job, etc.).
  </action>
  <verify>
    <automated>grep -c "sudo docker compose" .github/workflows/deploy.yml | grep -q "3" && ! grep -qi "cloud.run\|cloudrun\|gcloud run\|Cloud Run" .github/workflows/deploy.yml && grep -q "sleep 5" .github/workflows/deploy.yml && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <done>deploy.yml has sudo on all 3 docker commands, sleep 5 before migrate, zero Cloud Run references (including comments)</done>
</task>

<task type="auto">
  <name>Task 2: Update CUTV-01 requirement and ROADMAP success criteria</name>
  <files>.planning/REQUIREMENTS.md, .planning/ROADMAP.md</files>
  <action>
  1. In `.planning/REQUIREMENTS.md`, update CUTV-01 line from:
     `- [ ] **CUTV-01**: All historical email data migrated from production Sheet to PostgreSQL with zero data loss`
     to:
     `- [ ] **CUTV-01**: v2 starts fresh — production Sheet preserved as read-only archive, no data migration needed`

  2. In `.planning/ROADMAP.md`, update Phase 6 success criteria item 1 from:
     `1. All historical email data from production Google Sheet is migrated to PostgreSQL with zero data loss`
     to:
     `1. v2 starts fresh — production Sheet preserved as read-only archive, no data migration needed`

  Do NOT change any other requirements or roadmap content.
  </action>
  <verify>
    <automated>grep "CUTV-01" .planning/REQUIREMENTS.md | grep -q "fresh" && grep "fresh" .planning/ROADMAP.md | grep -q "archive" && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <done>CUTV-01 wording reflects "fresh start" decision in both REQUIREMENTS.md and ROADMAP.md</done>
</task>

</tasks>

<verification>
- `grep -c "sudo docker compose" .github/workflows/deploy.yml` returns 3
- `grep -ri "cloud run" .github/workflows/deploy.yml` returns nothing
- `grep "sleep 5" .github/workflows/deploy.yml` returns a match
- `grep "CUTV-01" .planning/REQUIREMENTS.md` shows "fresh" wording
- `pytest --tb=short -q` passes (no regressions)
</verification>

<success_criteria>
- deploy.yml is production-ready: sudo on all docker commands, sleep 5 before migrate, no Cloud Run references
- CUTV-01 requirement wording updated to reflect "fresh start" decision
- All existing tests still pass
</success_criteria>

<output>
After completion, create `.planning/phases/06-migration-cutover/06-01-SUMMARY.md`
</output>
