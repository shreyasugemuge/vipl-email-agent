---
phase: 03-dashboard
plan: 03
type: execute
wave: 2
depends_on: ["03-01"]
files_modified:
  - apps/emails/views.py
  - apps/emails/urls.py
  - templates/emails/activity_log.html
  - templates/emails/_activity_feed.html
  - templates/base.html
  - apps/emails/tests/test_activity.py
autonomous: false
requirements: [DASH-05, DASH-06]

must_haves:
  truths:
    - "Dedicated activity log page shows all assignment and status change events across all emails"
    - "Activity log entries show who, what action, on which email, and when"
    - "Dashboard cards stack vertically on mobile (< 768px breakpoint)"
    - "Sidebar collapses on mobile"
    - "Cards are usable on small screens without horizontal scrolling"
  artifacts:
    - path: "templates/emails/activity_log.html"
      provides: "Full-page activity log view"
      contains: "activity"
    - path: "templates/emails/_activity_feed.html"
      provides: "Activity feed partial for HTMX pagination"
      contains: "hx-"
    - path: "apps/emails/views.py"
      provides: "activity_log view function"
      contains: "def activity_log"
  key_links:
    - from: "apps/emails/views.py"
      to: "apps/emails/models.py"
      via: "ActivityLog.objects.select_related('email', 'user')"
      pattern: "ActivityLog.*select_related"
    - from: "templates/base.html"
      to: "templates/emails/activity_log.html"
      via: "Sidebar Activity nav link"
      pattern: "activity"
---

<objective>
Add the activity log page and make the dashboard mobile-responsive. Activity log is the audit trail showing who assigned/reassigned/changed status. Mobile responsiveness ensures the dashboard is usable on phones.

Purpose: Complete the dashboard feature set (DASH-05, DASH-06). The activity log gives the manager visibility into team actions. Mobile-responsive layout means the manager can check triage status from their phone.

Output: /emails/activity/ page with paginated activity feed, responsive card layout that works on mobile, visual verification checkpoint.
</objective>

<execution_context>
@/Users/uge/.claude/get-shit-done/workflows/execute-plan.md
@/Users/uge/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-dashboard/03-CONTEXT.md
@.planning/phases/03-dashboard/03-RESEARCH.md
@.planning/phases/03-dashboard/03-01-SUMMARY.md

<interfaces>
<!-- From Plan 01 outputs -->

From apps/emails/models.py (added in Plan 01):
```python
class ActivityLog(TimestampedModel):
    class Action(models.TextChoices):
        ASSIGNED = "assigned", "Assigned"
        REASSIGNED = "reassigned", "Reassigned"
        STATUS_CHANGED = "status_changed", "Status Changed"
        ACKNOWLEDGED = "acknowledged", "Acknowledged"
        CLOSED = "closed", "Closed"

    email = models.ForeignKey(Email, on_delete=models.CASCADE, related_name="activity_logs")
    user = models.ForeignKey(settings.AUTH_USER_MODEL, on_delete=models.SET_NULL, null=True)
    action = models.CharField(max_length=20, choices=Action.choices)
    detail = models.TextField(blank=True, default="")
    old_value = models.CharField(max_length=255, blank=True, default="")
    new_value = models.CharField(max_length=255, blank=True, default="")
```

From templates/base.html (rebuilt in Plan 01):
- Sidebar with nav links including "Activity" link
- Tailwind CSS v4 CDN + HTMX 2.0 CDN
- Sidebar width: w-56, main content: flex-1
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Activity log page, mobile-responsive layout</name>
  <files>
    apps/emails/views.py,
    apps/emails/urls.py,
    templates/emails/activity_log.html,
    templates/emails/_activity_feed.html,
    templates/base.html,
    apps/emails/tests/test_activity.py
  </files>
  <behavior>
    - Test 1: activity_log view returns 200 for authenticated user
    - Test 2: activity_log view shows ActivityLog entries with user, action, email subject, timestamp
    - Test 3: activity_log view paginates at 50 entries per page
    - Test 4: activity_log HTMX request returns _activity_feed.html partial
    - Test 5: activity_log non-HTMX request returns full activity_log.html page
    - Test 6: activity_log entries ordered by -created_at (newest first)
  </behavior>
  <action>
    1. Add activity_log view to apps/emails/views.py:
       - Login required
       - Queryset: ActivityLog.objects.select_related("email", "user").order_by("-created_at")
       - For non-admin members without can_see_all_emails: filter to own activity or activity on own assigned emails
       - Paginate at 50 per page
       - HTMX detection: return _activity_feed.html partial for HTMX, activity_log.html for full page

    2. Add URL route: path("activity/", views.activity_log, name="activity_log")

    3. Create templates/emails/activity_log.html:
       - Extends base.html
       - Page title: "Activity Log"
       - Contains #activity-feed div with include of _activity_feed.html

    4. Create templates/emails/_activity_feed.html:
       - List of activity entries, each showing:
         - User name (who performed the action)
         - Action label (Assigned, Reassigned, Status Changed, etc.)
         - Email subject (linked to email detail or email list with ?selected=pk)
         - Old value -> New value (for reassignment/status changes)
         - Detail/note (if present)
         - Time ago
       - Pagination with hx-get for next page, hx-target="#activity-feed", hx-swap="innerHTML"
       - Empty state: "No activity yet"

    5. Make dashboard mobile-responsive by updating templates:
       - templates/base.html:
         - Sidebar: hidden on mobile (hidden md:flex), hamburger menu button in topbar (visible on mobile only)
         - Add simple JS toggle for sidebar visibility on mobile (inline, no external file)
         - Main content: full width on mobile
       - templates/emails/email_list.html:
         - Card list: full width on mobile (no detail panel split). Detail panel hidden on mobile or shown as overlay/modal
         - Tab bar: horizontal scroll on mobile if needed
         - Filter dropdowns: stack vertically or use a collapsible filter section on mobile
       - templates/emails/_email_card.html:
         - Cards stack vertically (already natural with flex-col)
         - Ensure text doesn't overflow, truncate long subjects
         - Priority/category badges wrap gracefully
       - Use Tailwind responsive prefixes: md: for tablet, lg: for desktop
       - Desktop-first as specified in CONTEXT.md (default styles = desktop, responsive overrides for mobile)

    6. Write tests in apps/emails/tests/test_activity.py covering behaviors above.

    7. Ensure the Activity nav link in sidebar points to /emails/activity/.
  </action>
  <verify>
    <automated>cd /Users/uge/code/vipl-email-agent && source .venv/bin/activate && pytest apps/emails/tests/test_activity.py -x -q</automated>
  </verify>
  <done>
    - /emails/activity/ shows paginated activity feed with user, action, email, timestamp
    - HTMX pagination works on activity feed
    - Sidebar collapses on mobile with hamburger toggle
    - Cards stack vertically on small screens
    - Detail panel is hidden or overlay on mobile
    - All tests pass
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Visual verification of complete dashboard</name>
  <action>
    Human verifies the complete Phase 3 dashboard works end-to-end.

    What was built:
    - Email card list at /emails/ with filtering, sorting, pagination
    - Assignment dropdown on cards, slide-out detail panel
    - Status change controls (Acknowledge, Close)
    - Activity log page at /emails/activity/
    - Mobile-responsive layout

    How to verify:
    1. Start dev server: `source .venv/bin/activate && python manage.py test_pipeline --count 11 && python manage.py runserver`
    2. Log in at http://localhost:8000/accounts/login/
    3. Verify email card list at /emails/ shows cards with priority badges, sender, subject, summary
    4. Click tabs: All, Unassigned, My Emails -- verify filtering works
    5. Use dropdown filters (Status, Priority) -- verify list updates via HTMX
    6. Click a card -- verify detail panel slides out on the right with full email body, draft reply, attachments
    7. Assign an email via the dropdown (as admin) -- verify card updates, activity log entry created
    8. Click "Acknowledge" on an assigned email -- verify status changes
    9. Visit /emails/activity/ -- verify activity log shows assignment/status events
    10. Open Chrome DevTools, toggle mobile viewport (iPhone 14) -- verify cards stack, sidebar hidden, hamburger menu works
  </action>
  <verify>Human confirms dashboard works end-to-end</verify>
  <done>User types "approved" or describes issues to fix</done>
</task>

</tasks>

<verification>
- `pytest apps/emails/tests/test_activity.py -x -q` -- activity log tests pass
- `pytest -v` -- full suite green
- Visual: dashboard works on desktop and mobile viewport
</verification>

<success_criteria>
- /emails/activity/ shows paginated feed of all assignment and status change events
- Activity entries show who, what, which email, when
- Dashboard is usable on mobile: cards stack, sidebar collapses, no horizontal scroll
- Human verification confirms the complete dashboard flow works end-to-end
</success_criteria>

<output>
After completion, create `.planning/phases/03-dashboard/03-03-SUMMARY.md`
</output>
