---
phase: 03-dashboard
plan: 02
type: execute
wave: 2
depends_on: ["03-01"]
files_modified:
  - apps/emails/services/assignment.py
  - apps/emails/services/chat_notifier.py
  - apps/emails/views.py
  - apps/emails/urls.py
  - templates/emails/_email_card.html
  - templates/emails/_email_detail.html
  - templates/emails/_assign_dropdown.html
  - apps/emails/tests/test_assignment.py
autonomous: true
requirements: [ASGN-01, ASGN-02, ASGN-05, SLA-01]

must_haves:
  truths:
    - "Admin can assign an unassigned email to a team member via dropdown on the card"
    - "Admin can reassign an email to a different team member with optional note"
    - "Assignment triggers a Google Chat notification to the assignee"
    - "Team member can change status on their own email: Acknowledge and Close"
    - "Only admins can assign/reassign"
    - "Click on card opens slide-out detail panel with full email body, draft reply, attachments, assign/status controls, Open in Gmail"
    - "Activity log entry created for every assignment and status change"
  artifacts:
    - path: "apps/emails/services/assignment.py"
      provides: "assign_email and change_status service functions"
      exports: ["assign_email", "change_status"]
    - path: "apps/emails/services/chat_notifier.py"
      provides: "notify_assignment method on ChatNotifier"
      contains: "notify_assignment"
    - path: "templates/emails/_email_detail.html"
      provides: "Slide-out detail panel partial"
      contains: "detail-panel"
    - path: "templates/emails/_assign_dropdown.html"
      provides: "Assignee dropdown partial for HTMX swap"
      contains: "hx-post"
  key_links:
    - from: "apps/emails/views.py"
      to: "apps/emails/services/assignment.py"
      via: "assign_email_view calls assign_email service"
      pattern: "assign_email"
    - from: "apps/emails/services/assignment.py"
      to: "apps/emails/services/chat_notifier.py"
      via: "Assignment triggers notify_assignment"
      pattern: "notify_assignment"
    - from: "apps/emails/services/assignment.py"
      to: "apps/emails/models.py"
      via: "Creates ActivityLog entries"
      pattern: "ActivityLog\\.objects\\.create"
---

<objective>
Add assignment workflow, status changes, detail panel, and Chat notifications. This is the core action layer -- the manager assigns emails, team members acknowledge them, and everyone gets notified.

Purpose: Move from "view-only list" to "actionable inbox management." The manager should be able to triage 20 emails in 2 minutes with dropdown assigns (per CONTEXT.md).

Output: Working assignment dropdown on cards, slide-out detail panel, status change controls, Chat notifications on assignment, activity log entries for all actions.
</objective>

<execution_context>
@/Users/uge/.claude/get-shit-done/workflows/execute-plan.md
@/Users/uge/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-dashboard/03-CONTEXT.md
@.planning/phases/03-dashboard/03-RESEARCH.md
@.planning/phases/03-dashboard/03-01-SUMMARY.md

<interfaces>
<!-- From Plan 01 outputs -- executor needs these contracts -->

From apps/emails/models.py (added in Plan 01):
```python
class ActivityLog(TimestampedModel):
    class Action(models.TextChoices):
        ASSIGNED = "assigned", "Assigned"
        REASSIGNED = "reassigned", "Reassigned"
        STATUS_CHANGED = "status_changed", "Status Changed"
        ACKNOWLEDGED = "acknowledged", "Acknowledged"
        CLOSED = "closed", "Closed"

    email = models.ForeignKey(Email, on_delete=models.CASCADE, related_name="activity_logs")
    user = models.ForeignKey(settings.AUTH_USER_MODEL, on_delete=models.SET_NULL, null=True)
    action = models.CharField(max_length=20, choices=Action.choices)
    detail = models.TextField(blank=True, default="")
    old_value = models.CharField(max_length=255, blank=True, default="")
    new_value = models.CharField(max_length=255, blank=True, default="")

    class Meta:
        ordering = ["-created_at"]
```

From apps/emails/views.py (added in Plan 01):
```python
def email_list(request):  # Main dashboard view with filtering/sorting/pagination
```

From apps/emails/services/chat_notifier.py (existing from Phase 2):
```python
class ChatNotifier:
    def __init__(self, webhook_url: str = ""):
        ...
    def _post(self, payload: dict) -> bool:
        ...
    def _is_quiet_hours(self) -> bool:
        ...
    def notify_new_emails(self, emails) -> bool:
        ...
```

From apps/emails/urls.py (after Plan 01):
```python
urlpatterns = [
    path("", views.email_list, name="email_list"),
    path("inspect/", views.inspect, name="inspect"),
]
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Assignment service, status change, Chat notification</name>
  <files>
    apps/emails/services/assignment.py,
    apps/emails/services/chat_notifier.py,
    apps/emails/tests/test_assignment.py
  </files>
  <behavior>
    - Test 1: assign_email sets assigned_to, assigned_by, assigned_at on Email
    - Test 2: assign_email creates ActivityLog with action=ASSIGNED, new_value=assignee name
    - Test 3: reassign_email (email already has assignee) creates ActivityLog with action=REASSIGNED, old_value=old assignee, new_value=new assignee
    - Test 4: assign_email with note stores note in ActivityLog.detail
    - Test 5: assign_email calls ChatNotifier.notify_assignment (mocked)
    - Test 6: change_status updates Email.status and creates ActivityLog with action=STATUS_CHANGED
    - Test 7: change_status with action=ACKNOWLEDGED sets status to "acknowledged"
    - Test 8: change_status with action=CLOSED sets status to "closed"
    - Test 9: ChatNotifier.notify_assignment sends Cards v2 payload with subject, sender, priority, dashboard link
    - Test 10: ChatNotifier.notify_assignment respects quiet hours (returns False during quiet)
  </behavior>
  <action>
    1. Create apps/emails/services/assignment.py with two service functions:

       `assign_email(email, assignee, assigned_by, note="")`:
       - Get old_assignee before update
       - Set email.assigned_to = assignee, email.assigned_by = assigned_by, email.assigned_at = timezone.now()
       - save(update_fields=["assigned_to", "assigned_by", "assigned_at", "updated_at"])
       - Determine action: REASSIGNED if old_assignee else ASSIGNED
       - Create ActivityLog entry with old_value/new_value (use get_full_name or username)
       - Fire-and-forget: call ChatNotifier.notify_assignment (catch exceptions, log errors, never crash)
       - Return updated email

       `change_status(email, new_status, changed_by)`:
       - Validate new_status is in Email.Status.values
       - Store old_status
       - Set email.status = new_status, save(update_fields=["status", "updated_at"])
       - Map status to ActivityLog action: "acknowledged" -> ACKNOWLEDGED, "closed" -> CLOSED, else STATUS_CHANGED
       - Create ActivityLog entry with old_value=old_status, new_value=new_status
       - Return updated email

    2. Add notify_assignment method to ChatNotifier:
       - Takes Django Email model instance and assignee User
       - Respects quiet hours (return False if quiet)
       - Build Cards v2 payload:
         - Header: "Assigned to you: {subject[:50]}", subtitle: "{priority_emoji} {priority} | {category}"
         - Section widgets: From (name + address), Summary (ai_summary), "Open in Dashboard" button linking to /emails/?selected={pk}
       - Call self._post(payload), return bool
       - Get tracker_url from SystemConfig.get("tracker_url", "https://triage.vidarbhainfotech.com")

    3. Write tests in apps/emails/tests/test_assignment.py covering all behaviors above.
       - Mock ChatNotifier._post to avoid real webhook calls
       - Use pytest-django fixtures (@pytest.mark.django_db)
       - Create test Email and User objects using model factories or direct creation
  </action>
  <verify>
    <automated>cd /Users/uge/code/vipl-email-agent && source .venv/bin/activate && pytest apps/emails/tests/test_assignment.py -x -q</automated>
  </verify>
  <done>
    - assign_email service function assigns, logs activity, notifies via Chat
    - change_status service function updates status and logs activity
    - ChatNotifier.notify_assignment sends Cards v2 with correct payload
    - All assignment tests pass
  </done>
</task>

<task type="auto">
  <name>Task 2: Assignment views, detail panel, status change endpoints</name>
  <files>
    apps/emails/views.py,
    apps/emails/urls.py,
    templates/emails/_email_detail.html,
    templates/emails/_assign_dropdown.html,
    templates/emails/_email_card.html
  </files>
  <action>
    1. Add views to apps/emails/views.py:

       `email_detail(request, pk)` (GET):
       - Login required
       - Get email with select_related("assigned_to", "assigned_by") and prefetch activity_logs
       - Get attachment metadata via email.attachmentmetadata_set.all()
       - Get team members for assign dropdown (active users)
       - Return _email_detail.html partial

       `assign_email_view(request, pk)` (POST, admin only):
       - Login required, check request.user.is_admin_role (return 403 if not)
       - Get assignee_id from POST data, get User object
       - Get optional note from POST data
       - Call assign_email service function
       - Return updated _email_card.html partial (hx-swap="outerHTML" on the card)

       `change_status_view(request, pk)` (POST):
       - Login required
       - Get new_status from POST data
       - Permission check: user must be admin OR the assigned_to user
       - Team members can only Acknowledge or Close their own assigned emails
       - Call change_status service function
       - Return updated _email_detail.html partial (refreshes the detail panel)

    2. Add URL routes in apps/emails/urls.py:
       - path("<int:pk>/detail/", views.email_detail, name="email_detail")
       - path("<int:pk>/assign/", views.assign_email_view, name="assign_email")
       - path("<int:pk>/status/", views.change_status_view, name="change_status")

    3. Create templates/emails/_email_detail.html:
       - Slide-out panel content (this is loaded into #detail-panel via HTMX)
       - Header: subject, priority badge, category badge, status badge
       - "Open in Gmail" link (email.gmail_link, opens in new tab). If no gmail_link, show "No Gmail link"
       - Assign dropdown (admin only): select with team members, hx-post to assign endpoint, hx-target="#card-{pk}" for card update
       - Optional note field: hidden by default, appears on reassignment (if email already assigned)
       - Status controls: buttons for available transitions
         - If assigned_to == current user or user is admin:
           - "Acknowledge" button (if status is new)
           - "Close" button (if status is acknowledged or replied)
       - Full email body (body or body_html rendered safely)
       - AI draft reply (if exists, in a collapsible section)
       - Attachment list (filename, size, mime_type)
       - Activity log: list of recent activity entries (from email.activity_logs.select_related("user").all()[:20])

    4. Create templates/emails/_assign_dropdown.html:
       - Select element with team members
       - hx-post to /emails/{pk}/assign/
       - hx-target="#card-{pk}" hx-swap="outerHTML"
       - Include "Unassigned" option (value="")
       - Current assignee pre-selected

    5. Update templates/emails/_email_card.html (from Plan 01):
       - Add hx-get="/emails/{pk}/detail/" hx-target="#detail-panel" hx-swap="innerHTML" on card click
       - Add assignee dropdown (for admin users) that triggers inline assignment without opening detail panel
       - Ensure card has id="card-{{ email.pk }}" for HTMX targeting after assignment

    6. Update templates/emails/email_list.html (from Plan 01):
       - Add #detail-panel div on the right side (40% list / 60% detail split per CONTEXT.md)
       - Detail panel starts empty with a prompt message ("Select an email to view details")
  </action>
  <verify>
    <automated>cd /Users/uge/code/vipl-email-agent && source .venv/bin/activate && pytest apps/emails/tests/ -x -q</automated>
  </verify>
  <done>
    - Click card opens slide-out detail panel with full email body, draft reply, attachments, activity log
    - Admin can assign/reassign via dropdown on card or in detail panel
    - Team member can Acknowledge or Close their assigned email via detail panel buttons
    - Assignment returns updated card partial (card reflects new assignee instantly)
    - Status change refreshes detail panel
    - All existing + new tests pass
  </done>
</task>

</tasks>

<verification>
- `pytest apps/emails/tests/test_assignment.py -x -q` -- assignment service tests pass
- `pytest apps/emails/tests/ -x -q` -- all email app tests pass
- `pytest -v` -- full suite green
- Manual: assign an email via dropdown, verify card updates and activity log entry appears in detail panel
</verification>

<success_criteria>
- Admin assigns email via dropdown -> card shows new assignee, ActivityLog created, Chat notification sent
- Admin reassigns email -> old assignee replaced, ActivityLog shows reassignment with old/new values
- Team member clicks Acknowledge -> status changes, ActivityLog created
- Team member clicks Close -> status changes, ActivityLog created
- Non-admin cannot assign (403 returned)
- Detail panel shows full email body, draft reply, attachments, activity log, Open in Gmail link
</success_criteria>

<output>
After completion, create `.planning/phases/03-dashboard/03-02-SUMMARY.md`
</output>
