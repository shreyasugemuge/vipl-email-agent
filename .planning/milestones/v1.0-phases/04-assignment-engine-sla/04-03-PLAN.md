---
phase: 04-assignment-engine-sla
plan: 03
type: execute
wave: 3
depends_on: ["04-01", "04-02"]
files_modified:
  - apps/emails/services/sla.py
  - apps/emails/services/chat_notifier.py
  - apps/emails/services/assignment.py
  - apps/emails/management/commands/run_scheduler.py
  - apps/emails/tests/test_sla.py
autonomous: false
requirements: [SLA-03, SLA-04]

must_haves:
  truths:
    - "SLA breach summary posts to Chat at 9 AM, 1 PM, 5 PM IST with counts and top 3 worst offenders"
    - "Manager gets full breach summary; each assignee gets a separate personal message listing only their breached emails"
    - "Auto-escalation bumps priority one level on breach and logs PRIORITY_BUMPED in ActivityLog"
    - "Auto-assign batch job runs every 3 minutes via APScheduler"
    - "SLA breach check runs every 15 minutes via APScheduler"
  artifacts:
    - path: "apps/emails/services/sla.py"
      provides: "check_and_escalate_breaches(), build_breach_summary()"
      contains: "def check_and_escalate_breaches"
    - path: "apps/emails/services/chat_notifier.py"
      provides: "notify_breach_summary() and notify_personal_breach() methods on ChatNotifier"
      contains: "def notify_breach_summary"
    - path: "apps/emails/management/commands/run_scheduler.py"
      provides: "auto_assign_batch job (interval 3min), SLA breach check (every 15min), SLA summary (CronTrigger 9,13,17)"
      contains: "auto_assign"
  key_links:
    - from: "apps/emails/management/commands/run_scheduler.py"
      to: "apps/emails/services/assignment.py"
      via: "scheduler calls auto_assign_batch every 3 minutes"
      pattern: "auto_assign_batch"
    - from: "apps/emails/management/commands/run_scheduler.py"
      to: "apps/emails/services/sla.py"
      via: "scheduler calls check_and_escalate_breaches on CronTrigger(hour=9,13,17)"
      pattern: "check_and_escalate_breaches"
    - from: "apps/emails/services/sla.py"
      to: "apps/emails/services/chat_notifier.py"
      via: "breach summary calls ChatNotifier.notify_breach_summary and notify_personal_breach per assignee"
      pattern: "notify_breach_summary|notify_personal_breach"
---

<objective>
SLA breach alerting, auto-escalation, and scheduler integration for auto-assign + SLA jobs.

Purpose: Wire the batch auto-assignment job and SLA breach detection into APScheduler. Add breach summary Chat notifications (3x daily at 9, 13, 17 IST) with both a manager summary and per-assignee personal alerts. Implement auto-escalation (priority bump on breach). This completes the automation layer -- the system now auto-assigns, tracks SLA, detects breaches, escalates, and alerts.

Output: Scheduler with 2 new jobs (auto-assign every 3min, SLA breach check/summary at 9/13/17), ChatNotifier breach summary + personal alerts, auto-escalation logic, tests, visual checkpoint.
</objective>

<execution_context>
@/Users/uge/.claude/get-shit-done/workflows/execute-plan.md
@/Users/uge/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-assignment-engine-sla/04-CONTEXT.md
@.planning/phases/04-assignment-engine-sla/04-RESEARCH.md
@.planning/phases/04-assignment-engine-sla/04-01-SUMMARY.md
@.planning/phases/04-assignment-engine-sla/04-02-SUMMARY.md

<interfaces>
<!-- Contracts from Plan 01 and 02 -->

From apps/emails/services/sla.py (Plan 01):
```python
def calculate_sla_deadline(start_time, hours) -> datetime
def set_sla_deadlines(email) -> None
def get_breached_emails(breach_type="respond") -> QuerySet
```

From apps/emails/services/assignment.py (Plan 01):
```python
def auto_assign_batch() -> None
def claim_email(email, claimed_by) -> Email
def assign_email(email, assignee, assigned_by, note="") -> Email
```

From apps/emails/services/chat_notifier.py:
```python
class ChatNotifier:
    def __init__(self, webhook_url: str): ...
    def _post(self, payload: dict) -> bool: ...
    def _is_quiet_hours(self) -> bool: ...
    def notify_assignment(self, email, assignee) -> bool: ...
    def notify_new_emails(self, emails) -> bool: ...
```

From apps/emails/models.py (Plan 01):
```python
class ActivityLog:
    class Action:
        SLA_BREACHED = "sla_breached"
        PRIORITY_BUMPED = "priority_bumped"

class Email:
    sla_ack_deadline = DateTimeField(null=True, blank=True)
    sla_respond_deadline = DateTimeField(null=True, blank=True)
    priority = CharField(max_length=50)
    assigned_to = ForeignKey(AUTH_USER_MODEL, ...)
```

From apps/emails/management/commands/run_scheduler.py:
```python
# Existing jobs: heartbeat (1min), poll (5min), retry (30min)
# Uses BlockingScheduler with timezone="Asia/Kolkata"
# Pattern: top-level functions with close_old_connections() at start
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Breach detection, auto-escalation, Chat summary with per-assignee alerts, scheduler jobs</name>
  <files>apps/emails/services/sla.py, apps/emails/services/chat_notifier.py, apps/emails/services/assignment.py, apps/emails/management/commands/run_scheduler.py, apps/emails/tests/test_sla.py</files>
  <behavior>
    - check_and_escalate_breaches(): finds breached emails (respond), bumps priority one level (MEDIUM->HIGH, HIGH->CRITICAL, CRITICAL stays), logs PRIORITY_BUMPED + SLA_BREACHED in ActivityLog, builds summary, calls ChatNotifier for manager summary AND per-assignee personal alerts
    - Priority bump: only bumps once per breach cycle (check if SLA_BREACHED already logged for this email in last 24h to avoid re-bumping)
    - build_breach_summary(): returns dict with total_breached count, per-assignee breakdown, top 3 worst offenders (most overdue first)
    - ChatNotifier.notify_breach_summary(summary_data): posts Cards v2 with breach count header, top 3 offenders section, per-assignee section -- this is the MANAGER summary (full view of all breaches)
    - ChatNotifier.notify_personal_breach(assignee_name, assignee_emails): posts a SEPARATE Cards v2 message per assignee listing only THEIR breached emails (subject, priority, time overdue). Per CONTEXT.md: "each assignee gets personal alert (their breached emails only)"
    - check_and_escalate_breaches sends manager summary first, then iterates per-assignee breakdown and calls notify_personal_breach for each assignee who has breached emails
    - Scheduler: auto_assign_batch every 3 minutes (interval), SLA breach summary at 9,13,17 IST (CronTrigger hour="9,13,17" minute=0), SLA update check every 15 minutes (interval)
    - _sla_check_job: runs get_breached_emails, updates breach status fields (future-proof), does NOT send alerts (that is the cron job)
    - _sla_summary_job: runs check_and_escalate_breaches + sends Chat summary (only at 9,13,17)
  </behavior>
  <action>
1. Extend apps/emails/services/sla.py:
   - Add PRIORITY_ESCALATION = {"LOW": "MEDIUM", "MEDIUM": "HIGH", "HIGH": "CRITICAL", "CRITICAL": "CRITICAL"}
   - Add check_and_escalate_breaches(chat_notifier=None): calls get_breached_emails("respond") + get_breached_emails("ack"). For each breached email:
     a. Check if ActivityLog with action=SLA_BREACHED exists for this email in last 24 hours -- skip escalation if yes (avoid re-bumping).
     b. If not already escalated: bump priority via PRIORITY_ESCALATION map. Save email. Create ActivityLog(action=PRIORITY_BUMPED, detail=f"SLA breach: {old_priority} -> {new_priority}"). Create ActivityLog(action=SLA_BREACHED, detail=f"{'Ack' or 'Respond'} deadline breached").
   - Add build_breach_summary(breached_respond, breached_ack): returns dict with:
     - total_respond_breached, total_ack_breached
     - per_assignee: dict of assignee_name -> list of {subject, priority, overdue_minutes}
     - top_offenders: list of top 3 most overdue emails [{subject, assignee_name, priority, overdue_str}]
   - After escalation, if chat_notifier provided and there are any breached emails:
     a. Call chat_notifier.notify_breach_summary(summary) -- manager gets full summary
     b. For each assignee in summary["per_assignee"], call chat_notifier.notify_personal_breach(assignee_name, assignee_emails) -- each assignee gets only their breached emails

2. Add notify_breach_summary AND notify_personal_breach to ChatNotifier:
   - notify_breach_summary(self, summary_data: dict) -> bool:
     Respects quiet hours (skip if quiet).
     Builds Cards v2 payload: header with total breach count, section with top 3 worst offenders (subject[:50], assignee, time overdue, priority emoji), section with per-assignee breakdown counts. This is the MANAGER view.
   - notify_personal_breach(self, assignee_name: str, breached_emails: list) -> bool:
     Respects quiet hours (skip if quiet).
     Builds Cards v2 payload: header "@{assignee_name}: {count} SLA breach(es) need attention", section listing each breached email (subject[:50], priority, time overdue). This is the PERSONAL alert per CONTEXT.md requirement "each assignee gets personal alert (their breached emails only)".
     Note: Google Chat webhook posts to the same space (no per-user DM). The personal alert is a separate message in the channel @-mentioning or naming the assignee. True DMs would require Chat API with user-scoped auth (deferred).

3. Add scheduler jobs to run_scheduler.py:
   - Import auto_assign_batch from assignment service, import check_and_escalate_breaches from sla service.
   - Add _auto_assign_job() wrapper: close_old_connections(), call auto_assign_batch() in try/except.
   - Add _sla_summary_job(chat_notifier) wrapper: close_old_connections(), call check_and_escalate_breaches(chat_notifier) in try/except.
   - Register jobs in handle():
     a. scheduler.add_job(_auto_assign_job, "interval", minutes=3, id="auto_assign", max_instances=1, coalesce=True)
     b. scheduler.add_job(_sla_summary_job, CronTrigger(hour="9,13,17", minute=0, timezone="Asia/Kolkata"), args=[chat_notifier], id="sla_breach_summary", max_instances=1, coalesce=True)
   - Update the startup log message to include new jobs.

4. Write/extend tests in test_sla.py:
   - TestBreachSummary: test build_breach_summary returns correct structure, test top 3 ordering by overdue, test per_assignee breakdown groups correctly
   - TestCheckAndEscalate: test priority bump from MEDIUM to HIGH, test CRITICAL stays CRITICAL, test skip re-bump within 24h, test ActivityLog entries created
   - TestChatBreachSummary: test notify_breach_summary builds correct Cards v2 payload (mock _post), test it includes per-assignee counts
   - TestChatPersonalBreach: test notify_personal_breach builds correct per-assignee Cards v2 payload (mock _post), test it lists only that assignee's emails, test quiet hours respected
   - TestBreachNotificationFlow: test check_and_escalate_breaches calls notify_breach_summary once AND notify_personal_breach N times (once per assignee with breaches)
  </action>
  <verify>
    <automated>cd /Users/uge/code/vipl-email-agent && source .venv/bin/activate && pytest apps/emails/tests/test_sla.py -x -v && pytest -v</automated>
  </verify>
  <done>SLA breach detection finds overdue emails. Auto-escalation bumps priority and logs activity. Manager gets full breach summary via Chat. Each assignee gets a separate personal breach alert listing only their emails. Scheduler has auto-assign (3min) + SLA summary (9/13/17 IST) jobs. All tests pass.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Visual verification of complete Phase 4 automation</name>
  <files></files>
  <what-built>Complete Phase 4 automation layer: auto-assignment rules, SLA deadlines, breach detection with escalation, Chat alerts (manager summary + per-assignee personal alerts), claiming flow, AI workload-aware suggestions, settings page.</what-built>
  <how-to-verify>
1. Start dev server: `source .venv/bin/activate && python manage.py runserver`
2. Log in as admin at http://127.0.0.1:8000/
3. Navigate to Settings (/emails/settings/):
   a. Assignment Rules tab: add a rule for "Sales Lead" -> pick a team member. Drag to reorder if multiple.
   b. Category Visibility tab: check categories for a team member.
   c. SLA Configuration tab: set ack=0.5h and respond=4h for HIGH + Sales Lead. Save.
4. Navigate to email list (/emails/):
   a. Verify SLA countdown appears on email cards (color-coded).
   b. Click an unassigned email -- verify SLA bar in detail panel shows ack + respond times.
   c. If the email has an AI suggestion, verify the blue suggestion bar with Accept/Dismiss buttons.
5. Log in as a team member (non-admin):
   a. Verify "Claim" button appears on unassigned emails in their visible categories.
   b. Verify "Claim" button does NOT appear on categories they cannot see.
6. Check activity log (/emails/activity/) for any auto_assigned or claimed entries.
7. Run full test suite: `pytest -v` -- confirm all green.
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

</tasks>

<verification>
- `pytest apps/emails/tests/test_sla.py -x -v` -- SLA + breach tests pass
- `pytest -v` -- full suite green
- Scheduler starts with 5 jobs (heartbeat, poll, retry, auto_assign, sla_breach_summary)
- SLA countdowns visible on cards with correct color coding
- Settings page functional with all three tabs
- Breach summary sends manager summary AND per-assignee personal alerts
</verification>

<success_criteria>
- SLA breach detection correctly identifies overdue emails (ack + respond separately)
- Auto-escalation bumps priority one level on breach, logs to ActivityLog
- Chat breach summary posts at configured times with counts + top 3 offenders (manager view)
- Each assignee gets a separate personal breach alert listing only their breached emails
- Scheduler runs auto-assign every 3 minutes and SLA summary at 9/13/17 IST
- Human verification confirms end-to-end flow works visually
- Full test suite passes
</success_criteria>

<output>
After completion, create `.planning/phases/04-assignment-engine-sla/04-03-SUMMARY.md`
</output>
