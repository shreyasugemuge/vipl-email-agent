---
phase: 04-collaboration
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/emails/models.py
  - apps/emails/views.py
  - apps/emails/urls.py
  - apps/emails/services/assignment.py
  - templates/emails/_thread_detail.html
  - templates/emails/_thread_note.html
  - apps/emails/tests/test_notes.py
autonomous: true
requirements: [COLLAB-01, COLLAB-02, COLLAB-03]

must_haves:
  truths:
    - "User can add an internal note on any thread via a text input at the bottom of the thread detail"
    - "Notes are stored in the database and never visible to the email sender"
    - "Notes appear inline in the thread timeline, visually distinct from email messages (yellow/amber background, 'Internal note' label)"
    - "@mentioning a team member in a note triggers a Google Chat notification to that person"
    - "Notes show the author's name, avatar, timestamp, and note content"
    - "ActivityLog records NOTE_ADDED and MENTIONED actions for audit trail"
  artifacts:
    - path: "apps/emails/models.py"
      provides: "InternalNote model and NOTE_ADDED/MENTIONED ActivityLog actions"
      contains: "class InternalNote"
    - path: "templates/emails/_thread_note.html"
      provides: "Note card template with distinct styling"
      min_lines: 10
    - path: "apps/emails/views.py"
      provides: "add_note_view endpoint and updated thread_detail with notes in timeline"
      contains: "def add_note_view"
    - path: "apps/emails/tests/test_notes.py"
      provides: "Tests for note creation, @mention parsing, permissions"
      min_lines: 40
  key_links:
    - from: "templates/emails/_thread_detail.html note form"
      to: "apps/emails/views.py:add_note_view"
      via: "hx-post for note submission"
      pattern: "add_note"
    - from: "apps/emails/views.py:add_note_view"
      to: "apps/emails/models.py:InternalNote"
      via: "ORM create"
      pattern: "InternalNote.objects.create"
    - from: "apps/emails/views.py:add_note_view"
      to: "apps/emails/services/assignment.py:notify_mention"
      via: "fire-and-forget notification on @mention"
      pattern: "notify_mention"
---

<objective>
Add internal notes with @mentions on threads. Notes are team-internal (never visible to email senders), appear inline in the thread timeline with distinct visual styling, and @mentions trigger Google Chat notifications.

Purpose: Enable team coordination on threads without leaving the triage tool.
Output: InternalNote model, note input form in thread detail, @mention notification via Chat, inline note display.
</objective>

<execution_context>
@/Users/uge/.claude/get-shit-done/workflows/execute-plan.md
@/Users/uge/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-collaboration/04-CONTEXT.md
@.planning/phases/03-conversation-ui/03-01-SUMMARY.md

Key existing code:
- `apps/emails/models.py` — Thread, Email, ActivityLog models (add InternalNote model, extend Action choices)
- `apps/emails/views.py` — thread_detail view (add notes to timeline), existing action endpoints
- `apps/emails/services/assignment.py` — _send_assignment_chat, notify_assignment_email patterns for notifications
- `apps/emails/services/chat_notifier.py` — ChatNotifier class with Google Chat Cards v2
- `apps/accounts/models.py` — User model with first_name, last_name, get_full_name(), avatar_url
- `templates/emails/_thread_detail.html` — thread detail panel (add note form + inline notes)

<interfaces>
<!-- Key models and services the executor needs -->

From apps/emails/models.py:
  Thread: pk, gmail_thread_id, subject, status, assigned_to, category, priority
          activity_logs (reverse FK from ActivityLog)
          emails (reverse FK from Email)

  ActivityLog.Action TextChoices:
    ASSIGNED, REASSIGNED, STATUS_CHANGED, ACKNOWLEDGED, CLOSED,
    AUTO_ASSIGNED, CLAIMED, SLA_BREACHED, NEW_EMAIL_RECEIVED, REOPENED, THREAD_CREATED

From apps/accounts/models.py:
  User: id, first_name, last_name, email, get_full_name(), avatar_url, is_active, role

From apps/emails/services/assignment.py:
  _send_assignment_chat(email_or_thread, assignee) — sends Google Chat card notification
  notify_assignment_email(email_or_thread, assignee) — sends Django email notification

From apps/emails/services/chat_notifier.py:
  ChatNotifier.notify_assignment(email, assignee) — builds Cards v2 and posts to webhook

Existing timeline pattern (from thread_detail view):
  timeline_items = merged list of emails + activity_logs sorted by timestamp
  Each item: { "type": "message"|"activity", "obj": model_instance, "timestamp": datetime }
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: InternalNote model, @mention parsing, notification service, and tests</name>
  <files>apps/emails/models.py, apps/emails/services/assignment.py, apps/emails/tests/test_notes.py</files>
  <behavior>
    - Creating an InternalNote with thread FK, author FK, and body stores the note
    - InternalNote uses SoftDeleteModel + TimestampedModel base classes
    - parse_mentions("Hey @john.doe check this") extracts ["john.doe"] (username format)
    - parse_mentions("No mentions here") returns []
    - parse_mentions("@alice.smith and @bob.jones") returns ["alice.smith", "bob.jones"]
    - ActivityLog gets NOTE_ADDED and MENTIONED action choices
    - Creating a note creates a NOTE_ADDED ActivityLog entry
    - @mention in note creates MENTIONED ActivityLog entry per mentioned user
    - notify_mention sends Google Chat notification (mock in tests)
  </behavior>
  <action>
1. **InternalNote model** in models.py (after ActivityLog class):
   ```python
   class InternalNote(SoftDeleteModel, TimestampedModel):
       thread = models.ForeignKey(Thread, on_delete=models.CASCADE, related_name="notes")
       author = models.ForeignKey(settings.AUTH_USER_MODEL, on_delete=models.SET_NULL, null=True, related_name="internal_notes")
       body = models.TextField()
       mentioned_users = models.ManyToManyField(settings.AUTH_USER_MODEL, blank=True, related_name="mentioned_in_notes")

       class Meta:
           ordering = ["created_at"]

       def __str__(self):
           return f"Note by {self.author} on {self.thread_id}"
   ```

2. **Extend ActivityLog.Action** with two new choices:
   - `NOTE_ADDED = "note_added", "Note Added"`
   - `MENTIONED = "mentioned", "Mentioned"`

3. **parse_mentions(body)** function in assignment.py:
   - Regex: `r'@([\w.]+)'` — extract usernames from note body
   - Return list of username strings (not User objects)
   - Place near top of assignment.py as a utility

4. **notify_mention(thread, note_author, mentioned_user)** function in assignment.py:
   - Build a simple Google Chat text message (not full Cards v2 — keep it lightweight):
     `f"{note_author.get_full_name()} mentioned you in a note on: {thread.subject}"`
   - Use `_send_mention_chat(thread, note_author, mentioned_user)` helper that posts to webhook
   - Fire-and-forget pattern (try/except, log errors, never raise)
   - Also send Django email notification using same pattern as notify_assignment_email

5. **Migration**: Run `makemigrations` after model changes.

6. **Tests** in test_notes.py:
   - Test InternalNote creation (model save, FK integrity)
   - Test parse_mentions with various inputs (no mentions, single, multiple, edge cases)
   - Test that NOTE_ADDED and MENTIONED are valid ActivityLog.Action choices
   - Test notify_mention with mocked Chat webhook (no real API calls)
  </action>
  <verify>
    <automated>cd /Users/uge/code/vipl-email-agent-threads && python manage.py makemigrations --check --dry-run && pytest apps/emails/tests/test_notes.py -x -v 2>&1 | tail -20</automated>
  </verify>
  <done>InternalNote model exists with migration, parse_mentions correctly extracts @usernames, notify_mention sends Chat+email notifications (mocked in tests), ActivityLog has NOTE_ADDED and MENTIONED actions, all tests pass</done>
</task>

<task type="auto">
  <name>Task 2: Note input form, inline display, and @mention autocomplete in thread detail</name>
  <files>apps/emails/views.py, apps/emails/urls.py, templates/emails/_thread_detail.html, templates/emails/_thread_note.html</files>
  <action>
1. **add_note_view(request, pk)** in views.py — POST endpoint:
   - Get thread: `get_object_or_404(Thread, pk=pk)`
   - Get body from POST data, strip whitespace, reject if empty
   - Create InternalNote: `InternalNote.objects.create(thread=thread, author=request.user, body=body)`
   - Create ActivityLog: `ActivityLog.objects.create(thread=thread, user=request.user, action=ActivityLog.Action.NOTE_ADDED, detail=body[:200])`
   - Parse @mentions: `usernames = parse_mentions(body)`
   - For each mentioned username, look up active User: `User.objects.filter(username=username, is_active=True).first()`
   - If found: add to note.mentioned_users, create MENTIONED ActivityLog, call `notify_mention(thread, request.user, mentioned_user)` in try/except
   - Return: re-rendered thread detail panel (call thread_detail view logic to rebuild full panel)

2. **Update thread_detail view** to include notes in timeline:
   - Load notes: `thread.notes.select_related("author").prefetch_related("mentioned_users")`
   - Add notes to the merged timeline alongside emails and activity_logs
   - Each note item: `{"type": "note", "obj": note, "timestamp": note.created_at}`
   - Sort entire timeline by timestamp

3. **URL pattern** in urls.py:
   ```python
   path("threads/<int:pk>/note/", views.add_note_view, name="add_note"),
   ```

4. **_thread_note.html** template — individual note card in timeline:
   - Amber/yellow background (`bg-amber-50 border-l-4 border-amber-400`) to distinguish from email messages
   - "Internal note" label in small amber text at top
   - Author avatar (first letter circle) + full name + timestamp (time_ago filter)
   - Note body text (plain text, preserve newlines with `{{ note.body|linebreaksbr }}`)
   - Sanitize with nh3 before rendering (XSS protection)
   - If mentioned_users exist, show subtle "@mentioned: Name1, Name2" below body

5. **Note input form** at bottom of _thread_detail.html (below the timeline, sticky to bottom):
   - Simple textarea (2 rows, auto-expand optional) with placeholder "Add an internal note... Use @username to mention"
   - Submit button (amber accent to match note styling)
   - `hx-post="{% url 'emails:add_note' thread.pk %}"` `hx-target="#thread-detail-panel"` `hx-swap="innerHTML"`
   - Clear textarea after successful submission (HTMX handles via full re-render)

6. **@mention autocomplete** (lightweight, no external library):
   - Inline `<script>` in _thread_detail.html or a small JS block
   - On textarea input, detect `@` character followed by typing
   - Show a small dropdown of team members (pass `team_members` from view context as JSON in a data attribute)
   - Filter dropdown as user types after `@`
   - On selection, insert `@username` into textarea and close dropdown
   - Dropdown positioned above/below textarea, simple absolute positioning
   - Keep it minimal: plain `<div>` with `<button>` items, basic keyboard nav (up/down/enter)
  </action>
  <verify>
    <automated>cd /Users/uge/code/vipl-email-agent-threads && python -c "from apps.emails.views import add_note_view; print('view exists')" && python manage.py check && pytest apps/emails -x -q 2>&1 | tail -5</automated>
  </verify>
  <done>Note input form at bottom of thread detail, notes appear inline in timeline with amber styling and "Internal note" label, @mention autocomplete shows team members, submitting a note re-renders the detail panel with the new note visible, all existing tests pass</done>
</task>

</tasks>

<verification>
1. `python manage.py runserver 8000` — open a thread detail
2. Note input form visible at bottom of detail panel
3. Type a note and submit — note appears inline with amber background and "Internal note" label
4. Type `@` in note textarea — autocomplete dropdown shows team members
5. Submit note with @mention — mentioned user gets Chat notification (check webhook or mock)
6. Notes appear in correct chronological position in timeline (between emails and activity events)
7. `pytest apps/emails -v` — all tests pass including new test_notes.py
</verification>

<success_criteria>
- Internal notes create, save, and display inline in thread timeline with distinct amber styling
- @mention autocomplete works in note textarea
- @mentions trigger Google Chat + email notifications
- ActivityLog tracks NOTE_ADDED and MENTIONED actions
- Notes are team-internal only (no external visibility path)
</success_criteria>

<output>
After completion, create `.planning/phases/04-collaboration/04-01-SUMMARY.md`
</output>
