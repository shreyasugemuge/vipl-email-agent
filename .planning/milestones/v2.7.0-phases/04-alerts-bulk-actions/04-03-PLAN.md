---
phase: 04-alerts-bulk-actions
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/emails/views.py
  - apps/emails/services/reports.py
  - templates/emails/_corrections_digest.html
  - templates/emails/thread_list.html
  - apps/emails/tests/test_corrections_digest.py
autonomous: true
requirements: [ALERT-04]

must_haves:
  truths:
    - "Gatekeeper/admin sees a collapsible corrections digest card above the thread list on the triage queue page"
    - "Digest shows correction counts for category changes, priority overrides, and spam corrections in last 7 days"
    - "Digest shows top 3-5 repeating patterns from ActivityLog (e.g., 'General -> Govt (5x)')"
    - "Members do not see the digest card"
    - "Collapse state is remembered via localStorage"
    - "Empty state shows 'No corrections in the last 7 days'"
  artifacts:
    - path: "apps/emails/services/reports.py"
      provides: "get_corrections_digest() function"
      contains: "def get_corrections_digest"
    - path: "templates/emails/_corrections_digest.html"
      provides: "Collapsible digest card partial"
    - path: "apps/emails/tests/test_corrections_digest.py"
      provides: "Tests for digest query and template rendering"
  key_links:
    - from: "apps/emails/views.py"
      to: "apps/emails/services/reports.py"
      via: "thread_list view calls get_corrections_digest()"
      pattern: "get_corrections_digest"
    - from: "templates/emails/thread_list.html"
      to: "templates/emails/_corrections_digest.html"
      via: "include partial for gatekeeper/admin only"
      pattern: "_corrections_digest"
---

<objective>
AI corrections digest card on the triage queue showing recent correction counts and top patterns for gatekeeper awareness.

Purpose: Gatekeepers see at a glance what the AI is getting wrong, helping them understand triage patterns and anticipate corrections needed.
Output: Collapsible digest card with correction counts and top patterns, rendered server-side on page load.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/milestones/v2.7.0-phases/04-alerts-bulk-actions/04-CONTEXT.md
@.planning/milestones/v2.7.0-phases/04-alerts-bulk-actions/04-RESEARCH.md
@.planning/milestones/v2.7.0-phases/04-alerts-bulk-actions/04-UI-SPEC.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Corrections digest query function + template + integration into thread_list view + tests</name>
  <files>
    apps/emails/services/reports.py,
    templates/emails/_corrections_digest.html,
    templates/emails/thread_list.html,
    apps/emails/views.py,
    apps/emails/tests/test_corrections_digest.py
  </files>
  <read_first>
    apps/emails/services/reports.py,
    apps/emails/models.py,
    apps/emails/views.py,
    templates/emails/thread_list.html
  </read_first>
  <behavior>
    - Test: get_corrections_digest returns zero counts when no ActivityLog entries exist
    - Test: get_corrections_digest counts CATEGORY_CHANGED, PRIORITY_CHANGED, SPAM_MARKED actions from last 7 days
    - Test: get_corrections_digest ignores entries older than 7 days
    - Test: get_corrections_digest returns top 5 patterns from detail text sorted by frequency
    - Test: get_corrections_digest returns total as sum of all correction types
    - Test: thread_list view includes corrections_digest in context for admin user on triage queue view
    - Test: thread_list view does NOT include corrections_digest for member user
  </behavior>
  <action>
    1. Add `get_corrections_digest()` function to `apps/emails/services/reports.py`:
       ```python
       def get_corrections_digest():
           """Build corrections digest for last 7 days from ActivityLog.

           Returns dict with correction counts by type, total, and top repeating patterns.
           Used by the triage queue page for gatekeeper/admin awareness.
           """
           from collections import Counter
           from datetime import timedelta
           from django.db.models import Count as DjCount
           from django.utils import timezone
           from apps.emails.models import ActivityLog

           cutoff = timezone.now() - timedelta(days=7)

           correction_actions = [
               ActivityLog.Action.CATEGORY_CHANGED,
               ActivityLog.Action.PRIORITY_CHANGED,
               ActivityLog.Action.SPAM_MARKED,
           ]

           # Counts by action type
           counts_qs = (
               ActivityLog.objects.filter(
                   action__in=correction_actions,
                   created_at__gte=cutoff,
               )
               .values("action")
               .annotate(c=DjCount("pk"))
           )
           counts = {row["action"]: row["c"] for row in counts_qs}

           category_changes = counts.get(ActivityLog.Action.CATEGORY_CHANGED, 0)
           priority_overrides = counts.get(ActivityLog.Action.PRIORITY_CHANGED, 0)
           spam_corrections = counts.get(ActivityLog.Action.SPAM_MARKED, 0)

           # Top patterns from detail text
           recent_details = list(
               ActivityLog.objects.filter(
                   action__in=[
                       ActivityLog.Action.CATEGORY_CHANGED,
                       ActivityLog.Action.PRIORITY_CHANGED,
                   ],
                   created_at__gte=cutoff,
               )
               .exclude(detail="")
               .exclude(detail__isnull=True)
               .values_list("detail", flat=True)[:100]
           )

           pattern_counts = Counter(recent_details)
           top_patterns = pattern_counts.most_common(5)

           return {
               "category_changes": category_changes,
               "priority_overrides": priority_overrides,
               "spam_corrections": spam_corrections,
               "total": category_changes + priority_overrides + spam_corrections,
               "top_patterns": top_patterns,  # list of (detail_text, count)
           }
       ```

    2. Create `templates/emails/_corrections_digest.html`:
       ```html
       {# AI Corrections Digest -- collapsible card for gatekeeper/admin #}
       {% if corrections_digest and corrections_digest.total > 0 %}
       <div class="mx-3 mb-2 rounded-xl border border-blue-200 bg-blue-50 overflow-hidden" id="corrections-digest-card">
           {# Header -- clickable to collapse #}
           <div class="flex items-center justify-between px-4 py-2 cursor-pointer select-none"
                onclick="toggleDigest()">
               <div class="flex items-center gap-2">
                   {# Lightbulb icon #}
                   <svg class="w-4 h-4 text-blue-500" fill="none" viewBox="0 0 24 24" stroke-width="1.5" stroke="currentColor">
                       <path stroke-linecap="round" stroke-linejoin="round" d="M12 18v-5.25m0 0a6.01 6.01 0 0 0 1.5-.189m-1.5.189a6.01 6.01 0 0 1-1.5-.189m3.75 7.478a12.06 12.06 0 0 1-4.5 0m3.75 2.383a14.406 14.406 0 0 1-3 0M14.25 18v-.192c0-.983.658-1.823 1.508-2.316a7.5 7.5 0 1 0-7.517 0c.85.493 1.509 1.333 1.509 2.316V18" />
                   </svg>
                   <span class="text-[11px] font-semibold text-blue-700">AI Corrections (7 days)</span>
               </div>
               <div class="flex items-center gap-2">
                   <span class="text-[11px] font-semibold text-blue-500">{{ corrections_digest.total }} corrections</span>
                   <svg id="digest-chevron" class="w-4 h-4 text-blue-400 transition-transform duration-200" fill="none" viewBox="0 0 24 24" stroke-width="2" stroke="currentColor">
                       <path stroke-linecap="round" stroke-linejoin="round" d="m19.5 8.25-7.5 7.5-7.5-7.5" />
                   </svg>
               </div>
           </div>

           {# Body -- collapsible #}
           <div id="digest-body" class="overflow-hidden transition-all duration-200"
                aria-expanded="true"
                style="max-height: 300px;">
               <div class="px-4 pb-3">
                   {# Correction counts row #}
                   <div class="flex items-center gap-4 flex-wrap">
                       <div class="flex items-center gap-1">
                           <span class="text-sm font-semibold text-amber-600">{{ corrections_digest.category_changes }}</span>
                           <span class="text-[11px] font-semibold text-slate-600">category</span>
                       </div>
                       <div class="flex items-center gap-1">
                           <span class="text-sm font-semibold text-red-600">{{ corrections_digest.priority_overrides }}</span>
                           <span class="text-[11px] font-semibold text-slate-600">priority</span>
                       </div>
                       <div class="flex items-center gap-1">
                           <span class="text-sm font-semibold text-orange-600">{{ corrections_digest.spam_corrections }}</span>
                           <span class="text-[11px] font-semibold text-slate-600">spam</span>
                       </div>
                   </div>

                   {% if corrections_digest.top_patterns %}
                   <div class="border-t border-blue-100 my-2"></div>
                   <div class="text-[10px] font-semibold text-blue-600 uppercase tracking-wider mb-1">Top Patterns</div>
                   <div class="space-y-0.5">
                       {% for pattern_text, pattern_count in corrections_digest.top_patterns %}
                       <div class="text-[11px] text-slate-600">{{ pattern_text }} ({{ pattern_count }}x)</div>
                       {% endfor %}
                   </div>
                   {% endif %}
               </div>
           </div>
       </div>
       {% elif corrections_digest and corrections_digest.total == 0 %}
       <div class="mx-3 mb-2 rounded-xl border border-blue-200 bg-blue-50 overflow-hidden">
           <div class="flex items-center gap-2 px-4 py-2">
               <svg class="w-4 h-4 text-blue-500" fill="none" viewBox="0 0 24 24" stroke-width="1.5" stroke="currentColor">
                   <path stroke-linecap="round" stroke-linejoin="round" d="M12 18v-5.25m0 0a6.01 6.01 0 0 0 1.5-.189m-1.5.189a6.01 6.01 0 0 1-1.5-.189m3.75 7.478a12.06 12.06 0 0 1-4.5 0m3.75 2.383a14.406 14.406 0 0 1-3 0M14.25 18v-.192c0-.983.658-1.823 1.508-2.316a7.5 7.5 0 1 0-7.517 0c.85.493 1.509 1.333 1.509 2.316V18" />
               </svg>
               <span class="text-[11px] text-blue-400 italic">No corrections in the last 7 days</span>
           </div>
       </div>
       {% endif %}
       ```

    3. Add digest collapse/expand JS. This can go in the same `_corrections_digest.html` partial or in `thread_list.html`:
       ```javascript
       function toggleDigest() {
           const body = document.getElementById('digest-body');
           const chevron = document.getElementById('digest-chevron');
           const isCollapsed = body.style.maxHeight === '0px';

           if (isCollapsed) {
               body.style.maxHeight = '300px';
               body.setAttribute('aria-expanded', 'true');
               chevron.style.transform = 'rotate(0deg)';
               localStorage.removeItem('digest_collapsed');
           } else {
               body.style.maxHeight = '0px';
               body.setAttribute('aria-expanded', 'false');
               chevron.style.transform = 'rotate(180deg)';
               localStorage.setItem('digest_collapsed', 'true');
           }
       }

       // Restore collapsed state on load
       document.addEventListener('DOMContentLoaded', function() {
           if (localStorage.getItem('digest_collapsed') === 'true') {
               const body = document.getElementById('digest-body');
               const chevron = document.getElementById('digest-chevron');
               if (body) {
                   body.style.maxHeight = '0px';
                   body.setAttribute('aria-expanded', 'false');
                   if (chevron) chevron.style.transform = 'rotate(180deg)';
               }
           }
       });
       ```

    4. In `templates/emails/thread_list.html`, include the digest partial above the thread list body, inside the condition that checks for triage queue view and admin/gatekeeper role:
       ```html
       {% if is_admin and view == "unassigned" %}
           {% include "emails/_corrections_digest.html" %}
       {% endif %}
       ```
       Place this AFTER the stat cards row and BEFORE the thread list body (`#thread-list-body`).

    5. In `apps/emails/views.py`, in the `thread_list` function, add the digest to context when the user is admin/gatekeeper and viewing the triage queue:
       ```python
       # After the existing is_admin check and view filtering
       corrections_digest = None
       if is_admin and view == "unassigned":
           from apps.emails.services.reports import get_corrections_digest
           corrections_digest = get_corrections_digest()
       ```
       Add `"corrections_digest": corrections_digest` to the context dict passed to the template.

    6. Create `apps/emails/tests/test_corrections_digest.py` with all test behaviors. Create ActivityLog entries with actions CATEGORY_CHANGED, PRIORITY_CHANGED, SPAM_MARKED with various `created_at` dates (within and beyond 7 days). Test the function returns correct counts and patterns. Test view integration: admin sees digest in context, member does not.
  </action>
  <verify>
    <automated>cd /Users/uge/code/vipl-email-agent-gatekeeper && python -m pytest apps/emails/tests/test_corrections_digest.py -x -v</automated>
  </verify>
  <acceptance_criteria>
    - apps/emails/services/reports.py contains `def get_corrections_digest()`
    - get_corrections_digest returns dict with keys: category_changes, priority_overrides, spam_corrections, total, top_patterns
    - templates/emails/_corrections_digest.html exists and contains `id="corrections-digest-card"`
    - templates/emails/_corrections_digest.html contains `text-amber-600` for category, `text-red-600` for priority, `text-orange-600` for spam
    - templates/emails/_corrections_digest.html contains `No corrections in the last 7 days` empty state
    - templates/emails/thread_list.html includes `_corrections_digest.html` conditionally for admin on unassigned view
    - apps/emails/views.py thread_list function contains `get_corrections_digest`
    - apps/emails/tests/test_corrections_digest.py has at least 7 test functions
    - All tests pass
  </acceptance_criteria>
  <done>Collapsible AI corrections digest card appears on triage queue for gatekeeper/admin showing correction counts and top patterns. Members cannot see it. Collapse state persists in localStorage.</done>
</task>

</tasks>

<verification>
- `pytest apps/emails/tests/test_corrections_digest.py -x -v` -- all digest tests pass
- `pytest apps/emails/tests/ -x -q` -- no regressions
- Digest card renders with correct colors and content structure
- Empty state renders when no corrections exist
</verification>

<success_criteria>
- Corrections digest shows accurate 7-day counts for category, priority, and spam corrections
- Top 5 patterns displayed with frequency counts
- Card is collapsible with localStorage persistence
- Only visible to gatekeeper/admin on triage queue view
</success_criteria>

<output>
After completion, create `.planning/milestones/v2.7.0-phases/04-alerts-bulk-actions/04-03-SUMMARY.md`
</output>
