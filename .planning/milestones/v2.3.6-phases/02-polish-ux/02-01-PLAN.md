---
phase: 02-polish-ux
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/emails/views.py
  - templates/emails/email_list.html
  - apps/emails/tests/test_views.py
autonomous: true
requirements:
  - UX-01
  - UX-02
  - UX-03

must_haves:
  truths:
    - "First-time users see a welcome banner with role-specific guidance that dismisses and stays dismissed for the session"
    - "When filters are active, a count badge and clear-all link appear between tabs and email list"
    - "Mobile stat cards snap cleanly when swiping horizontally"
  artifacts:
    - path: "templates/emails/email_list.html"
      provides: "Welcome banner HTML, filter indicator row, scroll-snap classes on stat cards"
      contains: "welcome-banner"
    - path: "apps/emails/views.py"
      provides: "active_filter_count in email_list view context"
      contains: "active_filter_count"
    - path: "apps/emails/tests/test_views.py"
      provides: "Tests for UX-01, UX-02, UX-03"
      contains: "TestWelcomeBanner"
  key_links:
    - from: "apps/emails/views.py"
      to: "templates/emails/email_list.html"
      via: "active_filter_count context variable"
      pattern: "active_filter_count"
    - from: "templates/emails/email_list.html"
      to: "sessionStorage"
      via: "JS welcome banner dismiss logic"
      pattern: "sessionStorage.*welcome_dismissed"
---

<objective>
Add welcome banner, active filter indicators, and scroll-snap stat cards to the email dashboard.

Purpose: Polish the dashboard with guided onboarding (UX-01), visible filter state (UX-02), and responsive scroll-snap on mobile stat cards (UX-03).
Output: Updated email_list.html with three UX features, updated views.py with filter count context, and tests.
</objective>

<execution_context>
@/Users/uge/.claude/get-shit-done/workflows/execute-plan.md
@/Users/uge/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/milestones/v2.3.4-phases/02-polish-ux/02-CONTEXT.md
@.planning/milestones/v2.3.4-phases/02-polish-ux/02-RESEARCH.md

<interfaces>
<!-- Key types and contracts the executor needs -->

From apps/emails/views.py (email_list view, lines 64-199):
- View already passes to context: `is_admin`, `current_view`, `current_status`, `current_priority`, `current_category`, `current_inbox`, `current_search`, `current_sort`, `dash_stats`, `total_count`
- HTMX partial returns `_email_list_body.html`; full page returns `email_list.html`
- Filter variables: `status`, `priority`, `category`, `inbox`, `search_query` are local vars in the view

From templates/emails/email_list.html:
- Stats bar: div with `class="flex items-center gap-2.5 overflow-x-auto"` containing `.stat-card` divs (line 13)
- Tab bar + filters: div at line 65
- Detail panel: `#detail-panel` div at line 171
- extra_js block: lines 188-216 (mobile detail panel + mobile filters toggle)

From templates/base.html:
- Toast animations defined: `@keyframes toast-in`, `@keyframes toast-out` (lines 71-72)
- Toast container: `#toast-container` with `fixed top-4 right-4 z-[100]` (line 272)
- Existing scrollbar-hide class (line 76)
- `user.first_name`, `user.role`, `user.is_staff` available in template context
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add active_filter_count to view + write tests for UX-01, UX-02, UX-03</name>
  <files>apps/emails/views.py, apps/emails/tests/test_views.py</files>
  <behavior>
    - TestWelcomeBanner: authenticated GET /emails/ response contains id="welcome-banner" div
    - TestWelcomeBanner: banner contains role-specific text (admin vs member)
    - TestWelcomeBanner: banner contains sessionStorage JS for dismiss logic
    - TestFilterIndicators: GET /emails/?status=new response contains "1 filter active" and "Clear all" link
    - TestFilterIndicators: GET /emails/?status=new&priority=HIGH contains "2 filters active"
    - TestFilterIndicators: GET /emails/ (no filters) does NOT contain "filter active" text
    - TestFilterIndicators: Clear all link preserves current view param
    - TestScrollSnap: GET /emails/ response contains "snap-x" and "snap-mandatory" in stat cards container
    - TestScrollSnap: Each stat card contains "snap-start" class
  </behavior>
  <action>
    1. In `apps/emails/views.py` `email_list` view, after the filter variables are set (after line 110), compute:
       ```python
       active_filter_count = sum(1 for f in [status, priority, category, inbox, search_query] if f)
       ```
       Add `"active_filter_count": active_filter_count` to the context dict (around line 194).

    2. In `apps/emails/tests/test_views.py`, add three test classes:
       - `TestWelcomeBanner`: Create admin user + member user. GET /emails/ as each. Assert `welcome-banner` div present. Assert admin sees "assign" guidance text. Assert member sees "My Emails" guidance text. Assert `sessionStorage` JS present.
       - `TestFilterIndicators`: GET /emails/?status=new, assert `active_filter_count` works and "1 filter active" text in response. GET with 2 filters, assert "2 filters active". GET with no filters, assert no "filter active" text. Assert clear-all link contains `?view=` param.
       - `TestScrollSnap`: GET /emails/, assert `snap-x` and `snap-mandatory` in response content. Assert `snap-start` appears (stat cards have it).

    Note: Tests will fail initially (RED) since templates aren't updated yet. That's expected -- Task 2 makes them GREEN.
  </action>
  <verify>
    <automated>cd /Users/uge/code/vipl-email-agent-ui-ux && source .venv/bin/activate && pytest apps/emails/tests/test_views.py -x -k "TestWelcomeBanner or TestFilterIndicators or TestScrollSnap" 2>&1 | tail -20</automated>
  </verify>
  <done>active_filter_count added to view context, all three test classes written and failing (RED phase)</done>
</task>

<task type="auto">
  <name>Task 2: Implement welcome banner, filter indicators, and scroll-snap in template</name>
  <files>templates/emails/email_list.html</files>
  <action>
    1. **Welcome banner (UX-01)** -- Add between `{% block content %}` opening div and the stats bar (before line 12). Use the toast-style design per CONTEXT.md decisions:
       - Div with `id="welcome-banner"`, initially hidden with `class="hidden"`
       - `bg-gradient-to-r from-primary-50 to-primary-100 border-b border-primary-200/60 px-4 md:px-5 py-3 shrink-0`
       - Role-specific text using `{% if is_admin %}` -- admin guidance about assigning emails/managing team, member guidance about checking My Emails/claiming unassigned
       - Greeting: "Welcome, {{ user.first_name|default:user.username }}!"
       - X dismiss button + "Don't show again" link
       - JS in `{% block extra_js %}`:
         - On page load: check `sessionStorage.getItem('vipl_welcome_dismissed')` AND `localStorage.getItem('vipl_welcome_permanent')`. Show banner only if neither is set.
         - X button: `sessionStorage.setItem('vipl_welcome_dismissed', '1')`, hide banner
         - "Don't show again": `localStorage.setItem('vipl_welcome_permanent', '1')`, hide banner
         - Auto-fade after 8 seconds if not interacted with (use `setTimeout` + `toast-out` animation)

    2. **Filter indicators (UX-02)** -- Add a conditional row between the tab bar div (line 158) and the main content split (line 161):
       ```html
       {% if active_filter_count %}
       <div class="flex items-center gap-2 px-4 md:px-5 py-1.5 bg-amber-50/60 border-b border-amber-200/40 shrink-0">
           <span class="text-[10px] font-bold text-amber-700">
               {{ active_filter_count }} filter{{ active_filter_count|pluralize }} active
           </span>
           <span class="text-[10px] text-slate-300">|</span>
           <a href="{% url 'emails:email_list' %}?view={{ current_view }}"
              class="text-[10px] font-bold text-amber-600 hover:text-amber-800 underline">
               Clear all
           </a>
       </div>
       {% endif %}
       ```

    3. **Scroll-snap (UX-03)** -- Modify the stat cards container (line 13):
       - Add `snap-x snap-mandatory` to the flex container div
       - Add `snap-start shrink-0` to each `.stat-card` div (they already have `min-w-[120px]`)
       - The existing `overflow-x-auto` and `scrollbar-hide` (from parent) already work

    After implementing, run the tests from Task 1 -- they should now pass (GREEN).
  </action>
  <verify>
    <automated>cd /Users/uge/code/vipl-email-agent-ui-ux && source .venv/bin/activate && pytest apps/emails/tests/test_views.py -x -k "TestWelcomeBanner or TestFilterIndicators or TestScrollSnap" && pytest -x 2>&1 | tail -20</automated>
  </verify>
  <done>Welcome banner visible with role-specific text and dismiss logic. Filter indicator shows "N filters active - Clear all" when filters applied. Stat cards have scroll-snap classes. All tests pass including full suite.</done>
</task>

</tasks>

<verification>
- `pytest apps/emails/tests/test_views.py -x` -- all new tests pass
- `pytest -x` -- full suite green, no regressions
- Manual: `python manage.py runserver 8000` then visit http://triage.local/emails/ -- welcome banner appears on first visit, dismisses on click, stays dismissed on refresh within same session
- Manual: Apply a filter (e.g., status=new) -- "1 filter active | Clear all" bar appears. Click "Clear all" -- filters reset but view tab stays.
- Manual: On mobile viewport, swipe stat cards -- they snap to card boundaries
</verification>

<success_criteria>
- Welcome banner shows role-specific greeting, dismisses via X or "Don't show again", respects sessionStorage/localStorage
- Filter indicator appears only when filters are active, shows correct count, clear-all preserves view tab
- Stat cards container has snap-x snap-mandatory, each card has snap-start
- All 381+ existing tests still pass, plus new UX tests
</success_criteria>

<output>
After completion, create `.planning/milestones/v2.3.4-phases/02-polish-ux/02-01-SUMMARY.md`
</output>
