---
phase: 05-dev-inspector
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/emails/views.py
  - templates/emails/inspect.html
autonomous: true
requirements:
  - DEV-01
  - DEV-02

must_haves:
  truths:
    - "Force poll button works in all modes, shows inline result banner with counts and duration, auto-dismisses after 5s"
    - "Force poll button shows 'Polling...' while in progress, re-enables on completion"
    - "Poll history table refreshes after force poll without full page reload"
    - "Poll history table shows interval column with time since previous poll"
    - "Poll gaps > 2x interval are highlighted amber in the interval column"
    - "Empty polls (found=0) render with muted/dimmed text color"
    - "Error/failure rows remain fully visible (not dimmed)"
    - "Poll history limited to 25 rows by default"
    - "Timestamps show 12-hour India time with AM/PM and relative time inline"
  artifacts:
    - path: "apps/emails/views.py"
      provides: "Updated force_poll (JSON response, all-mode access) and inspect (25-row limit, interval annotation)"
    - path: "templates/emails/inspect.html"
      provides: "Inline result banner, AJAX force poll, enhanced history table with interval/dimming/timestamps"
  key_links:
    - from: "templates/emails/inspect.html"
      to: "apps/emails/views.py::force_poll"
      via: "fetch() POST from JS, receives JSON with status/found/spam/duration"
      pattern: "fetch.*force.poll"
    - from: "templates/emails/inspect.html"
      to: "apps/emails/views.py::inspect"
      via: "poll_logs context with interval annotation"
      pattern: "poll_logs"
---

<objective>
Polish the dev inspector page: make force poll return inline results (no redirect), enhance the poll history table with interval tracking, gap highlighting, dimmed empty polls, and India-format timestamps.

Purpose: Dev inspector is the primary tool for monitoring poll health -- these changes make it immediately obvious when something is wrong (gaps, errors) and give instant feedback on force polls.
Output: Updated views.py (force_poll JSON + inspect query changes) and inspect.html (JS fetch, result banner, enhanced table)
</objective>

<execution_context>
@/Users/uge/.claude/get-shit-done/workflows/execute-plan.md
@/Users/uge/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-dev-inspector/05-CONTEXT.md

@apps/emails/views.py (lines 2372-2458 — inspect + force_poll views)
@templates/emails/inspect.html (full file — standalone inspector page)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Force poll inline result + all-mode access</name>
  <files>apps/emails/views.py, templates/emails/inspect.html</files>
  <action>
**views.py — force_poll view (line 2436):**
- Remove the mode restriction check — force poll should work in ALL modes, admin-only (auth check stays)
- The view already returns JsonResponse — keep that. Ensure the JSON response includes: `status`, `emails_found`, `emails_processed`, `spam_filtered`, `duration_ms`. Parse these from the subprocess stdout or, better, query the latest PollLog record created during the poll (more reliable than parsing stdout). After `subprocess.run` completes successfully, fetch `PollLog.objects.order_by('-started_at').first()` and return its fields as JSON.
- On subprocess error/timeout, return the error JSON as-is (already handled)

**inspect.html — Force poll button + inline banner (around line 101-109):**
- Replace the form POST + page submit with a JS `fetch()` POST to the force-poll URL
- Button: on click, set text to "Polling...", disable button. On response, re-enable button, restore text
- After successful response, show an inline result banner below the button: `"Poll complete: {found} found, {spam} spam, {duration}ms"` styled as a small green banner (background #166534, text #bbf7d0, rounded, padding 8px 16px, font-size 0.8rem)
- Auto-dismiss banner after 5s with a fade-out
- On error response, show red banner with error message, also auto-dismiss after 5s
- After force poll completes (success or error), also refresh the poll history table: fetch the inspect page via AJAX, extract the poll history table tbody HTML, and replace the existing tbody. Use vanilla JS (no HTMX — inspector is standalone). Specifically: `fetch('/emails/inspect/')` then use DOMParser to extract `#poll-history-tbody` content and swap it in.
- Add `id="poll-history-tbody"` to the table tbody element
- Add `id="force-poll-result"` to the result banner container (initially hidden/empty)
- Timer should NOT reset after force poll (per user decision — force poll is "extra")
- Extract CSRF token from the existing cookie or from a `{% csrf_token %}` hidden input
  </action>
  <verify>
    <automated>cd /Users/uge/code/vipl-email-agent-fixes && python -c "from apps.emails.views import force_poll, inspect; print('Views import OK')" && pytest apps/emails/ -x -q --no-header 2>&1 | tail -5</automated>
  </verify>
  <done>Force poll button triggers AJAX call, shows "Polling..." during execution, displays inline result banner with poll stats, banner auto-dismisses after 5s, poll history table refreshes after completion, works in all operating modes</done>
</task>

<task type="auto">
  <name>Task 2: Poll history table enhancements</name>
  <files>apps/emails/views.py, templates/emails/inspect.html</files>
  <action>
**views.py — inspect view (line 2374):**
- Change poll_logs query from `[:50]` to `[:25]` (limit to last 25 polls per user decision)
- After fetching poll_logs, annotate each log with an `interval` field: time difference between this poll's `started_at` and the previous poll's `started_at`. For the oldest log in the list, interval is None. Calculate server-side using a simple loop over the queryset results (they're already ordered by `-started_at`):
  ```python
  poll_logs = list(PollLog.objects.all()[:25])
  poll_interval_seconds = int(poll_interval) * 60
  for i, log in enumerate(poll_logs):
      if i < len(poll_logs) - 1:
          delta = log.started_at - poll_logs[i + 1].started_at
          log.interval_seconds = int(delta.total_seconds())
          log.interval_gap = log.interval_seconds > (poll_interval_seconds * 2)
      else:
          log.interval_seconds = None
          log.interval_gap = False
  ```

**inspect.html — Poll history table (line 177-215):**
- Add "Interval" column header between "Time" and "Status"
- For each row, render the interval:
  - If `log.interval_seconds` is None: show "--"
  - Otherwise format as human-readable: if < 60s show "{s}s", if < 3600s show "{m}m {s}s", else "{h}h {m}m"
  - If `log.interval_gap` is True: render interval text in amber (#f59e0b) with a small warning icon or amber background highlight
- **Dimmed empty polls:** If `log.emails_found == 0` AND `log.status == 'success'`, apply muted styling to the entire row: `color: #475569` (dimmed text) instead of the default `#cbd5e1`
- **Error/failure rows:** Keep full visibility — no dimming. Errors must remain obvious.
- **Timestamp format:** Change from `{{ log.started_at|date:"d M H:i:s" }}` to India 12-hour format with relative time. Use Django's `date` filter: `{{ log.started_at|date:"g:i:s A" }}` for 12-hour AM/PM. Add a small relative time in parentheses next to it using JS (calculate from `log.started_at` ISO timestamp). Add a `data-ts` attribute with the unix timestamp to each time cell, and a small JS snippet at the end that iterates all `[data-ts]` cells and appends `(Xm ago)` relative text in a `<span>` with muted color.
- Ensure the tbody has `id="poll-history-tbody"` (for Task 1's AJAX refresh)
  </action>
  <verify>
    <automated>cd /Users/uge/code/vipl-email-agent-fixes && python -c "
from apps.emails.views import inspect
print('inspect view OK')
# Verify PollLog model has expected fields
from apps.emails.models import PollLog
fields = [f.name for f in PollLog._meta.get_fields()]
assert 'started_at' in fields, 'Missing started_at'
assert 'emails_found' in fields, 'Missing emails_found'
assert 'duration_ms' in fields, 'Missing duration_ms'
print('PollLog fields OK:', fields)
" && pytest apps/emails/ -x -q --no-header 2>&1 | tail -5</automated>
  </verify>
  <done>Poll history table shows 25 rows max, has Interval column with human-readable durations, amber-highlighted gaps > 2x interval, dimmed empty successful polls, full-visibility error rows, 12-hour India timestamps with relative time</done>
</task>

</tasks>

<verification>
1. `python -c "from apps.emails.views import force_poll, inspect"` — imports clean
2. `pytest apps/emails/ -x -q` — all existing tests pass
3. Visual: Force poll button triggers AJAX, shows result banner, table refreshes
4. Visual: Poll history shows interval column, dimmed empty rows, amber gaps, 12-hour timestamps
</verification>

<success_criteria>
- Force poll returns inline JSON result with banner display, works in all modes
- Poll history table limited to 25 rows with interval, gap highlighting, dimming, and India timestamps
- No regressions in existing tests
- Inspector page remains standalone (no base.html/HTMX dependency)
</success_criteria>

<output>
After completion, create `.planning/phases/05-dev-inspector/05-01-SUMMARY.md`
</output>
