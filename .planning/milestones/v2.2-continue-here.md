---
phase: milestone-setup
task: requirements-confirmation
total_tasks: 4
status: in_progress
last_updated: 2026-03-14T10:06:43.681Z
---

<current_state>
Mid `/gsd:new-milestone` workflow for v2.2 "Polish & Hardening".
Research complete (4 parallel researchers + synthesizer). Requirements drafted and presented to user for confirmation. Awaiting "yes / adjust" before committing REQUIREMENTS.md and spawning roadmapper.
</current_state>

<completed_work>

- Step 1-2: Loaded context (PROJECT.md, MILESTONES.md, STATE.md) — v2.1 shipped, 7 phases, 18 plans
- Step 3: Version determined — v2.2
- Step 4: PROJECT.md updated with Current Milestone section (v2.2 Polish & Hardening)
- Step 5: STATE.md reset for new milestone
- Step 6: Committed milestone start docs (28e18ad)
- Step 7: Init context loaded, research preference persisted (workflow.research = true)
- Step 8: Research complete — 4 parallel researchers (Stack, Features, Architecture, Pitfalls) + synthesizer. All 5 files written and committed (1a4e14e):
  - .planning/research/STACK.md
  - .planning/research/FEATURES.md
  - .planning/research/ARCHITECTURE.md
  - .planning/research/PITFALLS.md
  - .planning/research/SUMMARY.md
- Step 9 (partial): Requirements scoped via AskUserQuestion:
  - AUTH: Google OAuth SSO (no password fallback)
  - SETT: Grouped form UI + type-aware inputs + pre-filled values + better coverage
  - BRND: Logo + theme colors + branded login
  - SPAM: Mark not spam + whitelist + whitelist management UI
  - CHAT: Richer cards + open-in-dashboard link
  - UX: Dashboard card polish + consistent brand in HTMX partials
- Requirements list presented, awaiting confirmation
</completed_work>

<remaining_work>

- Step 9: Get user confirmation on requirements list (yes/adjust), write REQUIREMENTS.md, commit
- Step 10: Spawn gsd-roadmapper (phases continue from phase 7, v2.1 ended there), get approval, commit
- Step 11: Display completion summary with next steps
</remaining_work>

<decisions_made>

- v2.2 version (not v3.0 — incremental polish, not major rewrite)
- Research enabled — user chose "Research first"
- django-allauth[socialaccount] is the only new library (researchers agreed)
- Whitelist skips regex pre-filter only, never bypasses AI triage (security decision from pitfalls research)
- No password fallback — user declined "Keep password fallback" option
- No SLA breach alerts in Chat — user didn't select that option
- Settings page is template-only rewrite (existing 6 save endpoints work)
</decisions_made>

<blockers>
- VIPL logo asset needs to be retrieved from Google Drive (external dependency for branding phase)
- Existing users may have blank email fields (must fix before allauth install)
</blockers>

<context>
User wants v2.2 to be UX polish + hardening on the live system. Not a feature-heavy milestone.
Key research findings: allauth needs server-side domain enforcement (hd param is UI-only), spam whitelist must not bypass AI triage, Tailwind v4 CDN + HTMX partials need @theme audit, SystemConfig type coercion is lenient (form must validate before save).
Suggested phase order from research: SSO first, then Settings+Spam, then Branding, then Chat polish.
</context>

<next_action>
Resume at Step 9: Present the requirements list again for confirmation, then write REQUIREMENTS.md.

The proposed requirements are:
- AUTH-01 to AUTH-03 (Google OAuth SSO)
- SETT-01 to SETT-04 (Settings page overhaul)
- BRND-01 to BRND-03 (VIPL branding)
- SPAM-01 to SPAM-04 (Spam learning/whitelist)
- CHAT-01 to CHAT-02 (Chat notification improvements)
- UX-01 to UX-02 (Dashboard polish)

After confirmation: commit REQUIREMENTS.md, spawn roadmapper starting from phase 8 (v2.1 had phases 1-6 + 4.5), present roadmap for approval, commit, done.
</next_action>
