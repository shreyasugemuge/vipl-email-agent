---
phase: 03-qa-verification
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .gitignore
  - .planning/milestones/v2.3.4-phases/03-qa-verification/03-QA-REPORT.md
autonomous: false
requirements:
  - QA-01

must_haves:
  truths:
    - "QA media directory exists and is gitignored"
    - "BUG-01 through BUG-07 verified working on live site with screenshot evidence"
    - "UX-01 through UX-05 verified working on live site with screenshot evidence"
    - "Console shows no JS errors on any tested page"
    - "All Phase 1+2 fixes confirmed with PASS/FAIL status in QA report"
  artifacts:
    - path: ".planning/qa/"
      provides: "Screenshot and GIF storage directory"
    - path: ".planning/milestones/v2.3.4-phases/03-qa-verification/03-QA-REPORT.md"
      provides: "QA report with Phase 1+2 verification results"
  key_links:
    - from: "Claude-in-Chrome MCP"
      to: "triage.vidarbhainfotech.com"
      via: "Active Google OAuth session in Chrome"
      pattern: "navigate.*triage.vidarbhainfotech.com"
---

<objective>
Verify all Phase 1 bug fixes (BUG-01 through BUG-07) and Phase 2 UX features (UX-01 through UX-05) are working correctly on the live site using Claude-in-Chrome browser automation.

Purpose: Confirm that all previous phase work actually works in production before doing a general sweep.
Output: QA report with PASS/FAIL for each requirement, screenshots as evidence, inline bug fixes if needed.
</objective>

<execution_context>
@/Users/uge/.claude/get-shit-done/workflows/execute-plan.md
@/Users/uge/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/milestones/v2.3.4-phases/03-qa-verification/03-CONTEXT.md
@.planning/milestones/v2.3.4-phases/03-qa-verification/03-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: QA setup and gitignore</name>
  <files>.gitignore</files>
  <action>
    1. Create `.planning/qa/` directory (for screenshots and GIF recordings)
    2. Add `.planning/qa/` to `.gitignore` so media files are ephemeral
    3. Verify the directory exists and gitignore entry is correct
  </action>
  <verify>
    <automated>test -d .planning/qa/ && grep -q "\.planning/qa" .gitignore && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <done>.planning/qa/ directory exists and is gitignored</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Checkpoint: Confirm Chrome OAuth session</name>
  <action>
    QA setup is complete. Before browser automation can begin, the user needs to have an active Google OAuth session in Chrome on triage.vidarbhainfotech.com.

    Steps for user:
    1. Open Chrome and navigate to https://triage.vidarbhainfotech.com
    2. Confirm you are logged in (you should see the email dashboard, not a login page)
    3. If not logged in, sign in with your @vidarbhainfotech.com Google account
    4. Type "ready" when Chrome has an active session on the live site
  </action>
  <verify>User confirms active OAuth session in Chrome</verify>
  <done>Chrome has active Google OAuth session on triage.vidarbhainfotech.com</done>
</task>

<task type="auto">
  <name>Task 2: Phase 1+2 verification and QA report</name>
  <files>.planning/milestones/v2.3.4-phases/03-qa-verification/03-QA-REPORT.md</files>
  <action>
    Use Claude-in-Chrome MCP tools to verify all Phase 1 and Phase 2 changes on the live site. For each item below, navigate to the relevant page, interact with elements, take screenshots, and check console for errors.

    **Phase 1 Bug Fix Verification (desktop 1440px first, then mobile 375px):**

    BUG-01: Navigate to /emails/, inspect email cards — AI suggestion badges should show clean text (e.g., "Shreyas"), NOT XML like `<parameter name="name">`. Screenshot evidence.

    BUG-02: Resize to 375px mobile viewport, reload page. Tap an email card — detail panel should slide in as full-screen overlay with back button. Verify scroll lock on body. Screenshot + GIF if possible.

    BUG-03: At 375px, verify filter bar shows stacked vertical layout with full-width selects, not horizontal overflow. Screenshot.

    BUG-04: Navigate to /emails/activity/ — verify filter chips are fully visible, "Priority Bump" not truncated on any viewport. Screenshot at desktop + mobile.

    BUG-05: On /emails/, switch between All/Unassigned/My Emails views — verify email count label updates correctly after each switch. Read network requests to confirm HTMX partial swap.

    BUG-06: Navigate to every page (/emails/, /emails/activity/, /emails/settings/, /accounts/team/) — verify page title follows "VIPL Triage | {Page Name}" pattern using javascript_tool to read document.title.

    BUG-07: At 375px, trigger a toast (e.g., save a setting or assign an email) — verify toast appears below header (not behind it) with touch-friendly close button. Screenshot.

    **Phase 2 UX Feature Verification:**

    UX-01: Clear session storage, navigate to /emails/ — verify welcome banner appears with role-specific guidance. Dismiss it. Reload page — verify it does NOT reappear in same session. Screenshot before and after dismiss.

    UX-02: Apply a filter (e.g., status=new), verify count badge and "clear all" link appear above email list. Click "clear all" — filters reset. Screenshot with active filters.

    UX-03: At 375px, verify stat cards have horizontal scroll-snap behavior. Swipe between cards — each should snap cleanly. Screenshot or GIF.

    UX-04: At desktop viewport, click an email card to open detail panel. Press Escape — panel should close. Use arrow keys to navigate between cards. Verify focus ring on selected card.

    UX-05: Click an email card — verify loading skeleton (pulsing placeholder) appears in detail panel while HTMX fetches content. May need to throttle network or look carefully at fast connections. Screenshot if capturable.

    **After each page/interaction:**
    - `read_console_messages(onlyErrors: true)` — document any JS errors
    - `read_network_requests` — verify HTMX requests return 200, no 404/500

    **If bugs found:**
    - Document in QA report with description and screenshot
    - Fix the code if within scope (template/CSS/JS fixes)
    - Run `pytest -x --tb=short` after each fix
    - Re-verify the fix in browser
    - Batch commit fixes by section

    **Write QA report** to `.planning/milestones/v2.3.4-phases/03-qa-verification/03-QA-REPORT.md` following the structure from RESEARCH.md. Include:
    - Summary (pages tested, interactions verified, bugs found/fixed)
    - Phase 1+2 Verification section with PASS/FAIL for each BUG-xx and UX-xx
    - Screenshots referenced (stored in .planning/qa/)
    - Bugs Found and Fixed table (if any)
    - Console error summary
  </action>
  <verify>
    <automated>test -f .planning/milestones/v2.3.4-phases/03-qa-verification/03-QA-REPORT.md && grep -c "PASS\|FAIL" .planning/milestones/v2.3.4-phases/03-qa-verification/03-QA-REPORT.md</automated>
  </verify>
  <done>QA report exists with PASS/FAIL status for all 12 items (BUG-01 through BUG-07 + UX-01 through UX-05). Screenshots captured for each verification. Any inline fixes committed with pytest passing.</done>
</task>

</tasks>

<verification>
- QA report has PASS/FAIL for every BUG-01..07 and UX-01..05
- Screenshots exist in .planning/qa/ for each verification
- Console error check performed on every tested page
- Any bugs found during QA are fixed, tested with pytest, and committed
- pytest full suite passes: `pytest -v`
</verification>

<success_criteria>
All 12 Phase 1+2 requirements verified with evidence. QA report documents results. No regressions in pytest suite.
</success_criteria>

<output>
After completion, create `.planning/milestones/v2.3.4-phases/03-qa-verification/03-01-SUMMARY.md`
</output>
