---
phase: 03-qa-verification
plan: 02
type: execute
wave: 2
depends_on:
  - 03-01
files_modified:
  - .planning/milestones/v2.3.4-phases/03-qa-verification/03-QA-REPORT.md
autonomous: false
requirements:
  - QA-01

must_haves:
  truths:
    - "Every HTMX interaction on the email detail panel works (assign, status, AI accept/reject, whitelist)"
    - "All 6 settings tabs save correctly with inline feedback"
    - "Team management toggles and role changes work"
    - "Activity log filters, date range, and pagination work"
    - "All pages render correctly at 375px mobile and 768px tablet viewports"
    - "No console errors or failed network requests on any page"
  artifacts:
    - path: ".planning/milestones/v2.3.4-phases/03-qa-verification/03-QA-REPORT.md"
      provides: "Complete QA report with all pages and viewports covered"
  key_links:
    - from: "Claude-in-Chrome MCP"
      to: "All 27 URL routes"
      via: "Systematic page-by-page walkthrough"
      pattern: "navigate.*triage.vidarbhainfotech.com"
---

<objective>
Complete general sweep of all pages and interactions not covered in Plan 01, plus viewport testing at mobile (375px) and tablet (768px). Finalize QA report.

Purpose: Ensure every interactive element across the entire dashboard works, not just the Phase 1+2 changes.
Output: Complete QA report covering all pages, viewports, and interactions. Any discovered bugs fixed inline.
</objective>

<execution_context>
@/Users/uge/.claude/get-shit-done/workflows/execute-plan.md
@/Users/uge/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/milestones/v2.3.4-phases/03-qa-verification/03-CONTEXT.md
@.planning/milestones/v2.3.4-phases/03-qa-verification/03-RESEARCH.md
@.planning/milestones/v2.3.4-phases/03-qa-verification/03-01-SUMMARY.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Checkpoint: Confirm Chrome OAuth session for sweep</name>
  <action>
    Plan 01 completed Phase 1+2 verification. This plan continues with the general sweep. Need to confirm Chrome still has an active OAuth session.

    Steps for user:
    1. Check Chrome is still logged into triage.vidarbhainfotech.com
    2. If session expired, sign in again with Google OAuth
    3. Type "ready" when session is active
  </action>
  <verify>User confirms active OAuth session in Chrome</verify>
  <done>Chrome has active Google OAuth session on triage.vidarbhainfotech.com</done>
</task>

<task type="auto">
  <name>Task 1: General sweep — all pages and interactions</name>
  <files>.planning/milestones/v2.3.4-phases/03-qa-verification/03-QA-REPORT.md</files>
  <action>
    Use Claude-in-Chrome MCP tools to systematically test every interactive element not already covered in Plan 01. Follow the QA workflow per page pattern from RESEARCH.md.

    **Email Detail Panel (/emails/{pk}/detail/):**
    - Open detail for an email. Verify all sections render (email body, AI triage, draft reply, activity).
    - Test assign dropdown — select a user, verify assignment saves (hx-post to /emails/{pk}/assign/).
    - Test Acknowledge button — verify status changes to "acknowledged" (hx-post to /emails/{pk}/status/).
    - Test Close button — verify status changes to "closed".
    - Test Accept AI Suggestion — verify it applies the suggestion.
    - Test Reject AI Suggestion — verify it clears the suggestion.
    - Test Whitelist Sender — verify it adds to whitelist.
    - Screenshot detail panel at desktop viewport.
    - `read_console_messages(onlyErrors: true)` after each interaction.
    - `read_network_requests` — all HTMX requests return 200.

    **Settings Page (/emails/settings/) — all 6 tabs:**
    - Tab 1: Assignment Rules — load tab, modify a rule, save. Verify inline save feedback.
    - Tab 2: Category Visibility — toggle a category, save. Verify feedback.
    - Tab 3: SLA Config — change a value, save. Verify feedback.
    - Tab 4: Inbox Config — verify inbox list renders. Save if editable.
    - Tab 5: System Config — verify key-value pairs render. Edit one, save. Verify feedback.
    - Tab 6: Webhooks — verify webhook config renders. Save. Verify feedback.
    - Screenshot each tab.
    - Check console errors after each tab switch and save.

    **Whitelist Tab (in Settings):**
    - Navigate to whitelist tab.
    - Add a test whitelist entry (if safe to do so — use a clearly fake email like test-qa@example.com).
    - Verify it appears in the list.
    - Delete the test entry.
    - Screenshot.

    **Team Management (/accounts/team/):**
    - Verify user list renders with roles and status.
    - Test toggle active/deactivate if safe (or just verify the button exists and is clickable).
    - Test change role dropdown.
    - Test toggle category visibility.
    - Screenshot.
    - Check console errors.

    **Activity Log (/emails/activity/):**
    - Verify activity entries render.
    - Test filter chips (click a filter, verify list updates via HTMX).
    - Test date range filter if available.
    - Test pagination (next/prev page).
    - Screenshot.
    - Check console errors.

    **Health Endpoint (/health/):**
    - Navigate to /health/, verify JSON response renders.
    - Screenshot.

    **Login Page (/accounts/login/):**
    - Open in a new tab or verify by looking at the page structure (don't log out!).
    - Verify VIPL branding, Google OAuth button present.
    - Screenshot.

    **Viewport Testing (mobile 375px + tablet 768px):**
    After desktop sweep, resize to 375px and reload each page:
    - /emails/ — verify mobile card layout, stacked filters, stat card scroll
    - /emails/{pk}/detail/ — verify full-screen slide-in panel
    - /emails/settings/ — verify tab navigation works on mobile
    - /emails/activity/ — verify filter chips wrap properly
    - /accounts/team/ — verify table/list is readable on mobile
    Resize to 768px and spot-check:
    - /emails/ — verify tablet layout
    - /emails/settings/ — verify settings layout
    Screenshot each viewport.

    **After all testing:**
    - `read_console_messages(onlyErrors: true)` — final sweep
    - Document ALL findings in the QA report

    **If bugs found:**
    - Document with description + screenshot
    - Fix if within scope (template/CSS/JS)
    - Run `pytest -x --tb=short` after each fix
    - Re-verify in browser
    - Batch commit fixes by page/section

    **Update QA report** — append "Page-by-Page Results" and "Viewport Testing" sections. Update summary counts. Add "Bugs Found and Fixed" table. Add "Remaining Issues" section (if any).
  </action>
  <verify>
    <automated>grep -c "###" .planning/milestones/v2.3.4-phases/03-qa-verification/03-QA-REPORT.md | xargs -I{} test {} -ge 10 && pytest -x --tb=short -q 2>&1 | tail -3</automated>
  </verify>
  <done>QA report covers all pages (email list, detail panel, settings 6 tabs, team management, activity log, health, login) at desktop + mobile + tablet viewports. Every interactive element tested. Console error check on every page. Any bugs found are fixed and committed. pytest passes.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Checkpoint: Final QA report approval</name>
  <action>
    Complete QA sweep of the entire dashboard. QA report is at `.planning/milestones/v2.3.4-phases/03-qa-verification/03-QA-REPORT.md` with:
    - PASS/FAIL for all Phase 1+2 items
    - Page-by-page results for every page
    - Viewport testing at desktop, mobile (375px), tablet (768px)
    - Bugs found and fixed (if any)
    - Screenshots in .planning/qa/

    Steps for user:
    1. Review the QA report at `.planning/milestones/v2.3.4-phases/03-qa-verification/03-QA-REPORT.md`
    2. Spot-check a few screenshots in `.planning/qa/`
    3. Optionally manually verify 2-3 interactions on the live site yourself
    4. If satisfied, type "approved"
    5. If issues remain, describe what needs re-testing
  </action>
  <verify>User approves QA report</verify>
  <done>QA report approved by user. Milestone is shippable.</done>
</task>

</tasks>

<verification>
- QA report covers ALL 27 URL routes from research inventory
- Every HTMX interaction tested (38 endpoint references across 17 templates)
- Screenshots captured for all pages at desktop viewport minimum
- Mobile (375px) and tablet (768px) viewport testing complete
- Console error checks on every page
- No unresolved critical bugs
- pytest full suite passes: `pytest -v`
</verification>

<success_criteria>
Complete QA report with evidence that all interactive elements work. No critical bugs remaining. pytest green. User approves the report.
</success_criteria>

<output>
After completion, create `.planning/milestones/v2.3.4-phases/03-qa-verification/03-02-SUMMARY.md`
</output>
