# Secrets — NEVER commit these
.env
*.json
!tests/sample_emails.json
!package.json
service-account*.json
*-sa-key.json
.taiga-credentials

# Database
db.sqlite3
*.sqlite3

# Python
__pycache__/
*.py[cod]
*.egg-info/
dist/
build/
.eggs/

# Virtual environment
venv/
.venv/
env/

# IDE
.vscode/
.idea/
*.swp
*.swo

# Logs
*.log
agent.log

# State file (contains processed IDs)
state.json

# Temp / deleted files (local-only, never commit)
to_delete/

# Claude / MCP / Swarm runtime (local-only)
.claude/
.claude-flow/
.swarm/
.mcp.json

# GSD planning (local-only — keep off main, OK in feature branches/worktrees)
.planning/

# Node (Tailwind CSS build only)
node_modules/
package-lock.json

# OS
.DS_Store
Thumbs.db
