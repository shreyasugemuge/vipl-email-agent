# VIPL Email Agent v2 -- Environment Variables
# Copy to .env and fill in values

# Django
SECRET_KEY=change-me-to-a-random-string
DEBUG=True
ALLOWED_HOSTS=localhost,127.0.0.1

# Database (PostgreSQL in production, SQLite used if not set in dev)
# DATABASE_URL=postgres://vipl_agent:vipl_agent_2026@host.docker.internal:5432/vipl_email_agent

# Application
APP_VERSION=dev
