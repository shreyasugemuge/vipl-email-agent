# ============================================================
# VIPL Email Agent v2 -- CI/CD Pipeline
# ============================================================
# Triggers:
#   - Version tags (v*.*.*): test -> deploy via SSH to VM
#   - Pull requests to v2:   test only (no deploy)
#
# Replaces v1 Cloud Run deployment with VM SSH deploy.
# ============================================================

name: Deploy & Release

on:
  push:
    tags:
      - 'v*.*.*'
  pull_request:
    branches: [v2]

jobs:
  # ---- Job 1: Run tests (always) ----
  test:
    name: Test
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Install dependencies
        run: pip install -r requirements-dev.txt

      - name: Run tests
        run: pytest --tb=short -q

  # ---- Job 2: Deploy to VM via SSH (tags only) ----
  deploy:
    name: Deploy to VM
    needs: test
    if: github.event_name == 'push' && startsWith(github.ref, 'refs/tags/v')
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - name: Deploy via SSH
        uses: appleboy/ssh-action@v1
        with:
          host: ${{ secrets.VM_HOST }}
          username: ${{ secrets.VM_USER }}
          key: ${{ secrets.VM_SSH_KEY }}
          script: |
            cd /opt/vipl-email-agent
            git fetch --tags
            git checkout ${{ github.ref_name }}
            docker compose build --no-cache
            docker compose up -d
            docker compose exec -T web python manage.py migrate --noinput
            echo "Deployed ${{ github.ref_name }}"
